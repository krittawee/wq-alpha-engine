---
phase: quick-260611-l3w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - grade.py
  - test_phase4.py
autonomous: true
requirements: [GRADE-SETTINGS-FIDELITY]

must_haves:
  truths:
    - "grade_one persists BRAIN's RETURNED settings, not the requested settings"
    - "The 11 alpha_kb.db rows with delay=0 are corrected to delay=1 in both the column and settings_json"
    - "A unit test asserts that a simulate() response whose settings differ from the request results in the BRAIN settings being persisted"
  artifacts:
    - path: "grade.py"
      provides: "Reads actual settings from alpha dict returned by get_alpha(); falls back to active_settings if absent"
    - path: "test_phase4.py"
      provides: "test_grade_records_brain_actual_settings test function"
  key_links:
    - from: "grade.py line ~186"
      to: "alpha dict (result of sim.get_alpha())"
      via: "alpha.get('settings', {})"
      pattern: "alpha\\.get\\(['\"]settings"
---

<objective>
Fix grade.py to record BRAIN's ACTUAL returned simulation settings instead of the
requested settings. Correct 11 stale alpha_kb.db rows where delay was stored as 0
but BRAIN ran them at delay=1. Add a unit test that pins the correct behavior.

Purpose: BRAIN silently coerces some settings (e.g., delay=0 → delay=1). The DB
must reflect what BRAIN actually ran, not what was requested, so downstream
analysis (correlation lookups, optimizer variants) works from correct data.

Output: grade.py (fixed), alpha_kb.db (11 rows corrected), test_phase4.py (1 new test)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/PROJECT.md
@/Users/winter.__.kor/quant/.planning/ROADMAP.md
@/Users/winter.__.kor/quant/.planning/STATE.md
@/Users/winter.__.kor/quant/grade.py
@/Users/winter.__.kor/quant/test_phase4.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read BRAIN's returned settings from alpha dict in grade_one</name>
  <files>grade.py</files>
  <behavior>
    - When BRAIN's alpha dict contains a "settings" key, those values are used for
      region, universe, delay, decay, neutralization, truncation, and settings_json.
    - When BRAIN's alpha dict has no "settings" key (or it is empty/None), the code
      falls back to active_settings (same behavior as today — backward-compatible).
    - The comment "record the actual settings used" is updated to accurately describe
      the fallback logic.
  </behavior>
  <action>
    At Step 5 in grade_one (lines ~186-204), replace the block that builds
    settings_json and the individual setting columns with logic that first checks
    whether the alpha dict returned by BRAIN carries a "settings" sub-dict.

    After line 156 (`sim, alpha = _simulate_to_alpha(...)`) the variable `alpha`
    is already in scope. The alpha dict is BRAIN's authoritative record and may
    contain a top-level "settings" key with the coerced values BRAIN actually used.

    Replace the Step 5 block as follows (in prose — do not inline code):

    1. Resolve `active_settings` as before: `settings if settings is not None else _BASE_SETTINGS`.
    2. Extract `brain_settings = alpha.get("settings") or {}`.
    3. For each of the six persisted fields (region, universe, delay, decay,
       neutralization, truncation) resolve the value as:
       `brain_settings.get(field, active_settings.get(field))`.
       This means BRAIN wins if present; active_settings is the fallback.
    4. Build `settings_json` from a dict of the six resolved values (not the full
       active_settings dict — keep it consistent with what is persisted in the columns).
    5. Update the comment on the block from "record the actual settings used for this
       alpha" to "record BRAIN's returned settings; fall back to requested settings
       if BRAIN response has no settings dict".
    6. In the alpha_dict construction (lines ~193-220), update the region/universe/
       delay/decay/neutralization/truncation values and settings_json to use the
       resolved variables from step 3-4 above. No other lines change.

    Do NOT change any function signatures. Do NOT change _BASE_SETTINGS. Do NOT
    modify _simulate_to_alpha. The fix is entirely within the Step 5 block of
    grade_one.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && ./venv/bin/python -c "
import grade, inspect
src = inspect.getsource(grade.grade_one)
assert 'alpha.get' in src and 'settings' in src, 'alpha.get(settings) not found in grade_one'
assert 'brain_settings' in src or 'brain_set' in src, 'brain_settings variable not found'
print('grade.py patch verified')
"</automated>
  </verify>
  <done>
    grade_one contains logic that reads alpha.get("settings") and uses it (with
    fallback) for all six setting columns and settings_json. No function signatures
    changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct 11 stale alpha_kb.db rows (delay 0 → 1)</name>
  <files>alpha_kb.db</files>
  <action>
    CRITICAL: alpha_kb.db is gitignored. This task MUST run on the MAIN working
    tree at /Users/winter.__.kor/quant/alpha_kb.db — NOT inside any git worktree.
    Verify the path resolves to the real file before executing.

    Write and execute a one-off Python script using ./venv/bin/python that:

    1. Opens /Users/winter.__.kor/quant/alpha_kb.db with sqlite3.
    2. For each of the 11 alpha_ids listed below, updates the row:
       a. Sets the `delay` column to 1.
       b. Reads the current `settings_json` text, parses it as JSON, sets
          "delay" to 1 in the dict, re-serialises it as JSON, and writes it
          back to `settings_json`.
    3. Commits after processing all 11 rows.
    4. Prints a summary of rows updated.
    5. Closes the connection.

    The 11 alpha_ids to correct:
      1Ygw09oz, 6XEloQY7, A13lmvqw, GrolxOpZ, MPx7ZExM,
      QPQ7zqjG, mLXj0GRp, mLXj0lGX, omYqGpdk, omYqrXrk, qMXj30AO

    Do NOT call any BRAIN API. BRAIN's authoritative delay=1 for all 11 is already
    known from the pre-diagnosis; this is a pure DB correction.

    Write the script to /tmp/fix_delay.py, run it with
    ./venv/bin/python /tmp/fix_delay.py, then verify (see below).
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && ./venv/bin/python -c "
import sqlite3, json
conn = sqlite3.connect('alpha_kb.db')
ids = ['1Ygw09oz','6XEloQY7','A13lmvqw','GrolxOpZ','MPx7ZExM',
       'QPQ7zqjG','mLXj0GRp','mLXj0lGX','omYqGpdk','omYqrXrk','qMXj30AO']
bad = []
for aid in ids:
    row = conn.execute('SELECT delay, settings_json FROM alphas WHERE alpha_id=?',(aid,)).fetchone()
    if row is None:
        bad.append(f'{aid}: NOT FOUND')
        continue
    col_delay, sj = row
    sj_delay = json.loads(sj).get('delay') if sj else None
    if col_delay != 1 or sj_delay != 1:
        bad.append(f'{aid}: col={col_delay} sj={sj_delay}')
conn.close()
if bad:
    print('FAIL:', bad); raise SystemExit(1)
print('All 11 rows verified delay=1 in column and settings_json')
"</automated>
  </verify>
  <done>
    All 11 rows have delay=1 in the delay column AND delay=1 inside settings_json.
    No BRAIN API was called. alpha_kb.db is not committed (gitignored).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit test — grade records BRAIN's returned settings, not requested settings</name>
  <files>test_phase4.py</files>
  <behavior>
    - A mock simulate() is configured so that the returned alpha dict contains
      "settings": {"delay": 1} while the requested settings specify delay=0.
    - After grade_one runs, the persisted DB row has delay=1 (BRAIN's value).
    - After grade_one runs, json.loads(settings_json)["delay"] == 1.
    - The test uses only in-memory SQLite and MagicMock — zero BRAIN API calls.
  </behavior>
  <action>
    Append a new test function `test_grade_records_brain_actual_settings` to
    test_phase4.py, after the existing OPT-03 tests (end of file).

    The test must:

    1. Use only stdlib (sqlite3, json, unittest.mock.MagicMock, unittest.mock.patch)
       and the existing `conn` fixture or create its own in-memory DB via
       `db.init_db(":memory:")`.
    2. Build a mock BRAIN client where `client.simulate()` returns a mock `sim`
       object:
       - `sim.wait(verbose=False)` is a no-op.
       - `sim.alpha_id` is "TEST_BRAIN_ACTUAL".
       - `sim.get_alpha()` returns a dict with:
         - `"id"`: "TEST_BRAIN_ACTUAL"
         - `"is"`: `{"sharpe": 1.5, "fitness": 1.2, "checks": [{"result": "PASS", "name": "SHARPE"}]}`
         - `"settings"`: `{"delay": 1, "region": "USA", "universe": "TOP3000",
           "decay": 15, "neutralization": "SUBINDUSTRY", "truncation": 0.08}`
    3. Call `grade.grade_one(client, conn, "close / open", run_id="test-run",
       settings={"delay": 0, "region": "USA", "universe": "TOP3000",
       "decay": 15, "neutralization": "SUBINDUSTRY", "truncation": 0.08})`.
       Note: `settings` is passed with delay=0 (the REQUEST), but the mock BRAIN
       returns delay=1 (BRAIN's coerced value).
    4. Read the persisted row from `conn`:
       `conn.execute("SELECT delay, settings_json FROM alphas WHERE alpha_id=?",
       ("TEST_BRAIN_ACTUAL",)).fetchone()`
    5. Assert `row[0] == 1` (delay column is BRAIN's value).
    6. Assert `json.loads(row[1])["delay"] == 1` (settings_json reflects BRAIN's value).

    Include a docstring explaining the requirement (BRAIN is source of truth for
    settings; delay=0 request coerced by BRAIN to delay=1 must be recorded as 1).

    The test must not call any real BRAIN endpoint. It must pass with the fixed
    grade.py and fail against the unfixed grade.py (that's the red-green contract).
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && ./venv/bin/python -m pytest test_phase4.py test_phase3.py -q -k "test_grade_records_brain_actual_settings or not test_grade_records_brain_actual_settings" 2>&1 | tail -10</automated>
  </verify>
  <done>
    test_grade_records_brain_actual_settings passes. Full suites test_phase4.py and
    test_phase3.py remain green. No BRAIN API calls in the test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BRAIN API response → grade.py | BRAIN can return settings that differ from requested; treat response as authoritative, not the request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l3w-01 | Tampering | alpha_kb.db delay correction | accept | One-off script with explicit alpha_id list; no dynamic lookup; verified by post-check query |
| T-l3w-02 | Information Disclosure | settings_json mismatch | mitigate | Task 1 fix ensures BRAIN's settings are persisted; Task 3 test pins the behavior |
</threat_model>

<verification>
After all three tasks complete:

1. `./venv/bin/python -m pytest test_phase4.py test_phase3.py -q` — all green, including `test_grade_records_brain_actual_settings`.
2. Manual spot-check: `alpha.get("settings")` branch exists in grade.py Step 5 block.
3. DB check: all 11 alpha_ids have delay=1 in column and settings_json.
</verification>

<success_criteria>
- grade_one reads BRAIN's returned settings dict (with fallback) — code change is in place.
- All 11 stale rows in alpha_kb.db have delay=1 in both storage locations.
- New unit test passes; test_phase4.py and test_phase3.py suites remain fully green.
- No function signatures changed in grade.py.
- No BRAIN API called in either the DB correction or the test.
</success_criteria>

<output>
Create `.planning/quick/260611-l3w-grade-actual-settings-fix/260611-l3w-SUMMARY.md` when done.

Note in the SUMMARY that alpha_kb.db changes are NOT in git (gitignored) but all 11
rows were verified corrected. Record the verification query output as evidence.
</output>
