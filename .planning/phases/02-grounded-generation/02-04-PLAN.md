---
phase: 02-grounded-generation
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - test_phase2.py
autonomous: false
requirements:
  - GEN-01
  - GEN-02
user_setup: []

must_haves:
  truths:
    - "Running /find-alphas (via find_alphas.find_alphas) produces a thesis note citing real catalog operators/fields + >=1 SQLite insight (criterion 1)"
    - "The local validator rejects ZERO Ideator queueable outputs for unknown tokens (criterion 2)"
    - "Every generated candidate is archetype-tagged and confirmed absent from alphas.expression via db.expr_exists before queueing (criterion 3)"
    - "A human confirms the emitted Obsidian note reads as a genuine grounded thesis"
  artifacts:
    - path: "test_phase2.py"
      provides: "Automated end-to-end assertions for the 3 phase success criteria"
      contains: "def test_criterion"
      min_lines: 60
  key_links:
    - from: "test_phase2.py"
      to: "find_alphas.find_alphas + researcher + ideator + validate + db"
      via: "end-to-end pipeline run + criterion assertions"
      pattern: "import find_alphas"
---

<objective>
End-to-end checkpoint for Phase 2: run the full `/find-alphas` pipeline against the live `alpha_kb.db` and machine-verify all three ROADMAP success criteria, then have the human confirm the emitted Obsidian thesis note reads as a genuine grounded thesis. Optionally hand a small queueable batch to `python cli.py` (≤3 workers, single-shot login) as a manual smoke test — but the automated checkpoint itself does NOT grade (D-02).

Purpose: prove GEN-01 + GEN-02 are satisfied — criterion 1 (note cites real tokens + >=1 SQLite insight), criterion 2 (validator rejects zero Ideator outputs), criterion 3 (every candidate archetype-tagged + expr_exists-confirmed absent).

Output: `test_phase2.py` (automated criterion assertions) + a human-verify checkpoint approving the note quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-grounded-generation/02-CONTEXT.md
@CLAUDE.md
@db.py
@validate.py
@.planning/phases/02-grounded-generation/02-01-SUMMARY.md
@.planning/phases/02-grounded-generation/02-02-SUMMARY.md
@.planning/phases/02-grounded-generation/02-03-SUMMARY.md

<interfaces>
<!-- Full Phase 2 surface. Use directly. -->

From find_alphas.py (02-03):
- find_alphas(db_path="alpha_kb.db", archetype=None, prose=None) -> dict
    # {run_id, note_path, archetype, candidate_count, queueable_count}

From researcher.py (02-01): build_thesis(conn, archetype=None) -> dict
From ideator.py (02-02): generate_candidates(conn, thesis) -> list[dict]; queueable(candidates) -> list[dict]
From validate.py: validate(conn, expression) -> tuple[bool, str]
From db.py: init_db(path) -> conn; expr_exists(conn, expression) -> Optional[str]

8 archetype labels: reversal, momentum, value_garp, quality, growth, low_volatility, liquidity_volume, sentiment_event.

Grading handoff (manual smoke test only, D-02 Path A):
  python cli.py <seeds-file> [--workers {1,2,3}]   # single-shot login; workers clamped <=3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated end-to-end criterion tests</name>
  <files>test_phase2.py</files>
  <read_first>
    - test_phase2.py (the file being created — start empty)
    - find_alphas.py + researcher.py + ideator.py + validate.py + db.py (full Phase 2 + Phase 1 surface)
    - .planning/ROADMAP.md Phase 2 Success Criteria (the 3 criteria being asserted)
    - .planning/phases/02-grounded-generation/02-CONTEXT.md §success_criteria
  </read_first>
  <action>
    Create `test_phase2.py` with `import find_alphas, researcher, ideator, validate, db, os, re`. Add `test_criterion_1_grounded_note()`: run find_alphas.find_alphas(); read the emitted note; assert it exists, parse `source_operators`/`source_datafields` from frontmatter and assert every token is present in operators.name / the synced datafields.id slice (re-query the DB), and assert the note contains >=1 cited insight tied to a real alpha_id (cited_alpha_ids non-empty and the body references it). Add `test_criterion_2_validator_rejects_zero()`: build a thesis, generate candidates, take the queueable set, and assert `validate.validate(conn, expr)[0] is True` for EVERY queueable candidate (zero unknown-token rejections). Add `test_criterion_3_tagged_and_novel()`: assert every queueable candidate has a non-empty archetype in the 8-label set AND `db.expr_exists(conn, expr) is None` (confirmed absent before queueing). Each test opens its own conn via db.init_db and closes it. Tests make NO BRAIN/grade/simulate calls (D-02). Structure so `python test_phase2.py` runs all three and exits 0 on pass, non-zero on any failure (plain asserts + a __main__ runner, or pytest-compatible).
  </action>
  <verify>
    <automated>python test_phase2.py</automated>
  </verify>
  <acceptance_criteria>
    - `test_phase2.py contains def test_criterion_1_grounded_note`, `def test_criterion_2_validator_rejects_zero`, `def test_criterion_3_tagged_and_novel`.
    - `python test_phase2.py` exits 0 (all 3 criteria pass).
    - Criterion 1 test asserts every source_operators/source_datafields token in the note exists in the live catalog AND >=1 insight cites a real alpha_id.
    - Criterion 2 test asserts validate.validate returns True for EVERY queueable candidate (zero rejections).
    - Criterion 3 test asserts every queueable candidate is archetype-tagged (in the 8-label set) and db.expr_exists returns None.
    - test_phase2.py makes ZERO grade/simulate/login calls (verify: `grep -v '^#' test_phase2.py | grep -c -E 'grade\\.|simulate\\(|login\\('` returns 0) — D-02.
  </acceptance_criteria>
  <done>All three criterion tests pass under `python test_phase2.py`; the suite proves criteria 1/2/3 with no grading/sim calls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies thesis note quality + optional grading smoke test</name>
  <what-built>
    The full Phase 2 pipeline: `/find-alphas` (find_alphas.py) orchestrating researcher.py -> ideator.py, emitting a grounded Obsidian thesis note to alpha-kb/Theses/ with validated, deduped, archetype-tagged candidates in seeds.txt format, plus a runs row. test_phase2.py has already machine-verified all 3 success criteria.
  </what-built>
  <how-to-verify>
    1. Open the most recent note in `alpha-kb/Theses/` (path printed by the latest find_alphas run / test).
    2. Confirm the `## Thesis` and `## Economic rationale` sections read as a genuine, grounded economic claim (not placeholder text) — the hybrid LLM prose (D-03).
    3. Confirm the `## Grounding` tables list real operators/fields and the `## Past-result insight cited` section wikilinks >=1 real alpha_id with a plausible insight (criterion 1).
    4. Confirm the `## Candidate expressions` fenced block is valid seeds.txt format (4-8 lines, one FastExpr each) and the dedup table shows each candidate as novel.
    5. OPTIONAL smoke test (Path A, D-02 — human runs this, NOT the command): copy the candidate block into a seeds file and run `python cli.py <seeds-file> --workers 3`. Confirm single-shot login, <=3 concurrent sims, and that candidates grade without unknown-token validation failures. STOP if a 401 surfaces (do not re-auth).
  </how-to-verify>
  <resume-signal>Type "approved" if the note reads as a genuine grounded thesis (and the optional smoke test, if run, graded cleanly), or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test_phase2.py -> live alpha_kb.db | read-only criterion checks + note emit; no grading |
| human smoke test -> BRAIN (cli.py) | the ONLY place a BRAIN call may occur, human-initiated, single-shot login, <=3 workers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Elevation of Privilege | checkpoint auto-grading -> sim/auth lockout | mitigate | test_phase2.py makes ZERO grade/simulate/login calls (grep gate); grading is human-initiated cli.py only (D-02) |
| T-02-13 | Denial of Service | repeated login in smoke test -> 429 BIOMETRICS_THROTTLED | mitigate | smoke test uses cli.py single-shot login; instruction says STOP on 401, never re-auth (CLAUDE.md) |
| T-02-14 | Spoofing | note prose claims tokens not in catalog | mitigate | criterion-1 test re-queries DB and asserts every cited token is catalog-present |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | no package installs — test imports stdlib + Phase 1/2 modules only |
</threat_model>

<verification>
- `python test_phase2.py` exits 0 — all 3 success criteria machine-verified.
- Criterion 1: note cites real catalog tokens + >=1 SQLite insight.
- Criterion 2: validator rejects zero queueable Ideator outputs.
- Criterion 3: every queueable candidate archetype-tagged + expr_exists-confirmed absent.
- Human confirms the note reads as a genuine grounded thesis (D-03 prose quality).
- No grading/sim in the automated path (D-02); optional smoke test is human-initiated via cli.py only.
</verification>

<success_criteria>
All three Phase 2 success criteria are machine-verified by test_phase2.py and the human approves the emitted thesis note as a genuine grounded thesis. The phase delivers GEN-01 + GEN-02 end-to-end: a grounded thesis note with validated, deduped, archetype-tagged candidates, stopping cleanly for the human grading handoff (D-02).
</success_criteria>

<output>
Create `.planning/phases/02-grounded-generation/02-04-SUMMARY.md` when done.
</output>
