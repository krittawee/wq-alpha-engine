---
phase: 02-grounded-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - ideator.py
autonomous: true
requirements:
  - GEN-02
user_setup: []

must_haves:
  truths:
    - "ideator.py composes 4-8 FastExpr candidates from a thesis dict using grounded archetype skeletons"
    - "Every candidate passes validate.validate(conn, expr) — the validator rejects ZERO ideator outputs for unknown tokens (criterion 2)"
    - "Every candidate is checked against db.expr_exists(conn, expr) and flagged duplicate vs novel (criterion 3)"
    - "Every candidate inherits the single archetype tag from the thesis (one thesis = one archetype, D-04)"
    - "ideator.py can emit candidates in exact seeds.txt format (one FastExpr/line) for the human grading handoff (D-02 Path A)"
  artifacts:
    - path: "ideator.py"
      provides: "Ideator: skeleton->candidate composition, validate gate, expr_exists dedup, archetype inheritance, seeds.txt emit"
      contains: "def generate_candidates"
      min_lines: 90
  key_links:
    - from: "ideator.py"
      to: "validate.validate"
      via: "per-candidate validation gate before queueing"
      pattern: "validate\\.validate"
    - from: "ideator.py"
      to: "db.expr_exists"
      via: "per-candidate dedup against alphas.expression"
      pattern: "db\\.expr_exists"
---

<objective>
Build `ideator.py` — turns a thesis dict (from 02-01 `researcher.build_thesis`) into 4–8 FastExpr candidates (LOCKED D-01) drawn from the grounded archetype skeletons, gates EVERY candidate through `validate.validate(conn, expr)` (criterion 2 — validator rejects zero outputs), dedups EVERY candidate through `db.expr_exists(conn, expr)` (criterion 3), inherits the single archetype tag from the thesis (D-04), and can serialize the surviving candidates to exact seeds.txt format for the human grading handoff (D-02 Path A).

Purpose: GEN-02 — an Ideator agent generates FastExpr expressions using only verified operators/fields, tags each by archetype, and dedupes against the DB.

Output: `ideator.py` exporting a candidate-generation entrypoint returning candidate records (expression, archetype, valid, validation_reason, dedup_alpha_id) plus a seeds.txt serializer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-grounded-generation/02-CONTEXT.md
@.planning/phases/02-grounded-generation/02-GROUNDING.md
@CLAUDE.md
@db.py
@validate.py
@.planning/phases/02-grounded-generation/02-01-SUMMARY.md

<interfaces>
<!-- Phase 1 + 02-01 contracts. Use directly — no exploration needed. -->

From validate.py:
- validate(conn, expression: str) -> tuple[bool, str]   # validate.py:23 — THE GATE (criterion 2)
    Returns (True, "") if valid; else (False, reason). Fail-fast order:
    "empty expression" -> "unbalanced parentheses" -> "unknown operator: {tok}" -> "unknown data field: {tok}".
    Operator token = identifier immediately before "(". Field token = remaining identifier
    not in _EXCLUSIONS keyword set. Pure numeric literals never flagged.

From db.py:
- expr_exists(conn, expression: str) -> Optional[str]   # db.py:149 — THE DEDUP HELPER (criterion 3)
    Exact-string match (no normalization). Returns existing alpha_id or None. DO NOT hand-roll this SELECT.
- init_db(path="alpha_kb.db") -> sqlite3.Connection      # db.py:55

From researcher.py (02-01):
- build_thesis(conn, archetype=None) -> dict             # keys: archetype, source_operators, source_datafields,
                                                         #       cited_alpha_ids, cited_insights, region, universe, delay
- module-level archetype -> {operators, datafields} seed-token mapping (8 labels)

seeds.txt format (cli.py:62-64 consumes it):
  one FastExpr expression per line; blank lines ignored; lines starting with '#' ignored.
</interfaces>

Archetype skeletons (LOCKED, 02-GROUNDING.md §Archetype taxonomy — pseudo-valid, must still pass validate):
- reversal:          rank(reverse(ts_delta(close, 5)))
- momentum:          rank(ts_decay_linear(ts_delta(ts_delay(close, 21), 231), 5))
- value_garp:        group_neutralize(winsorize(rank(divide(bookvalue_ps, close)), std=4), industry)
- quality:           group_zscore(rank(divide(operating_income, assets)), industry)
- growth:            rank(divide(ts_delta(actual_sales_value_annual, 252), abs(ts_delay(actual_sales_value_annual, 252))))
- low_volatility:    rank(reverse(ts_std_dev(returns, 60)))
- liquidity_volume:  trade_when(greater(ts_zscore(volume, 20), 1), rank(ts_corr(close, volume, 20)), -1)
- sentiment_event:   group_neutralize(rank(ts_decay_linear(ts_mean(vec_avg(nws12_afterhsz_sl), 5), 5)), industry)

Steering (LOCKED §Steering notes): wrap final signal in cross-sectional normalizer (rank/zscore/scale);
prefer industry/subindustry neut; winsorize(signal,std=4) for self-corr control; reduce VECTOR fields with vec_avg.
Skeletons are pseudo-valid — exact arg order/windows/field ids MUST pass validate.validate before queueing.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Candidate composition from grounded skeletons + archetype inheritance</name>
  <files>ideator.py</files>
  <read_first>
    - ideator.py (the file being created — start empty)
    - researcher.py (build_thesis output shape + archetype->seed-token mapping)
    - .planning/phases/02-grounded-generation/02-GROUNDING.md §Archetype taxonomy (skeleton table + steering notes)
    - .planning/phases/02-grounded-generation/02-CONTEXT.md (D-01 4-8 candidates; D-04 one thesis = one archetype inherited)
    - CLAUDE.md (VECTOR fields wrap in vec_avg; never re-auth; no sim in this module)
  </read_first>
  <behavior>
    - generate_candidates(conn, thesis, n=None) consumes a thesis dict from researcher.build_thesis and produces between 4 and 8 candidate records (D-01). If n is given it is clamped to [4, 8].
    - Each candidate is derived from the thesis archetype's grounded skeleton plus grounded variations (window/field/normalizer swaps drawn ONLY from thesis.source_operators and thesis.source_datafields).
    - Every candidate record carries archetype == thesis['archetype'] (inherited; one thesis = one archetype, D-04) — no per-candidate classifier.
    - VECTOR datafields used in a candidate are wrapped in vec_avg/vec_sum (per steering notes); the final signal is wrapped in a cross-sectional normalizer.
    - Candidate record shape: {expression, archetype, valid, validation_reason, dedup_alpha_id} (valid/dedup populated in Task 2).
  </behavior>
  <action>
    Create `ideator.py` with `import db, validate, sqlite3` and `import researcher`. Add a module-level skeleton map keyed by the 8 archetype labels (copy the skeleton FastExprs from the context block). Add `generate_candidates(conn, thesis, n=None)` that selects the skeleton for `thesis['archetype']`, then composes 4-8 candidates by applying grounded variations (swap windows, swap in alternate operators/fields from thesis.source_operators / thesis.source_datafields, add winsorize/normalizer wrappers per steering notes). Clamp count to [4,8]. Stamp `archetype = thesis['archetype']` on every record (inheritance, D-04). Wrap any VECTOR-typed field token in vec_avg. Return a list of candidate dicts (valid/dedup fields left for Task 2). Do NOT call grade/simulate/login.
  </action>
  <verify>
    <automated>python -c "import ideator, researcher, db; c=db.init_db('alpha_kb.db'); t=researcher.build_thesis(c); cs=ideator.generate_candidates(c, t); assert 4<=len(cs)<=8, len(cs); assert all(x['archetype']==t['archetype'] for x in cs); assert all(isinstance(x['expression'],str) and x['expression'].strip() for x in cs); print('OK', t['archetype'], len(cs))"</automated>
  </verify>
  <acceptance_criteria>
    - `ideator.py contains def generate_candidates(`.
    - `python -c "import ideator"` exits 0.
    - generate_candidates returns 4-8 candidate records (D-01) for a thesis from researcher.build_thesis.
    - Every candidate record's archetype equals the thesis archetype (D-04 inheritance).
    - Every candidate expression is a non-empty string.
    - ideator.py makes ZERO grade/simulate/login calls (verify: `grep -v '^#' ideator.py | grep -c -E 'grade\\.|simulate\\(|login\\('` returns 0).
  </acceptance_criteria>
  <done>generate_candidates yields 4-8 archetype-inherited candidates composed from grounded skeletons; import succeeds; assert prints OK; no grading/sim code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validate.validate gate + db.expr_exists dedup + seeds.txt emit</name>
  <files>ideator.py</files>
  <read_first>
    - ideator.py (now has generate_candidates from Task 1)
    - validate.py (validate(conn, expr) -> (bool, reason) fail-fast order)
    - db.py (expr_exists(conn, expr) -> Optional[str] exact-string match)
    - cli.py lines 54-67 (seeds.txt parse contract: one expr/line, blank + '#' ignored)
    - .planning/phases/02-grounded-generation/02-CONTEXT.md (criterion 2 validator rejects ZERO outputs; criterion 3 dedup via expr_exists)
  </read_first>
  <behavior>
    - generate_candidates (extended) populates each candidate's `valid`/`validation_reason` by calling validate.validate(conn, expr), and `dedup_alpha_id` by calling db.expr_exists(conn, expr).
    - CRITERION 2: every emitted candidate that is queued for grading is valid==True — i.e. the composition in Task 1 must only emit tokens present in the catalog so validate rejects ZERO of them. If any candidate fails validate, generate_candidates repairs or drops it so the returned "queueable" set has zero unknown-token rejections.
    - CRITERION 3: candidates where dedup_alpha_id is not None are flagged duplicate (already in alphas.expression) and excluded from the queueable set; novel candidates (dedup_alpha_id is None) are queueable.
    - to_seeds_text(candidates) returns a string in exact seeds.txt format: one FastExpr per line for the queueable (valid + novel) candidates, with an optional leading '#'-comment header line; the body lines are directly parseable by cli.py.
  </behavior>
  <action>
    Extend `generate_candidates` so each candidate record is finalized with `valid, validation_reason = validate.validate(conn, expr)` and `dedup_alpha_id = db.expr_exists(conn, expr)` (use the helper — do NOT hand-roll the SELECT). Ensure the composition logic only emits catalog-present tokens so validate returns (True,"") for every queueable candidate (criterion 2); drop or regenerate any that fail. Add `to_seeds_text(candidates)` that filters to valid + dedup_alpha_id-None candidates and joins their expressions one-per-line (optionally with a leading '#' header), matching cli.py's parse contract. Add `queueable(candidates)` helper returning the valid+novel subset. Do NOT write the file to disk here (02-03 owns emit); just return the string.
  </action>
  <verify>
    <automated>python -c "import ideator, researcher, db, validate; c=db.init_db('alpha_kb.db'); t=researcher.build_thesis(c); cs=ideator.generate_candidates(c, t); q=ideator.queueable(cs); assert len(q)>=1, 'no queueable candidates'; assert all(validate.validate(c, x['expression'])[0] for x in q), [x['expression'] for x in q if not validate.validate(c,x['expression'])[0]]; assert all(x['dedup_alpha_id'] is None for x in q); txt=ideator.to_seeds_text(cs); lines=[l for l in txt.splitlines() if l.strip() and not l.startswith('#')]; assert len(lines)==len(q); print('OK queueable', len(q))"</automated>
  </verify>
  <acceptance_criteria>
    - `ideator.py contains def to_seeds_text(` and `def queueable(`.
    - Every queueable candidate passes validate.validate (criterion 2: validator rejects ZERO queueable outputs for unknown tokens) — the assert command proves it.
    - Every queueable candidate has dedup_alpha_id is None (criterion 3: confirmed absent from alphas.expression via db.expr_exists).
    - Candidate records carry both validation_reason and dedup_alpha_id fields.
    - to_seeds_text output body (non-#, non-blank lines) is one FastExpr per queueable candidate and parseable by cli.py (count matches queueable count).
    - ideator.py calls `db.expr_exists` and `validate.validate` (verify: both substrings present, grep returns >=1 each).
  </acceptance_criteria>
  <done>Every queueable candidate is validate-clean and expr_exists-novel; to_seeds_text emits cli.py-parseable seeds; assert prints OK; criteria 2 and 3 demonstrably satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| thesis dict -> ideator.py | tokens from researcher; assumed catalog-subset but re-gated by validate |
| ideator.py -> seeds.txt string | candidate expressions handed to human, then to cli.py/grade.py (authoritative re-gate) |
| catalog (operators/datafields) -> composed FastExpr | only catalog-present tokens may appear; enforced by validate.validate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | composed FastExpr with hallucinated token | mitigate | mandatory validate.validate gate; queueable set asserts zero validation failures (criterion 2) |
| T-02-05 | Repudiation/Dup | duplicate expression re-queued | mitigate | db.expr_exists dedup; queueable excludes non-None dedup_alpha_id (criterion 3) |
| T-02-06 | Denial of Service | accidental sim/auth from ideator | mitigate | ideator issues ZERO BRAIN calls (grep gate); grading is human-run (D-02) |
| T-02-07 | Tampering | malformed seeds.txt breaks cli.py parse | mitigate | to_seeds_text matches cli.py:62-64 contract (one expr/line, '#'/blank ignored); test asserts line count |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | no package installs — ideator imports stdlib + db.py/validate.py/researcher.py only |
</threat_model>

<verification>
- `python -c "import ideator"` exits 0.
- generate_candidates returns 4-8 archetype-inherited candidates (D-01, D-04).
- Every queueable candidate passes validate.validate (criterion 2 — zero unknown-token rejections).
- Every queueable candidate is novel per db.expr_exists (criterion 3).
- to_seeds_text emits cli.py-parseable seeds format (D-02 Path A handoff).
- No grade/simulate/login in ideator.py (D-02 — command stops before grading).
</verification>

<success_criteria>
ideator.py delivers GEN-02: 4-8 grounded FastExpr candidates per thesis, each gated by validate.validate (criterion 2: zero unknown-token rejections), deduped by db.expr_exists (criterion 3), tagged with the inherited thesis archetype (D-04), and serializable to exact seeds.txt format for the human grading handoff (D-02). No grading/simulation occurs in this module.
</success_criteria>

<output>
Create `.planning/phases/02-grounded-generation/02-02-SUMMARY.md` when done.
</output>
