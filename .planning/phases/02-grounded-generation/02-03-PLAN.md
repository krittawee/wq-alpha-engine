---
phase: 02-grounded-generation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - find_alphas.py
  - .claude/commands/find-alphas.md
  - alpha-kb/Theses/.gitkeep
  - alpha-kb/Archetypes/.gitkeep
  - alpha-kb/Failures/.gitkeep
  - alpha-kb/README.md
autonomous: true
requirements:
  - GEN-01
  - GEN-02
user_setup: []

must_haves:
  truths:
    - "alpha-kb/ vault exists with Theses/, Archetypes/, Failures/ subdirs (D-05)"
    - "find_alphas.py orchestrates researcher.build_thesis -> ideator.generate_candidates and emits a thesis note to alpha-kb/Theses/"
    - "The emitted thesis note cites real catalog operators/fields + >=1 SQLite insight and embeds the candidates as a seeds.txt-format fenced block + dedup table (criterion 1 + 3)"
    - "find_alphas.py writes one runs row (run_id, thesis path, started_at, notes) per invocation (D-06)"
    - "/find-alphas STOPS at candidates — it never calls grade.* / simulate (D-02)"
  artifacts:
    - path: "find_alphas.py"
      provides: "Orchestrator: thesis + candidates -> Obsidian note + runs row; no grading"
      contains: "def find_alphas"
      min_lines: 90
    - path: ".claude/commands/find-alphas.md"
      provides: "Claude Code /find-alphas command (hybrid: invokes find_alphas.py + writes LLM thesis prose)"
      contains: "find_alphas"
    - path: "alpha-kb/README.md"
      provides: "Vault scaffold marker + layout doc"
  key_links:
    - from: "find_alphas.py"
      to: "alpha-kb/Theses/YYYY-MM-DD-<archetype>-<slug>.md"
      via: "thesis note write"
      pattern: "alpha-kb/Theses"
    - from: "find_alphas.py"
      to: "runs table"
      via: "INSERT one row per invocation (run_id, thesis path, notes)"
      pattern: "INSERT.*runs"
---

<objective>
Create the `/find-alphas` Claude Code command and its `find_alphas.py` orchestrator: scaffold the git-tracked Obsidian vault at `/Users/winter.__.kor/quant/alpha-kb/` (Theses/, Archetypes/, Failures/ — D-05), orchestrate `researcher.build_thesis` -> `ideator.generate_candidates`, emit a grounded thesis note to `alpha-kb/Theses/YYYY-MM-DD-<archetype>-<slug>.md` (frontmatter + body + seeds.txt-format candidate block + dedup table), and write one `runs` row per invocation (D-06). The command STOPS at candidates for human review — it MUST NOT call grade.* / simulate (LOCKED D-02). The hybrid LLM prose (D-03: Thesis + Economic rationale paragraphs) is authored by the command's Claude agent step and injected into the note template.

Purpose: GEN-01 + GEN-02 end-to-end orchestration — `/find-alphas` produces the Obsidian thesis note (criterion 1) carrying validated, deduped, archetype-tagged candidates (criteria 2 + 3) and stops for the human grading handoff.

Output: `find_alphas.py`, `.claude/commands/find-alphas.md`, scaffolded `alpha-kb/` vault, and a `runs` row writer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-grounded-generation/02-CONTEXT.md
@.planning/phases/02-grounded-generation/02-GROUNDING.md
@CLAUDE.md
@db.py
@.planning/phases/02-grounded-generation/02-01-SUMMARY.md
@.planning/phases/02-grounded-generation/02-02-SUMMARY.md

<interfaces>
<!-- 02-01, 02-02, Phase 1 contracts. Use directly. -->

From researcher.py (02-01):
- build_thesis(conn, archetype=None) -> dict   # archetype, source_operators, source_datafields,
                                               # cited_alpha_ids, cited_insights, region, universe, delay
- read_catalog(conn) -> (operators, datafields)  # for the Grounding tables in the note body

From ideator.py (02-02):
- generate_candidates(conn, thesis, n=None) -> list[dict]   # {expression, archetype, valid, validation_reason, dedup_alpha_id}
- queueable(candidates) -> list[dict]                       # valid + novel subset
- to_seeds_text(candidates) -> str                          # exact seeds.txt format

From db.py:
- init_db(path="alpha_kb.db") -> sqlite3.Connection   # db.py:55

runs table schema (db.py:37-40 — CONFIRMED, write to these columns):
  runs(run_id TEXT PK, thesis TEXT, started_at TEXT, iterations INTEGER, num_pass INTEGER, notes TEXT)
  Phase 2 writes: run_id (uuid4()[:8]), thesis (one-line claim or archetype label),
                  started_at (iso utc), iterations (candidate count), num_pass (NULL — not graded yet),
                  notes (relative path to the emitted thesis note). D-06.
</interfaces>

Vault layout (D-05, 02-GROUNDING.md §Obsidian thesis-note template):
  alpha-kb/Theses/      <- Phase 2 emits here
  alpha-kb/Archetypes/  <- Phase 4 (scaffold only now)
  alpha-kb/Failures/    <- Phase 3+ (scaffold only now)
  Note path: alpha-kb/Theses/YYYY-MM-DD-<archetype>-<slug>.md

Thesis-note structure (full template in 02-GROUNDING.md §Obsidian thesis-note template):
  Frontmatter: title, date, status: proposed, archetype, run_id, region/universe/delay,
    source_operators (subset operators.name), source_datafields (subset datafields.id),
    cited_alpha_ids, cited_insights (list), candidate_count, tags.
  Body order: # title -> ## Thesis (LLM prose) -> ## Economic rationale (LLM prose) ->
    ## Grounding: operators & fields cited (markdown tables) ->
    ## Past-result insight cited ([[alpha_id]] wikilinks + prose tying >=1 SQLite fact) ->
    ## Candidate expressions (seeds.txt-format fenced block + table: # | expression | archetype | dedupe) ->
    ## Next steps (checklist: queue to grade.py, update status).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold alpha-kb/ vault + thesis-note writer + runs row</name>
  <files>alpha-kb/Theses/.gitkeep, alpha-kb/Archetypes/.gitkeep, alpha-kb/Failures/.gitkeep, alpha-kb/README.md, find_alphas.py</files>
  <read_first>
    - find_alphas.py (the file being created — start empty)
    - researcher.py + ideator.py (build_thesis / generate_candidates / queueable / to_seeds_text outputs)
    - db.py lines 37-40 (runs table schema) and db.py:55 (init_db)
    - .planning/phases/02-grounded-generation/02-GROUNDING.md §Obsidian thesis-note template (frontmatter + body)
    - .planning/phases/02-grounded-generation/02-CONTEXT.md (D-02 STOP before grading; D-05 vault; D-06 runs row)
  </read_first>
  <action>
    Create the vault scaffold by adding `.gitkeep` files at `alpha-kb/Theses/.gitkeep`, `alpha-kb/Archetypes/.gitkeep`, `alpha-kb/Failures/.gitkeep` and an `alpha-kb/README.md` documenting the layout (D-05, git-tracked).
    Create `find_alphas.py` with `import db, researcher, ideator, sqlite3, uuid` (+ datetime, pathlib). Add `render_note(thesis, candidates, run_id, prose=None)` that builds the full Markdown note: frontmatter (title, date, status: proposed, archetype, run_id, region/universe/delay, source_operators, source_datafields, cited_alpha_ids, cited_insights, candidate_count, tags) + body sections in template order. The Thesis + Economic-rationale paragraphs come from the optional `prose` arg (supplied by the command's LLM step, D-03); if prose is None, insert a clearly-marked placeholder. The Candidate-expressions section embeds `ideator.to_seeds_text(candidates)` inside a fenced ```text block plus a dedup table with columns `# | expression | archetype | dedupe(expr_exists)`. Add `slugify(text)` for the filename. Add `write_runs_row(conn, run_id, thesis, note_path, candidate_count)` that INSERTs one row into `runs` using the confirmed schema (run_id, thesis, started_at=iso utc, iterations=candidate_count, num_pass=NULL, notes=relative note path) — D-06. Do NOT import or call grade/simulate/login anywhere (D-02).
  </action>
  <verify>
    <automated>python -c "import find_alphas, researcher, ideator, db; import os; assert os.path.isdir('alpha-kb/Theses') and os.path.isdir('alpha-kb/Archetypes') and os.path.isdir('alpha-kb/Failures'); c=db.init_db('alpha_kb.db'); t=researcher.build_thesis(c); cs=ideator.generate_candidates(c,t); note=find_alphas.render_note(t, cs, 'testrun01'); assert '## Thesis' in note and '## Candidate expressions' in note and 'source_operators' in note and 'archetype:' in note; print('OK note len', len(note))"</automated>
  </verify>
  <acceptance_criteria>
    - `alpha-kb/Theses/`, `alpha-kb/Archetypes/`, `alpha-kb/Failures/` exist (D-05); `alpha-kb/README.md` documents the layout.
    - `find_alphas.py contains def render_note(` and `def write_runs_row(`.
    - render_note output contains frontmatter keys `archetype:`, `source_operators`, `source_datafields`, `run_id`, `candidate_count`, and body headers `## Thesis`, `## Economic rationale`, `## Grounding`, `## Past-result insight cited`, `## Candidate expressions`, `## Next steps`.
    - The Candidate-expressions section contains a fenced block with the seeds.txt-format expressions and a dedupe table.
    - find_alphas.py makes ZERO grade/simulate/login calls (verify: `grep -v '^#' find_alphas.py | grep -c -E 'grade\\.|simulate\\(|login\\('` returns 0) — D-02.
  </acceptance_criteria>
  <done>Vault scaffolded; render_note produces a template-complete thesis note; write_runs_row writes the confirmed runs schema; no grading code present.</done>
</task>

<task type="auto">
  <name>Task 2: find_alphas() orchestrator + /find-alphas command file</name>
  <files>find_alphas.py, .claude/commands/find-alphas.md</files>
  <read_first>
    - find_alphas.py (now has render_note / write_runs_row / slugify from Task 1)
    - researcher.py + ideator.py (full pipeline)
    - .claude/commands/ (does not exist yet — this task creates the first command file)
    - .planning/phases/02-grounded-generation/02-GROUNDING.md §Proposed plan breakdown item 3 + §Lessons (research->ideate loop ergonomics)
    - .planning/phases/02-grounded-generation/02-CONTEXT.md (D-02 STOP for human review; D-03 hybrid LLM prose)
  </read_first>
  <action>
    Add `find_alphas(db_path="alpha_kb.db", archetype=None, prose=None)` to `find_alphas.py` that: opens conn via db.init_db; calls researcher.build_thesis(conn, archetype); calls ideator.generate_candidates(conn, thesis); generates run_id = uuid4()[:8]; computes note path `alpha-kb/Theses/{YYYY-MM-DD}-{archetype}-{slug}.md`; renders the note via render_note (passing prose through); writes the note file; calls write_runs_row; closes conn; returns a dict {run_id, note_path, archetype, candidate_count, queueable_count}. It MUST NOT call grade/simulate (D-02) — it stops and prints the human handoff instruction (run `python cli.py <seeds-file>` separately, Path A). Add an `if __name__ == "__main__"` argparse entrypoint (optional --archetype, --db).
    Create `.claude/commands/find-alphas.md` defining the `/find-alphas` slash command (hybrid, D-03): (1) instruct the agent to run researcher.build_thesis to get the deterministic archetype + grounded facts, (2) have the AGENT write the Thesis + Economic-rationale prose grounded in the cited operators/fields/insights, (3) invoke `find_alphas.find_alphas(..., prose=<agent prose>)` to assemble + write the note + runs row, (4) STOP and present the candidate set + note path to the human for review — explicitly state grading is run separately via `python cli.py <seeds-file>` and the command MUST NOT grade/simulate (D-02). Reference researcher.py/ideator.py/find_alphas.py by name.
  </action>
  <verify>
    <automated>python -c "import find_alphas, os, glob; r=find_alphas.find_alphas(); assert os.path.isfile(r['note_path']), r['note_path']; assert r['note_path'].startswith('alpha-kb/Theses/') and r['note_path'].endswith('.md'); import sqlite3,db; c=db.init_db('alpha_kb.db'); n=c.execute('SELECT count(*) FROM runs WHERE run_id=?',(r['run_id'],)).fetchone()[0]; assert n==1, n; body=open(r['note_path']).read(); assert r['archetype'] in body; print('OK', r['run_id'], r['note_path'])" && grep -q "find_alphas" .claude/commands/find-alphas.md && grep -qiE "grade|cli.py" .claude/commands/find-alphas.md && echo CMD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `find_alphas.py contains def find_alphas(`.
    - Calling find_alphas() writes a real note to `alpha-kb/Theses/YYYY-MM-DD-<archetype>-<slug>.md` and returns {run_id, note_path, archetype, candidate_count, queueable_count}.
    - Exactly one `runs` row is written per invocation with the returned run_id (D-06).
    - `.claude/commands/find-alphas.md` exists, references find_alphas, and explicitly states grading is run separately (mentions cli.py / grade) and that the command stops at candidates (D-02).
    - find_alphas.py still makes ZERO grade/simulate/login calls (grep gate returns 0) — D-02.
    - The written note embeds the seeds.txt-format candidate block and contains the chosen archetype string.
  </acceptance_criteria>
  <done>find_alphas() runs the full Researcher->Ideator->note->runs pipeline, stops before grading, and the /find-alphas command file wires the hybrid LLM-prose step; both verify asserts pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM prose -> note file | agent-authored Thesis/rationale text written verbatim into Markdown; no code execution |
| find_alphas.py -> filesystem (alpha-kb/) | note + scaffold writes inside repo vault; paths derived from slugified archetype/date |
| find_alphas.py -> runs table | one INSERT per invocation; parameterized |
| command boundary -> grading | HARD STOP: no grade/simulate crosses this boundary (D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Elevation of Privilege | /find-alphas auto-grading -> sim/auth lockout | mitigate | command + find_alphas.py make ZERO grade/simulate/login calls; grep gate in acceptance criteria; D-02 enforced |
| T-02-09 | Tampering | slugified archetype/date -> path traversal in note filename | mitigate | slugify strips non-alnum/dash; archetype constrained to the 8-label set; date is iso |
| T-02-10 | Information Disclosure | note frontmatter cites NULL DB columns | mitigate | cited_insights/cited_alpha_ids sourced from 02-01 gather_insights (populated columns only) |
| T-02-11 | Tampering | runs row written to wrong/extra columns | mitigate | write only the confirmed db.py:37-40 schema columns; test asserts exactly one row |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | no package installs — find_alphas.py imports stdlib + db/researcher/ideator only |
</threat_model>

<verification>
- alpha-kb/ vault scaffolded with Theses/Archetypes/Failures (D-05).
- find_alphas() emits a complete thesis note citing real catalog tokens + >=1 SQLite insight (criterion 1) with validated, deduped, archetype-tagged candidates embedded as seeds.txt block + dedup table (criteria 2 + 3).
- Exactly one runs row per invocation (D-06).
- /find-alphas command file wires the hybrid LLM-prose step and STOPS before grading (D-02).
- No grade/simulate/login anywhere in find_alphas.py (D-02 grep gate).
</verification>

<success_criteria>
`/find-alphas` orchestrates Researcher -> Ideator end-to-end, scaffolds the git-tracked alpha-kb/ vault, emits a grounded Obsidian thesis note (criterion 1) with validated+deduped+archetype-tagged candidates (criteria 2+3) in seeds.txt format, writes one runs row per invocation (D-06), and stops for human grading review without ever calling grade/simulate (D-02). The hybrid LLM prose step (D-03) is authored in the command file and injected into the note.
</success_criteria>

<output>
Create `.planning/phases/02-grounded-generation/02-03-SUMMARY.md` when done.
</output>
