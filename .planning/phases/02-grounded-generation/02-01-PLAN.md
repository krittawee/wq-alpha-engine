---
phase: 02-grounded-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - researcher.py
autonomous: true
requirements:
  - GEN-01
user_setup: []

must_haves:
  truths:
    - "researcher.py reads operators and datafields from alpha_kb.db (no hardcoded catalog)"
    - "researcher.py queries past alpha results (sharpe/fitness/status/checks) and surfaces >=1 citable insight"
    - "Deterministic archetype selection picks one of the 8 taxonomy labels by rotating under-explored / targeting the 59-clean pool"
    - "researcher.py emits a structured thesis object carrying archetype, cited tokens (subset of catalog), cited insight, and cited alpha_ids"
  artifacts:
    - path: "researcher.py"
      provides: "Researcher: catalog reads + insight queries + deterministic archetype selection + thesis assembly"
      contains: "def select_archetype"
      min_lines: 80
  key_links:
    - from: "researcher.py"
      to: "db.init_db / alpha_kb.db operators+datafields tables"
      via: "import db + raw SELECT against operators/datafields"
      pattern: "import db"
    - from: "researcher.py"
      to: "alphas + checks tables"
      via: "SELECT sharpe/fitness/status + check pass/fail for insight"
      pattern: "FROM alphas"
---

<objective>
Build `researcher.py` — the deterministic-steering + grounded-fact half of the Researcher. It reads the live catalog (operators/datafields) and past-alpha memory (alphas/checks) from `alpha_kb.db`, deterministically selects ONE archetype (rotate under-explored archetypes / target the 59-alpha clean pool per LOCKED decision D-03), pulls grounded operator/field tokens and >=1 citable past-result insight, and assembles a structured thesis object that downstream plans (02-02 ideator, 02-03 command) consume.

Purpose: GEN-01 — a Researcher agent produces a grounded thesis from the catalog + past results in memory. This plan delivers the deterministic + data-grounding layer; the LLM prose layer is supplied by the `/find-alphas` command in 02-03 (hybrid researcher, D-03).

Output: `researcher.py` exporting a thesis-assembly entrypoint plus `select_archetype`, catalog-read, and insight-query helpers, returning a structured thesis dict ready for 02-02/02-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-grounded-generation/02-CONTEXT.md
@.planning/phases/02-grounded-generation/02-GROUNDING.md
@CLAUDE.md
@db.py

<interfaces>
<!-- Phase 1 contracts. Executor uses these directly — no codebase exploration needed. -->

From db.py:
- init_db(path: str = "alpha_kb.db") -> sqlite3.Connection   # db.py:55 — caller owns/closes conn; enables WAL
- expr_exists(conn, expression: str) -> Optional[str]        # db.py:149 — dedup helper (used by 02-02, not here)
- upsert_alpha(conn, alpha_dict: dict) -> None               # db.py:69 — `archetype` is a writable column

alpha_kb.db tables (no catalog-read helper exists — run raw SELECTs):
- operators(name PK, category, definition, signature)  # 67 rows; `signature` is EMPTY — read `definition` for arity
- datafields(id, description, dataset, region, universe, delay, type)  # 8155 rows; ONLY USA/TOP3000/delay=1 synced
- alphas(alpha_id PK, expression, archetype, sharpe, fitness, turnover, status, ...)  # 384 rows; archetype/self_corr/prod_corr are NULL across ALL rows
- checks(alpha_id, name, result, value, limit_val, checked_at)  # per-check pass/fail evidence

Catalog-read SELECTs (verbatim from grounding brief):
  SELECT name, category, definition FROM operators;
  SELECT id, description, dataset, type FROM datafields
    WHERE region='USA' AND universe='TOP3000' AND delay=1;

Insight queries (criterion 1 — sharpe/fitness/status only; archetype/self_corr are NULL in-DB):
  59-clean pool: SELECT count(*) FROM alphas WHERE status='UNSUBMITTED'
    AND sharpe>=1.25 AND fitness>=1.0 AND turnover<=0.4;
  common FAIL checks: SELECT name, count(*) FROM checks WHERE result LIKE '%FAIL%' GROUP BY name;
</interfaces>

The 8 archetype labels (LOCKED taxonomy, 02-GROUNDING.md §Archetype taxonomy):
reversal, momentum, value_garp, quality, growth, low_volatility, liquidity_volume, sentiment_event.
Each maps to grounded operator families + datafield categories — read the full skeleton table in 02-GROUNDING.md.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Catalog reads + past-alpha insight queries</name>
  <files>researcher.py</files>
  <read_first>
    - researcher.py (the file being created — start empty)
    - db.py (init_db / expr_exists / upsert_alpha signatures + table DDL)
    - .planning/phases/02-grounded-generation/02-GROUNDING.md §Catalog inventory + §Past-alpha insights (citable hooks, 59-pool, FAIL anti-patterns)
    - CLAUDE.md (never hardcode catalog; read from BRAIN-synced tables)
  </read_first>
  <behavior>
    - read_catalog(conn) returns operators (name/category/definition) and the USA/TOP3000/delay=1 datafields (id/description/dataset/type) read from the live tables — count matches the synced slice, not a hardcoded list.
    - gather_insights(conn) returns >=1 citable fact derived ONLY from columns that are populated in-DB: sharpe/fitness/turnover/status from `alphas`, pass/fail from `checks`. It MUST NOT cite `archetype`, `self_corr`, or `prod_corr` (NULL across all 384 rows).
    - gather_insights surfaces the 59-alpha clean pool count (UNSUBMITTED, sharpe>=1.25, fitness>=1.0, turnover<=0.4) and the most common FAIL check name from `checks`.
    - Each insight is a human-readable string with the supporting alpha_id(s) attached (for `cited_alpha_ids`).
  </behavior>
  <action>
    Create `researcher.py` with `import db, sqlite3`. Add `read_catalog(conn)` running the two grounded SELECTs from the interfaces block (operators full; datafields filtered `WHERE region='USA' AND universe='TOP3000' AND delay=1`). Add `gather_insights(conn)` running the 59-clean-pool count query and the common-FAIL-check GROUP BY query against `alphas`/`checks`, returning a list of `{text, cited_alpha_ids}` dicts. Restrict all `alphas` SELECTs to populated columns (sharpe, fitness, turnover, status); do NOT SELECT archetype/self_corr/prod_corr for citation. Open the conn via `db.init_db(path)` so the caller owns it. No BRAIN API calls — pure SQLite reads.
  </action>
  <verify>
    <automated>python -c "import researcher, db; c=db.init_db('alpha_kb.db'); ops,fields=researcher.read_catalog(c); assert len(ops)>=60 and len(fields)>=5000, (len(ops),len(fields)); ins=researcher.gather_insights(c); assert len(ins)>=1 and all('archetype' not in i.get('text','').lower() or 'self_corr' not in i.get('text','') for i in ins); print('OK', len(ops), len(fields), len(ins))"</automated>
  </verify>
  <acceptance_criteria>
    - `researcher.py contains def read_catalog(` and `def gather_insights(`.
    - `python -c "import researcher"` exits 0.
    - read_catalog returns >=60 operators and >=5000 datafields read from alpha_kb.db (proves no hardcoded catalog).
    - gather_insights returns >=1 insight string; no insight cites archetype/self_corr/prod_corr (those are NULL in-DB).
    - The datafields SELECT filters `region='USA' AND universe='TOP3000' AND delay=1`.
  </acceptance_criteria>
  <done>read_catalog + gather_insights read live tables, return grounded tokens and >=1 citable insight; the import succeeds and the assert command prints OK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic archetype selection + thesis assembly</name>
  <files>researcher.py</files>
  <read_first>
    - researcher.py (now has read_catalog + gather_insights from Task 1)
    - .planning/phases/02-grounded-generation/02-GROUNDING.md §Archetype taxonomy (the 8 labels + grounded operator/field families per archetype) and §Past-alpha insights (59-pool steering)
    - .planning/phases/02-grounded-generation/02-CONTEXT.md (D-03 hybrid: deterministic archetype selection; D-04 one thesis = one archetype)
    - db.py (alphas.archetype is NULL across all rows — see note below)
  </read_first>
  <behavior>
    - select_archetype(conn) returns exactly ONE of the 8 taxonomy labels (reversal, momentum, value_garp, quality, growth, low_volatility, liquidity_volume, sentiment_event), chosen deterministically.
    - Because `alphas.archetype` is NULL for all 384 rows, "under-explored" rotation is driven by a reproducible rotation rule (e.g. rotate by run count in `runs`, or fixed cycle order) plus the 59-clean-pool / FAIL-anti-pattern steering from gather_insights — not by reading a populated archetype column. The chosen rule must be documented in a docstring and be deterministic (same DB state -> same archetype).
    - build_thesis(conn, archetype=None) assembles a structured thesis dict with keys: archetype, source_operators (subset of operators.name relevant to that archetype), source_datafields (subset of datafields.id relevant to that archetype), cited_alpha_ids, cited_insights, region='USA', universe='TOP3000', delay=1. If archetype is None it calls select_archetype.
    - Every token in source_operators is present in operators.name; every token in source_datafields is present in the synced datafields.id slice (machine-checkable for criterion 1).
  </behavior>
  <action>
    Add `select_archetype(conn)` to `researcher.py` returning one of the 8 labels via a deterministic rotation documented in its docstring (since alphas.archetype is NULL, base rotation on `runs` row count or a fixed cycle; bias toward archetypes that diversify away from the FAIL anti-pattern of naive price/volume signals per the grounding brief). Add a module-level mapping from each archetype to its grounded operator-family and datafield seed-token lists (copied from 02-GROUNDING.md §Archetype taxonomy — these are confirmed-present tokens). Add `build_thesis(conn, archetype=None)` that calls read_catalog + gather_insights + select_archetype, intersects the archetype's seed tokens against the live catalog to guarantee subset membership, and returns the structured thesis dict described in behavior. Do NOT write any thesis prose here (the LLM step lives in the 02-03 command). Do NOT call grade/simulate.
  </action>
  <verify>
    <automated>python -c "import researcher, db; c=db.init_db('alpha_kb.db'); LABELS={'reversal','momentum','value_garp','quality','growth','low_volatility','liquidity_volume','sentiment_event'}; a=researcher.select_archetype(c); assert a in LABELS, a; t=researcher.build_thesis(c); ops={r[0] for r in c.execute('SELECT name FROM operators')}; ff={r[0] for r in c.execute(\"SELECT id FROM datafields WHERE region='USA' AND universe='TOP3000' AND delay=1\")}; assert set(t['source_operators'])<=ops, t['source_operators']; assert set(t['source_datafields'])<=ff, t['source_datafields']; assert t['archetype'] in LABELS and len(t['cited_insights'])>=1; print('OK', t['archetype'], len(t['source_operators']), len(t['source_datafields']))"</automated>
  </verify>
  <acceptance_criteria>
    - `researcher.py contains def select_archetype(` and `def build_thesis(`.
    - select_archetype returns a label in the 8-member taxonomy set; same DB state yields the same archetype on repeat calls (deterministic).
    - build_thesis returns a dict whose source_operators is a subset of operators.name and source_datafields is a subset of the synced datafields.id slice.
    - build_thesis output includes archetype, cited_alpha_ids, cited_insights (>=1), and region/universe/delay = USA/TOP3000/1.
    - researcher.py makes ZERO calls to grade.* / simulate / any BRAIN client (verify: `grep -v '^#' researcher.py | grep -c -E 'grade\\.|simulate\\(|login\\(' ` returns 0).
  </acceptance_criteria>
  <done>select_archetype deterministically returns one taxonomy label; build_thesis assembles a catalog-subset-verified, insight-bearing thesis dict; the assert command prints OK; no grading/simulation code present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alpha_kb.db -> researcher.py | DB content (operator/field tokens, past expressions) read into Python; treated as data, never executed |
| researcher.py -> downstream thesis dict | tokens emitted for later FastExpr assembly; must stay subset of catalog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | raw SELECTs in researcher.py | mitigate | parameterized queries only; no string-interpolated SQL with external input (all queries are static) |
| T-02-02 | Information Disclosure | citing NULL columns (archetype/self_corr) | mitigate | insight queries restricted to populated columns (sharpe/fitness/turnover/status); behavior test asserts no NULL-column citation |
| T-02-03 | Denial of Service | in-loop re-auth / sim | accept | researcher.py issues ZERO BRAIN calls (grep gate in acceptance criteria); lockout risk not reachable here |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan — researcher.py imports stdlib + existing db.py only |
</threat_model>

<verification>
- `python -c "import researcher"` exits 0.
- read_catalog returns the live synced catalog counts (>=60 ops, >=5000 fields), proving no hardcoded catalog (GEN-01 grounding).
- gather_insights returns >=1 citable insight from populated alphas/checks columns.
- select_archetype is deterministic and returns one of the 8 taxonomy labels.
- build_thesis source tokens are a verified subset of the live catalog.
- No grade/simulate/login references in researcher.py.
</verification>

<success_criteria>
researcher.py delivers the deterministic + data-grounding half of GEN-01: live catalog reads, >=1 citable past-result insight (from populated columns only), deterministic archetype selection from the 8-label taxonomy, and a structured thesis dict whose cited tokens are guaranteed present in the synced catalog. No prose, no grading, no BRAIN calls.
</success_criteria>

<output>
Create `.planning/phases/02-grounded-generation/02-01-SUMMARY.md` when done.
</output>
