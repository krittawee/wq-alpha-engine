---
phase: 07-brute-force-tool-tool-b
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - .claude/commands/bruteforce.md
  - test_phase7.py
autonomous: false
requirements:
  - BF-01
  - BF-02
  - BF-03
  - BF-04
  - BF-05
  - BF-06
must_haves:
  truths:
    - "Running /bruteforce with a real session produces observable log output: validate-dropped count, probe result per template, and stop reason"
    - "The command file instructs login() ONCE before bruteforce(); never re-auth inside the run"
    - "User can run /bruteforce --delay 0 --quota 5 and it enumerates templates, validates, probes, and stops cleanly at quota or dry"
    - "The command file documents all CLI flags: --db, --delay, --quota, --probe-size, --templates"
    - "All 5 ROADMAP Phase 7 success criteria are verifiable end-to-end after this plan"
    - "Final pytest test_phase7.py run shows all 11 tests green before checkpoint"
  artifacts:
    - path: ".claude/commands/bruteforce.md"
      provides: "/bruteforce command file (mirrors hunt.md pattern)"
      contains: "wq_login.login()"
    - path: "test_phase7.py"
      provides: "BF-05 static check test for AI-free constraint"
      contains: "test_no_llm_imports"
  key_links:
    - from: ".claude/commands/bruteforce.md login call"
      to: "bruteforce.bruteforce(client=client, ...)"
      via: "single-shot auth before engine"
      pattern: "login\\(\\).*bruteforce\\("
    - from: "/bruteforce --delay 0"
      to: "bruteforce.bruteforce(delay=0)"
      via: "argparse in bruteforce.py __main__"
      pattern: "delay.*0"
---

<objective>
Create the /bruteforce command file (.claude/commands/bruteforce.md) that wires the
engine to the Claude Code command interface. Add 1 static-check test for the AI-free
constraint. Then run a human-verify checkpoint confirming the end-to-end pipeline against
a real BRAIN session — covering all 5 Phase 7 ROADMAP success criteria.

Purpose: Plan 07-03 built the engine; this plan surfaces it as a usable command and
provides the end-to-end verification that the full pipeline (enumerate → validate → probe
→ bulk-sim → gate → record) works against BRAIN.

Output: .claude/commands/bruteforce.md (new); 1 new test; human verification of all
5 success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-brute-force-tool-tool-b/07-CONTEXT.md
@.planning/phases/07-brute-force-tool-tool-b/07-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from live code. -->

From .claude/commands/hunt.md (structural model for bruteforce.md):
The command file structure:
  - Title + one-line description
  - Auth constraint callout (ONCE, never re-auth)
  - "What this command does" section:
    1. [AGENT: Single-shot auth] — call wq_login.login() exactly once
    2. [AGENT: Run bruteforce.bruteforce()] — invoke engine with all flags
    3. [AGENT: Handle 401] — try/except HTTPError(401) → print + sys.exit(1)
    4. [AGENT: Display results] — present result dict to user
  - Stop conditions section
  - Flags table (--db, --delay, --quota, --probe-size, --templates)
  - Auth constraint section (copy + adapt from hunt.md)
  - Concurrency constraint section
  - Module references section
  - Output section

From bruteforce.py (just created in Plan 07-03):
```python
def bruteforce(
    client,
    db_path: str = "alpha_kb.db",
    delay: int = 0,
    quota: int = 5,
    probe_size: int = 5,
    template_names: list[str] | None = None,
) -> dict:
    # Returns: {"quota_count", "n_templates_done", "stop_reason", "additive_ids"}
```

From ROADMAP.md Phase 7 success criteria (the 5 criteria to verify):
  SC-1: Template enumeration uses only verified catalog entries (zero unknown-token combos reach sim)
  SC-2: Pre-filter run observable in log (dropped count + dropped reason per template)
  SC-3: Probe sim; "template abandoned after probe" logged if probe fails; remaining combos skipped
  SC-4: Bulk sim at ≤3 concurrent on cached session; stops on quota/401/dry; additivity gate required
  SC-5: Tool runs end-to-end with AI completely absent (no LLM call during the run)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .claude/commands/bruteforce.md command file + BF-05 static test</name>
  <files>.claude/commands/bruteforce.md, test_phase7.py</files>

  <read_first>
    - .claude/commands/hunt.md lines 1-132 in full: exact structure and prose style to mirror
      for bruteforce.md. Section headers, auth callout box, flag table format, module references
      section. bruteforce.md must be readable as a standalone command prompt with no prior context.
    - bruteforce.py __main__ block (created in Plan 07-03): flags and defaults to document.
    - CLAUDE.md §Constraints: copy the exact single-shot auth constraint wording and
      concurrency constraint wording — these are non-negotiable from CLAUDE.md and MUST appear
      verbatim in the command file.
    - 07-CONTEXT.md §D-08 (delay-0 default), §D-07 (quota default=5), §D-09 (stop conditions),
      §D-05 (probe abandon criterion) — these become the "Stop conditions" section.
    - 07-RESEARCH.md §Code Examples (401 catch pattern) — the exact try/except block for
      [AGENT: Handle 401] section.
  </read_first>

  <action>
    Create .claude/commands/bruteforce.md following the exact structural pattern of hunt.md.
    Key sections and content:

    TITLE: "# /bruteforce — AI-Free Brute-Force Alpha Discovery (Tool B)"
    Subtitle: "Enumerate parameterized templates, validate locally, probe-sim a sample,
    bulk-sim survivors at ≤3 concurrent on the cached BRAIN session, gate through additivity.
    Fully standalone — no LLM dependency. Runs when Claude Code AI quota is exhausted."

    AUTH CALLOUT (bold box at top, matches hunt.md):
    "Auth: wq_login is called ONCE before the loop. A 401 mid-loop stops the run cleanly —
    never re-auth in-loop (CLAUDE.md lockout constraint)."

    WHAT THIS COMMAND DOES section with 4 numbered steps:
    Step 1 [AGENT: Single-shot auth] — Call wq_login.login() ONCE. Store returned client.
      Do NOT call login() again anywhere inside the run. Explain why: biometric re-auth
      triggers 429 BIOMETRICS_THROTTLED (15-30 min lockout).
    Step 2 [AGENT: Run bruteforce.bruteforce()] — Import and call bruteforce() with all args.
      Show the exact Python invocation with all parameters and their CLI-flag overrides.
      Enumerate the inner pipeline: backfill_active_pnl → probe_and_gate (if delay != 1) →
      for each template: expand_slots → validate gate → probe sample → bulk-sim (≤3 concurrent)
      → additivity gate → bruteforce_runs persistence.
    Step 3 [AGENT: Handle 401] — Exact try/except HTTPError(401) block (from research §401
      catch pattern). Print partial progress. sys.exit(1). Never re-authenticate.
    Step 4 [AGENT: Display results] — Present result dict fields:
      - quota_count: number of additive survivors found
      - n_templates_done: templates fully processed
      - stop_reason: "quota_met" | "401" | "dry"
      - additive_ids: list of alpha_ids that passed IS checks + additivity gate

    STOP CONDITIONS section (D-09):
    Template loop stops when ANY of:
    1. quota_count >= --quota (additive survivors found; default 5)
    2. 401 session expiry — persists partial progress, reports, exits
    3. dry — all templates processed, quota not met
    Note: IS-pass-but-correlated does NOT count toward quota (D-07).

    FLAGS table (matches bruteforce.py argparse):
    | Flag | Default | Description |
    |------|---------|-------------|
    | --db | alpha_kb.db | SQLite knowledge-base path |
    | --delay | 0 | Simulation delay. 0 = delay-0 (default; decorrelated from delay-1 book). Use --delay 1 to override. |
    | --quota | 5 | Stop after this many additive survivors (IS-pass + additivity-gate pass). |
    | --probe-size | 5 | Probe sample size per template (spread-covers all distinct slot values). |
    | --templates | (all) | Space-separated template names to run. Omit to run all 4 shapes. |

    AUTH CONSTRAINT section (copy from hunt.md + adapt):
    login() called exactly once before bruteforce(). No auth inside loop. 401 = stop + exit.
    User controls re-authentication by re-running /bruteforce.

    CONCURRENCY CONSTRAINT section:
    bruteforce._bulk_sim_quota_aware() uses ThreadPoolExecutor(max_workers=3). This is the
    BRAIN concurrent simulation slot cap. Never raise this value.

    MODULE REFERENCES section:
    List: bruteforce.py (bruteforce, settings_grid_for_archetype), templates.py (TEMPLATES,
    expand_slots, probe_spread_sample), wq_login.py (login), validate.py (validate),
    grade.py (grade_one), editor.py (classify_from_checks), additivity.py (rank_by_proxy,
    confirm_additive), selfcorr.py (backfill_active_pnl), probe_delay.py (probe_and_gate),
    db.py (init_db, insert_bruteforce_run, update_bruteforce_run).

    OUTPUT section:
    After bruteforce.bruteforce() completes:
    1. quota_count — additive survivors found (these are in alphas + checks tables)
    2. additive_ids — alpha_ids ready for manual review + submission decision
    3. DB — survivors in alphas/checks; per-template aggregates in bruteforce_runs; run in runs
    4. Failure summary — validate_dropped, probe_abandoned, IS_fail, gate_fail counts per template

    Separately, append 1 test to test_phase7.py:

    test_no_llm_imports():
      Open bruteforce.py and templates.py as text. For each of 'claude', 'anthropic', 'llm',
      'openai': assert the term does not appear (case-insensitive) in either file's source.
      This is the BF-05 static verification that the tool is AI-free.
  </action>

  <verify>
    <automated>
cd /Users/winter.__.kor/quant && python -m pytest test_phase7.py::test_no_llm_imports -x -q && ls -la .claude/commands/bruteforce.md && grep -c "login()" .claude/commands/bruteforce.md
    </automated>
  </verify>

  <done>
    .claude/commands/bruteforce.md exists; test_no_llm_imports passes; grep "login()" in command
    file returns at least 1; command file has all 4 sections (auth, what-it-does, flags, output).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 07-01 through 07-04 built the complete Phase 7 pipeline:
    - db.py: bruteforce_runs schema + insert_bruteforce_run + update_bruteforce_run
    - templates.py: TEMPLATES (4 shapes) + expand_slots + probe_spread_sample
    - bruteforce.py: full engine (enumerate → validate → probe → bulk-sim → gate → persist)
    - .claude/commands/bruteforce.md: command file
    - test_phase7.py: 11 tests (schema + template + engine + static)

    Before this checkpoint, run the full test suite:
    python -m pytest test_phase7.py -v
  </what-built>

  <how-to-verify>
    Run these checks in order. The BRAIN session requires a real wq_login.login() call
    (biometric Persona auth if session is stale). Do NOT re-auth mid-run.

    **Check 1 — Full unit test suite (no BRAIN session needed):**
    python -m pytest test_phase7.py -v
    Expected: 11 tests, all pass.

    **Check 2 — Dry run (no sims, no BRAIN session needed):**
    python bruteforce.py --quota 1 --probe-size 1 --templates residual_momentum
    (If --max-sims 0 is available: python bruteforce.py --max-sims 0)
    Expected: Enumerate combos, run validate, print dropped count, skip sims,
    report stop_reason = "dry". No auth needed.

    NOTE: Checks 3-5 require a live BRAIN session. If session is stale, run wq_login.login()
    once interactively first. Then:

    **Check 3 — SC-1 + SC-2 (enumerate + validate log observable):**
    Run /bruteforce with template_names=["beta_neutral"] (all-literal slots, smallest set).
    Observe log output: should show n_combos count, n_validated count, validate_dropped count.
    Expected: zero "unknown token" combos reach grade_one (BF-02 confirmed).

    **Check 4 — SC-3 (probe abort):**
    Run /bruteforce with --probe-size 5 on a template known to return far FAIL.
    (Use residual_momentum which uses close/open confirmed delay-0 fields.)
    If probe result is near/pass: template proceeds — this is success (no abort needed).
    If probe result is all fail: observe "template abandoned after probe" log message.
    Either outcome satisfies SC-3 (the mechanism is exercised).

    **Check 5 — SC-4 + SC-5 (bulk-sim at ≤3 concurrent, AI absent):**
    Run /bruteforce --quota 1 with a single template.
    Observe: concurrent sims log shows ≤3 in flight at once.
    Observe: no LLM API call is made during the run.
    If an additive alpha is found: alpha_id printed; alpha present in alphas table.
    If quota not met: stop_reason = "dry" printed.

    **ROADMAP success criteria map:**
    SC-1: Enumeration uses catalog entries → verified by n_combos count in log
    SC-2: Pre-filter observable → validate_dropped count in log
    SC-3: Probe abandon → log message "template abandoned after probe" (if triggered) or probe survives
    SC-4: ≤3 concurrent, cached session, stops on quota/dry/401 → observable in logs
    SC-5: AI absent → test_no_llm_imports passes + LLM never called during run
  </how-to-verify>

  <resume-signal>
    Type "approved" if all checks pass (or describe which checks passed and which failed).
    If a check failed, describe: which check, what was observed vs expected.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → /bruteforce command | User-supplied CLI flags (--delay, --quota, etc.) parsed by argparse; typed Python values reach engine |
| bruteforce.md command prompt → agent | Command file is a Claude Code agent prompt; auth instructions must be unambiguous to prevent agent from re-authing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Authentication | wq_login.login() called multiple times by agent misreading command | mitigate | bruteforce.md uses identical "ONCE — never inside the loop" callout as hunt.md; bolded and in auth constraint section; [AGENT: Single-shot auth] step label makes it unambiguous |
| T-07-04-02 | Denial of Service | Agent ignores 401 and retries (biometric throttle risk) | mitigate | [AGENT: Handle 401] step specifies sys.exit(1) explicitly; command file has no retry language anywhere |
| T-07-04-03 | Denial of Service | User sets --quota to a very large number exhausting all sim slots | accept | Quota controls additive survivors, not total sims; the run naturally stops at "dry" when all templates processed; simulation count bounded by n_combos across all templates |
| T-07-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages; this plan adds only a markdown file and one Python test |
</threat_model>

<verification>
Final phase verification checklist:
- python -m pytest test_phase7.py -v exits 0 (11 tests)
- ls .claude/commands/bruteforce.md returns the file
- grep -c "login()" .claude/commands/bruteforce.md returns at least 1
- grep -c "ONCE" .claude/commands/bruteforce.md returns at least 1 (auth constraint)
- grep -c "ThreadPoolExecutor" .claude/commands/bruteforce.md returns at least 1 (concurrency)
- python bruteforce.py --help exits 0 (argparse working)
- Human checkpoint approved (all 5 ROADMAP SC-1..SC-5 confirmed)
</verification>

<success_criteria>
- .claude/commands/bruteforce.md exists with all sections: title, auth callout, 4 agent steps,
  stop conditions, flags table, auth/concurrency constraints, module refs, output
- test_no_llm_imports passes (BF-05: AI-free verified statically)
- Human checkpoint confirms all 5 Phase 7 ROADMAP success criteria:
  SC-1: Catalog-grounded enumeration (zero unknown-token combos reach BRAIN)
  SC-2: Validate pre-filter log observable
  SC-3: Probe abandon mechanism works
  SC-4: Bulk-sim ≤3 concurrent, stops on quota/401/dry, additivity gate required
  SC-5: Tool runs with AI completely absent
</success_criteria>

<output>
Create `.planning/phases/07-brute-force-tool-tool-b/07-04-SUMMARY.md` when done
</output>
