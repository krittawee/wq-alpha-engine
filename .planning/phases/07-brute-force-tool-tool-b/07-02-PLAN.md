---
phase: 07-brute-force-tool-tool-b
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - templates.py
  - test_phase7.py
autonomous: true
requirements:
  - BF-01
  - BF-02
must_haves:
  truths:
    - "Per D-01, TEMPLATES is an in-repo Python data structure in templates.py (mirroring delay0_candidates._D0_CANDIDATES) — no YAML/JSON parser, version-controlled and AI-free"
    - "Per D-02, the TEMPLATES list contains exactly the 4 ACE-inspired shapes: sentiment_rank, fundamental_value, residual_momentum, beta_neutral (shapes only — no ACE relogin/auto-submit)"
    - "Per D-03, a field slot may declare a catalog filter (e.g. dataset/type) that auto-expands to matching synced fields OR list literal tokens; expand_slots(conn, template) returns a list of (expression_string, settings_dict) tuples — all field tokens verified to exist in the datafields table (validate.validate backstop in Plan 07-03)"
    - "Per D-06, probe_spread_sample(combos, slot_value_lists, size=5) covers every distinct slot value at least once across returned combos (spread, not first-N or random); default size 5, configurable via --probe-size"
    - "VECTOR-type slots in the sentiment shape are wrapped in vec_avg() in the expression template, not bare field tokens"
    - "No template expression uses the simulate() regular= parameter; expression is just the FastExpr string passed positionally per CLAUDE.md"
    - "templates.py has zero imports of LLM/AI libraries (grep bruteforce.py templates.py for 'claude\|anthropic\|llm' returns no match)"
    - "All 4 new tests in test_phase7.py pass: test_template_enumeration, test_slot_expansion, test_validate_gate, test_probe_spread_sample"
  artifacts:
    - path: "templates.py"
      provides: "TEMPLATES list (4 shapes) + expand_slots + probe_spread_sample"
      contains: "TEMPLATES"
      exports: ["TEMPLATES", "expand_slots", "probe_spread_sample"]
    - path: "test_phase7.py"
      provides: "4 additional tests covering BF-01/BF-02"
      contains: "test_template_enumeration"
  key_links:
    - from: "templates.TEMPLATES[i]['slots']"
      to: "expand_slots(conn, template)"
      via: "catalog filter dict OR literal list"
      pattern: "SELECT DISTINCT id FROM datafields"
    - from: "expand_slots(conn, template)"
      to: "itertools.product(expression_combos, settings_combos)"
      via: "bruteforce.py Plan 07-03"
      pattern: "itertools\\.product"
    - from: "probe_spread_sample(combos, slot_lists, size)"
      to: "bruteforce.py probe phase"
      via: "imported from templates"
      pattern: "probe_spread_sample"
---

<objective>
Create templates.py with the 4 ACE-inspired template shapes, catalog-grounded slot
expansion logic, and the probe-spread sampling helper. Add 4 unit tests to test_phase7.py.
This plan runs in Wave 2 (after Plan 07-01) — templates.py is a new file, and Task 2
appends tests to test_phase7.py which Plan 07-01 Task 2 creates.

Purpose: Plan 07-03 imports TEMPLATES, expand_slots, and probe_spread_sample from templates.py.
These must exist and be correct before the engine can be written. The 4 template shapes
also give bruteforce an immediate working run out of the box (D-02).

Output: templates.py (new) with 4 concrete ACE-inspired shapes; test_phase7.py updated with
4 new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-brute-force-tool-tool-b/07-CONTEXT.md
@.planning/phases/07-brute-force-tool-tool-b/07-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from live code. -->

From delay0_candidates.py (structural model for templates.py per D-01 — lines 48-89):
The file defines a module-level list of dicts with descriptive fields, annotated comments,
and CLAIMS annotations. Mirror this pattern.

From optimizer.py (ARCHETYPE_HEURISTICS per D-04 — lines 42-90):
```python
ARCHETYPE_HEURISTICS: dict[str, list[tuple]] = {
    "reversal":        [(6, "SUBINDUSTRY", 0.07), (3, "MARKET", 0.08), ...],
    "momentum":        [(6, "INDUSTRY", 0.07), ...],
    "value_garp":      [(3, "SUBINDUSTRY", 0.08), ...],
    "sentiment_event": [(3, "SUBINDUSTRY", 0.08), (6, "INDUSTRY", 0.07), ...],
    # ... 8 archetypes total
}
# Each tuple: (decay, neutralization, truncation)
```

From grade.py (settings base pattern — lines 44-59):
```python
_BASE_SETTINGS = {
    "nanHandling": "OFF", "instrumentType": "EQUITY",
    "delay": 1, "universe": "TOP3000", "truncation": 0.08,
    "unitHandling": "VERIFY", "testPeriod": "P1Y",
    "pasteurization": "ON", "region": "USA", "language": "FASTEXPR",
    "decay": 15, "neutralization": "SUBINDUSTRY",
}
# NOTE: "regular" key is NEVER included — CLAUDE.md: buggy param, always use default
```

From db.py (datafields table schema — line 36):
```sql
PRIMARY KEY (id, region, universe, delay, dataset)
-- columns: id, description, dataset, region, universe, delay, type
-- type values seen: 'MATRIX', 'VECTOR', 'DOUBLE'
-- dataset examples: 'fundamental6', 'nws12', 'price_volume'
```

From delay0_candidates.py (CLAIMED_DELAY0_FIELDS — lines 55-89):
```python
# Delay-0 confirmed fields (empirically confirmed via Test A, alpha e7rvXqwz):
# volume, vwap, open, close, nws12_afterhsz_sl (VECTOR — must use vec_avg)
# Other fields in this list are CLAIMED but unverified — probe will catch issues.
```

From validate.py (signature — line 23):
```python
def validate(conn: sqlite3.Connection, expression: str) -> tuple[bool, str]:
    """Returns (True, '') if valid; (False, reason) if not."""
```

From 07-RESEARCH.md Q6 (probe_spread_sample algorithm) and Q3 (expand_slot pattern):
See research file for full algorithm text.

Template expression format rules (CLAUDE.md + RESEARCH.md pitfall 3):
- VECTOR-type fields MUST be wrapped: vec_avg({field}) not bare {field}
- Never use named param regular= when calling simulate(); expression is the FastExpr string
- {slot_name} placeholders in expression templates are replaced by expand_slots
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create templates.py with 4 ACE-inspired shapes + expand_slots + probe_spread_sample</name>
  <files>templates.py</files>

  <read_first>
    - delay0_candidates.py lines 1-89: module-level comment style, list-of-dicts pattern,
      CLAIMED annotation convention. Mirror structurally.
    - optimizer.py lines 42-90: ARCHETYPE_HEURISTICS for archetype keys that settings_archetype
      can reference. The 4 shapes should use archetypes that exist in this dict.
    - grade.py lines 44-59: _BASE_SETTINGS structure that settings dicts extend. Template
      settings_archetype maps to a key in ARCHETYPE_HEURISTICS.
    - 07-RESEARCH.md §Code Examples (template data structure), Q3 (expand_slot), Q6
      (probe_spread_sample algorithm). Use these exactly as the basis — do not invent new
      approaches for slot expansion or probe sampling.
    - 07-RESEARCH.md §Pitfall 3: VECTOR-type fields must be wrapped in vec_avg. The sentiment
      shape uses nws12 VECTOR fields — expression template must be vec_avg({field}).
    - 07-RESEARCH.md §Pitfall 4: SELECT DISTINCT id required in expand_slot to avoid duplicates.
    - CLAUDE.md §Constraints: simulate() regular param is buggy — never include "regular" as a
      key; expression templates produce bare FastExpr strings only.
  </read_first>

  <behavior>
    - len(TEMPLATES) == 4
    - All 4 names present: "sentiment_rank", "fundamental_value", "residual_momentum", "beta_neutral"
    - Each template dict has keys: name, archetype, description, slots, expression, settings_archetype
    - Each template's settings_archetype is a key in optimizer.ARCHETYPE_HEURISTICS
    - expand_slots(conn, template) with in-memory DB returns a list of (expr_str, settings_dict) tuples
    - For literal slots (list of values), expand_slots substitutes each value into the expression
    - For catalog filter slots (dict with dataset/type keys), expand_slots queries
      SELECT DISTINCT id FROM datafields WHERE ... and substitutes each returned id
    - The sentiment shape expression contains "vec_avg(" — VECTOR fields are wrapped
    - probe_spread_sample(combos, slot_value_lists, size=5) with combos covering 6 distinct field
      values returns at most 5 items covering all 6 field values (greedy cover, then fill)
    - No LLM/AI imports: grep for claude|anthropic|llm in templates.py returns empty
  </behavior>

  <action>
    Create templates.py as a module-level data file (no classes, no CLI). Structure:

    MODULE DOCSTRING: "templates.py — Parameterized alpha template shapes for /bruteforce.
    Templates are in-repo Python data structures (D-01). Slot expansion queries the live
    catalog (datafields table). Probe-spread sampling covers all distinct slot values.
    No AI/LLM dependency. All VECTOR-type fields must be wrapped in vec_avg() in
    expression templates (pitfall 3)."

    IMPORTS: sqlite3, itertools, typing (List, Tuple, Dict, Any). Nothing else.
    No import of grade, optimizer, or any BRAIN-touching module — templates.py is pure
    data + catalog query; settings_archetype is a string key, looked up by bruteforce.py.

    TEMPLATES list — exactly 4 shapes authored as concrete, runnable FastExpr templates
    targeting delay-0 fields from CLAIMED_DELAY0_FIELDS where possible. Each has:
      - name: snake_case string
      - archetype: string matching an ARCHETYPE_HEURISTICS key
      - description: one-sentence human-readable purpose
      - slots: dict of slot_name -> (list of literals OR dict with dataset/type keys)
      - expression: FastExpr template string using {slot_name} placeholders
      - settings_archetype: string key into ARCHETYPE_HEURISTICS

    The 4 shapes:

    1. "sentiment_rank" — rank of sentiment signal over a window (delay-0 safe):
       slots.field: {"dataset": "nws12", "type": "VECTOR"}  # catalog-expanded; wrap in vec_avg
       slots.window: [5, 10, 20]  # literal
       expression: "rank(ts_sum(vec_avg({field}), {window}))"
       settings_archetype: "sentiment_event"

    2. "fundamental_value" — rank of fundamental ratio over a window:
       slots.field: {"dataset": "fundamental6", "type": "MATRIX"}  # catalog-expanded
       slots.window: [5, 10, 20]
       expression: "rank(ts_mean({field}, {window}))"
       settings_archetype: "value_garp"

    3. "residual_momentum" — price momentum residual (delay-0 confirmed fields):
       slots.fast: [3, 5, 10]   # literal fast window
       slots.slow: [20, 40, 60]  # literal slow window
       expression: "rank(ts_mean(close, {fast}) / ts_mean(close, {slow}) - 1)"
       settings_archetype: "momentum"

    4. "beta_neutral" — volume-weighted return relative to market:
       slots.window: [5, 10, 20]
       slots.field: ["volume", "vwap"]  # literal — confirmed delay-0 from CLAIMED_DELAY0_FIELDS
       expression: "rank(ts_corr({field}, close, {window}))"
       settings_archetype: "reversal"

    FUNCTION expand_slots(conn: sqlite3.Connection, template: dict) -> list[tuple[str, None]]:
    (Note: returns list of (expression_string, slot_values_tuple) — the settings dict is
    built by bruteforce.py using the template's settings_archetype; expand_slots handles
    only the expression slot cartesian product.)

    More precisely: expand_slots returns list[tuple] where each tuple is the filled
    expression string (with all {slot_name} placeholders replaced) paired with a dict
    of the slot values used (for probe_spread_sample to work on). Internal steps:
      1. For each slot in template["slots"]: call _expand_one_slot(conn, slot_def)
         → list of string values
      2. Build cartesian product via itertools.product(*expanded_slot_lists)
      3. For each combo: fill template["expression"] via str.format_map(slot_dict)
         where slot_dict maps each slot_name to its value
      4. Return list of (filled_expr, slot_value_dict) tuples

    INTERNAL _expand_one_slot(conn, slot_def) -> list[str]:
    If slot_def is a list: return slot_def (literal)
    If slot_def is a dict: query SELECT DISTINCT id FROM datafields WHERE ...
    using dataset and/or type keys. Return [r[0] for r in rows].

    FUNCTION probe_spread_sample(combos: list, slot_names: list[str], size: int = 5) -> list:
    Implements the greedy-cover algorithm from 07-RESEARCH.md Q6 exactly:
    combos is the list of (filled_expr, slot_value_dict) tuples from expand_slots.
    slot_names is template["slots"].keys() (the slot dimension names).
    Stage 1: iterate combos in order; pick a combo if it adds a new value in ANY slot
    dimension; stop when len(selected) >= size.
    Stage 2: fill remaining slots up to size from the front of the unseen combos.
    Return selected list (subset of combos).

    Do NOT add any __main__ block. This is a pure data/utility module.
  </action>

  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import templates, sqlite3, db

# Basic shape checks
assert len(templates.TEMPLATES) == 4, f'expected 4 templates, got {len(templates.TEMPLATES)}'
names = {t['name'] for t in templates.TEMPLATES}
assert names == {'sentiment_rank', 'fundamental_value', 'residual_momentum', 'beta_neutral'}, f'wrong names: {names}'

# Vector wrapping
sent = next(t for t in templates.TEMPLATES if t['name'] == 'sentiment_rank')
assert 'vec_avg({field})' in sent['expression'] or 'vec_avg(' in sent['expression'], 'VECTOR field not wrapped in vec_avg'

# No LLM imports
import ast, sys
with open('templates.py') as f:
    src = f.read()
for bad in ['claude', 'anthropic', 'llm', 'openai']:
    assert bad not in src.lower(), f'Found AI import: {bad}'

# expand_slots with literal slots (no DB needed for literals)
conn = db.init_db(':memory:')
beta = next(t for t in templates.TEMPLATES if t['name'] == 'beta_neutral')
combos = templates.expand_slots(conn, beta)
assert len(combos) > 0, 'expand_slots returned empty list'
assert isinstance(combos[0], tuple) and len(combos[0]) == 2, f'combo shape wrong: {combos[0]}'
expr0 = combos[0][0]
assert '{' not in expr0, f'Unfilled placeholder in: {expr0}'

# probe_spread_sample covers all slot values up to size
momentum = next(t for t in templates.TEMPLATES if t['name'] == 'residual_momentum')
combos2 = templates.expand_slots(conn, momentum)
slot_names = list(momentum['slots'].keys())
sample = templates.probe_spread_sample(combos2, slot_names, size=5)
assert len(sample) <= 5, 'probe sample exceeds size'
# All distinct fast values covered
fast_vals = {c[1].get('fast') or c[1].get(slot_names[0]) for c in sample}
assert len(fast_vals) >= len(momentum['slots']['fast']), f'not all fast values covered: {fast_vals}'

print('ALL OK')
"
</automated>
  </verify>

  <done>
    templates.py exists with TEMPLATES (4 shapes), expand_slots, and probe_spread_sample.
    All verify assertions pass. No LLM imports. VECTOR fields wrapped in vec_avg.
    Each template's settings_archetype matches an existing ARCHETYPE_HEURISTICS key.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 4 BF-01/BF-02 unit tests to test_phase7.py</name>
  <files>test_phase7.py</files>

  <read_first>
    - test_phase7.py as created by Plan 07-01 Task 2: understand the fresh_db fixture and
      make_mock_* helper signatures before appending tests. Do NOT overwrite existing content.
    - templates.py (just created in Task 1): study expand_slots and probe_spread_sample
      signatures to write accurate assertions.
    - validate.py lines 23-94: understand the validate(conn, expression) signature for
      test_validate_gate (which mocks it).
    - 07-RESEARCH.md §Validation Architecture (BF-01/BF-02 test map rows).
  </read_first>

  <action>
    Append 4 new test functions to test_phase7.py (after the existing test_bruteforce_runs_schema).
    Do NOT delete or modify any existing content.

    test_template_enumeration():
      Uses db.init_db(':memory:'). Calls templates.expand_slots(conn, beta_template) where
      beta_template is templates.TEMPLATES[name='beta_neutral'] (2 field literals x 3 window
      literals = 6 combos). Asserts len(combos) == 6. Asserts all combo expressions have
      no unfilled {placeholder}. Asserts each combo is a tuple of (str, dict).

    test_slot_expansion():
      Tests the catalog-filter path. Uses db.init_db(':memory:'). Insert 2 rows into
      datafields with dataset='nws12' and type='VECTOR' and 1 row with dataset='nws12'
      and type='MATRIX' (to verify type filter). Call templates.expand_slots(conn,
      sentiment_template) where sentiment_template uses {"dataset":"nws12","type":"VECTOR"}
      slot. Assert only VECTOR field ids appear in the resulting expressions (not the MATRIX
      one). The in-memory DB starts empty so this tests the catalog-filter path in isolation.
      If 0 rows in datafields, expand_slots should return 0 combos (not error) — assert
      gracefully: len(combos_empty) == 0 when DB has no matching fields.

    test_validate_gate():
      Tests that every combo from expand_slots passes through validate.validate before
      reaching simulation. Uses unittest.mock.patch('validate.validate', return_value=(False, 'bad token')).
      In the actual bruteforce engine (Plan 07-03), validate is called per-combo. This test
      verifies the contract: given that validate returns (False, ...), no combo should be
      simmed. Since bruteforce.py doesn't exist yet, test the contract as a unit property:
      build a list of 3 combos, filter using a mocked validate, assert 0 pass through.
      This tests the intended logic pattern Plan 07-03 MUST implement (validate gate).

    test_probe_spread_sample():
      Builds a synthetic combos list of 9 tuples (3 field values x 3 window values) as
      (expr_str, {"field": f, "window": w}) tuples. Calls probe_spread_sample(combos,
      ["field", "window"], size=5). Asserts: len(result) <= 5; set of field values in result
      covers all 3 distinct field values; set of window values in result covers all 3 distinct
      window values (greedy cover should achieve this within 3-6 combos).

    Keep each test function self-contained (no shared state between tests). Use
    db.init_db(':memory:') inline, not a module-level connection.
  </action>

  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -m pytest test_phase7.py -x -q 2>&1 | tail -8</automated>
  </verify>

  <done>
    pytest test_phase7.py -x exits 0; all 5 tests (1 schema + 4 new) pass;
    no import errors; test_validate_gate demonstrates the validate-gate contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| templates.py slot filter → SQLite catalog | Catalog filter dict keys (dataset, type) control which fields expand; no user input flows here |
| expand_slots expression output → validate.validate | Every output expression is passed to validate before reaching BRAIN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | expand_slots SQL WHERE clause | mitigate | Only dataset and type keys are used in WHERE; both are hardcoded in TEMPLATES (no user-supplied filter keys); parameterized query prevents injection |
| T-07-02-02 | Tampering | expression template placeholder substitution | mitigate | {slot_name} placeholders are filled via str.format_map from catalog-returned field ids; validate.validate then rejects any unknown token before simulation |
| T-07-02-03 | Tampering | VECTOR fields used bare (pitfall 3) | mitigate | Expression templates in TEMPLATES wrap VECTOR slots in vec_avg(); plan enforces this in acceptance criteria and verify assertion |
| T-07-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced; stdlib only |
</threat_model>

<verification>
After both tasks:
- python -m pytest test_phase7.py -x -q exits 0 (5 tests pass)
- python -c "from templates import TEMPLATES, expand_slots, probe_spread_sample; print(len(TEMPLATES))" prints 4
- grep -c "vec_avg" templates.py returns at least 1 (sentiment shape)
- grep -v "^#" templates.py | grep -c "claude\|anthropic\|llm" returns 0
- grep -c "SELECT DISTINCT id FROM datafields" templates.py returns at least 1
</verification>

<success_criteria>
- templates.py with TEMPLATES (4 shapes), expand_slots, probe_spread_sample exists
- All 4 templates have: name, archetype, description, slots, expression, settings_archetype
- VECTOR fields wrapped in vec_avg in expression templates
- expand_slots uses SELECT DISTINCT id for catalog filter slots
- probe_spread_sample greedy-covers all distinct slot values up to size
- 5 total tests in test_phase7.py pass (1 from Plan 07-01 + 4 from this plan)
</success_criteria>

<output>
Create `.planning/phases/07-brute-force-tool-tool-b/07-02-SUMMARY.md` when done
</output>
