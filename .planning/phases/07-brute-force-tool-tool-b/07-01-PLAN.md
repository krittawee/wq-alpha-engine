---
phase: 07-brute-force-tool-tool-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db.py
  - test_phase7.py
autonomous: true
requirements:
  - BF-06
must_haves:
  truths:
    - "Running db.init_db(':memory:') on a fresh DB creates the bruteforce_runs table without error"
    - "insert_bruteforce_run writes a row; update_bruteforce_run patches it; both are idempotent-safe"
    - "The bruteforce_runs schema has all columns required by the D-11 spec: run_id, template_name, delay, quota_target, n_combos, n_validated, n_probed, n_simmed, n_survivors, n_additive, quota_hit, partial, failure_counts, examples, started_at, finished_at"
    - "An index on bruteforce_runs(run_id) exists"
    - "test_phase7.py scaffold is present with fixtures; pytest test_phase7.py -x runs without import errors"
  artifacts:
    - path: "db.py"
      provides: "bruteforce_runs DDL + insert_bruteforce_run + update_bruteforce_run functions"
      contains: "bruteforce_runs"
    - path: "test_phase7.py"
      provides: "Phase 7 test scaffold with fixtures and test_bruteforce_runs_schema"
      contains: "test_bruteforce_runs_schema"
  key_links:
    - from: "db.py _DDL list"
      to: "bruteforce_runs table"
      via: "init_db() DDL execution"
      pattern: "CREATE TABLE IF NOT EXISTS bruteforce_runs"
    - from: "bruteforce.py (Plan 07-03)"
      to: "db.insert_bruteforce_run + db.update_bruteforce_run"
      via: "import db"
      pattern: "db\\.insert_bruteforce_run\\|db\\.update_bruteforce_run"
---

<objective>
Add the `bruteforce_runs` table schema and CRUD functions to db.py, and create the
test_phase7.py scaffold with fixtures. This is the foundational data layer Plan 07-03
depends on for persisting per-template failure aggregates (D-11, BF-06).

Purpose: Plan 07-03 (bruteforce engine) needs insert_bruteforce_run and
update_bruteforce_run before it can be written. The test scaffold unblocks Plan 07-02
(templates) from adding its own tests immediately.

Output: db.py with bruteforce_runs DDL + 2 CRUD functions; test_phase7.py with fixtures
and 1 schema test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-brute-force-tool-tool-b/07-CONTEXT.md
@.planning/phases/07-brute-force-tool-tool-b/07-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from live code. -->

From db.py (existing DDL pattern — lines 15-55):
```python
_DDL = [
    """CREATE TABLE IF NOT EXISTS runs (
      run_id TEXT PRIMARY KEY, thesis TEXT, started_at TEXT,
      iterations INTEGER, num_pass INTEGER, notes TEXT
    )""",
    # ... other tables
]

def init_db(path: str = "alpha_kb.db") -> sqlite3.Connection:
    conn = sqlite3.connect(path, check_same_thread=False)
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute("PRAGMA busy_timeout=30000")
    for ddl in _DDL:
        conn.execute(ddl)
    conn.commit()
    return conn
```

From db.py (upsert pattern to mirror for insert/update — lines 68-97):
```python
def upsert_alpha(conn, alpha: dict) -> None:
    cols = [c for c in _ALPHA_COLS if c in alpha]
    placeholders = ", ".join("?" for _ in cols)
    col_list = ", ".join(cols)
    conn.execute(
        f"INSERT OR REPLACE INTO alphas ({col_list}) VALUES ({placeholders})",
        tuple(alpha[c] for c in cols),
    )
    conn.commit()
```

From 07-RESEARCH.md Q5 (recommended bruteforce_runs DDL):
```sql
CREATE TABLE IF NOT EXISTS bruteforce_runs (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  run_id       TEXT NOT NULL,
  template_name TEXT NOT NULL,
  delay        INTEGER,
  quota_target INTEGER,
  n_combos     INTEGER,
  n_validated  INTEGER,
  n_probed     INTEGER,
  n_simmed     INTEGER,
  n_survivors  INTEGER,
  n_additive   INTEGER,
  quota_hit    INTEGER DEFAULT 0,
  partial      INTEGER DEFAULT 0,
  failure_counts TEXT,
  examples     TEXT,
  started_at   TEXT,
  finished_at  TEXT
)
```
```sql
CREATE INDEX IF NOT EXISTS idx_bruteforce_runs_run ON bruteforce_runs(run_id)
```

failure_counts JSON schema: {"validate_dropped": N, "sim_error": N, "IS_fail_SHARPE": N,
  "IS_fail_TURNOVER": N, "gate_fail_correlated": N, "probe_abandoned": N}
examples JSON schema: {"validate_dropped": ["expr1", ...], "IS_fail_SHARPE": ["expr1", ...]}
(max 3 per class to avoid bloat)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bruteforce_runs DDL + CRUD to db.py</name>
  <files>db.py</files>

  <read_first>
    - db.py lines 1-140 in full: understand _DDL list structure, init_db, all existing upsert/insert
      patterns. The executor MUST replicate the same style (parameterized INSERT, conn.commit()
      after each write, no ORM).
    - 07-RESEARCH.md §Q5 (bruteforce_runs schema and CRUD patterns) — exact DDL to add.
    - 07-CONTEXT.md §D-11 — locked decision on failure_counts + examples as JSON TEXT columns.
  </read_first>

  <behavior>
    - After init_db(':memory:'): conn.execute("SELECT name FROM sqlite_master WHERE type='table' AND name='bruteforce_runs'").fetchone() is not None
    - After init_db(':memory:'): conn.execute("SELECT name FROM sqlite_master WHERE type='index' AND name='idx_bruteforce_runs_run'").fetchone() is not None
    - insert_bruteforce_run(conn, {"run_id": "r1", "template_name": "t1"}) returns an integer rowid
    - update_bruteforce_run(conn, rowid, {"n_additive": 3, "finished_at": "2026-01-01"}) patches only those columns; other columns unchanged
    - init_db is idempotent: calling it twice on same path does not raise
  </behavior>

  <action>
    Append two items to the _DDL list in db.py (per D-11, after the existing table DDLs):
    (1) The CREATE TABLE IF NOT EXISTS bruteforce_runs statement with all 17 columns listed
    in the interfaces block above: id (AUTOINCREMENT PK), run_id, template_name, delay,
    quota_target, n_combos, n_validated, n_probed, n_simmed, n_survivors, n_additive,
    quota_hit (DEFAULT 0), partial (DEFAULT 0), failure_counts (TEXT), examples (TEXT),
    started_at, finished_at.
    (2) CREATE INDEX IF NOT EXISTS idx_bruteforce_runs_run ON bruteforce_runs(run_id).

    Add two new public functions after the existing upsert functions:

    insert_bruteforce_run(conn, row: dict) -> int
      Inserts a row into bruteforce_runs using the same dynamic col_list + placeholders
      pattern as upsert_alpha. Uses plain INSERT (not INSERT OR REPLACE — each run invocation
      produces a unique row). Calls conn.commit(). Returns cur.lastrowid.

    update_bruteforce_run(conn, rowid: int, updates: dict) -> None
      Updates an existing row by id using SET k=? WHERE id=?. Calls conn.commit().
      updates is a dict of column-name→value pairs; only those columns are changed.

    Do NOT add any imports — json serialization happens in bruteforce.py before calling
    insert_bruteforce_run (failure_counts and examples are passed in as JSON strings, not
    dicts). Do NOT modify init_db's signature or behavior beyond adding the DDL entries to
    the _DDL list.
  </action>

  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import db, json
conn = db.init_db(':memory:')
# Table exists
t = conn.execute(\"SELECT name FROM sqlite_master WHERE type='table' AND name='bruteforce_runs'\").fetchone()
assert t, 'table missing'
# Index exists
i = conn.execute(\"SELECT name FROM sqlite_master WHERE type='index' AND name='idx_bruteforce_runs_run'\").fetchone()
assert i, 'index missing'
# Insert
rowid = db.insert_bruteforce_run(conn, {
    'run_id': 'test-run', 'template_name': 'sentiment_rank',
    'delay': 0, 'quota_target': 5, 'n_combos': 100,
    'n_validated': 80, 'n_probed': 5, 'n_simmed': 20,
    'n_survivors': 3, 'n_additive': 1, 'quota_hit': 0, 'partial': 0,
    'failure_counts': json.dumps({'validate_dropped': 20}),
    'examples': json.dumps({'validate_dropped': ['rank(x)']}),
    'started_at': '2026-01-01T00:00:00'
})
assert isinstance(rowid, int) and rowid > 0, 'bad rowid'
# Update
db.update_bruteforce_run(conn, rowid, {'n_additive': 3, 'finished_at': '2026-01-01T01:00:00'})
row = conn.execute('SELECT n_additive, finished_at FROM bruteforce_runs WHERE id=?', (rowid,)).fetchone()
assert row[0] == 3 and row[1] == '2026-01-01T01:00:00', 'update failed'
# Idempotent reinit
conn2 = db.init_db(':memory:')
print('ALL OK')
"
</automated>
  </verify>

  <done>
    db.py _DDL contains CREATE TABLE IF NOT EXISTS bruteforce_runs and the idx index;
    insert_bruteforce_run and update_bruteforce_run exist and pass the inline verify assertions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test_phase7.py scaffold with fixtures and schema test</name>
  <files>test_phase7.py</files>

  <read_first>
    - test_phase4.py: study the existing fixture patterns (in-memory SQLite, mock patch targets,
      test class or function style). The new test file must match the same pytest style.
    - db.py: verify the init_db(':memory:') signature for the in-memory fixture.
    - 07-RESEARCH.md §Validation Architecture (test map, fixture list, wave-0 gaps) — lists all
      tests that must exist by end of Phase 7; scaffold only the fixtures + one schema test now;
      remaining tests are added in Plans 07-02 and 07-03.
  </read_first>

  <action>
    Create test_phase7.py with:

    1. Module docstring: "Phase 7: Brute-Force Tool (Tool B) — test suite. Zero real BRAIN API calls."

    2. Imports: pytest, sqlite3, json, unittest.mock (patch, MagicMock), db (from local package).
       No import of bruteforce or templates yet — those modules don't exist until Plans 07-02/03.

    3. Shared fixture function or pytest fixture `fresh_db()` → returns a db.init_db(':memory:')
       connection. Used by all tests in this file.

    4. Helper fixtures (plain functions, not pytest fixtures, so they can be imported by Plans
       07-02/03 tests added later):
       - make_mock_grade_one_result(status="pass", alpha_id="test_alpha_001") -> dict
         Returns a dict matching grade.grade_one's return shape:
         {"status": status, "alpha_id": alpha_id, "checks": [], "expression": "rank(close/open)"}
       - make_mock_classify_result(status="near") -> tuple
         Returns (status, []) — matches editor.classify_from_checks return.
       - make_mock_additivity_result(additive=True, proxy_drop=False) -> object
         Returns a MagicMock with .additive=additive, .proxy_drop=proxy_drop, .alpha_id="test_alpha_001".

    5. One concrete test:
       def test_bruteforce_runs_schema():
         conn = fresh_db()  (or use the fixture inline)
         Asserts: table exists, index exists, insert_bruteforce_run writes a row with all
         expected columns readable back (run_id, template_name, n_combos, failure_counts as
         JSON string, examples as JSON string), update_bruteforce_run patches n_additive to 3.
         This mirrors the inline verify from Task 1 but as a proper pytest test.

    Do NOT stub or mock db itself — use the real db.init_db(':memory:').
    No fenced code blocks in this action; see read_first files for exact pytest conventions.
  </action>

  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -m pytest test_phase7.py::test_bruteforce_runs_schema -x -q 2>&1 | tail -5</automated>
  </verify>

  <done>
    test_phase7.py exists; pytest test_phase7.py -x exits 0; test_bruteforce_runs_schema passes;
    fresh_db fixture and 3 make_mock_* helpers are importable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bruteforce.py → db.py | Caller passes row dicts and update dicts; db.py executes parameterized SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | insert_bruteforce_run dynamic col_list | mitigate | Column names come from caller's dict keys — only known column names survive SQL execution; unknown keys cause OperationalError rather than injection; no user-supplied input flows here (all values are internal counters/timestamps/json strings from bruteforce.py) |
| T-07-01-02 | Tampering | failure_counts / examples JSON TEXT columns | accept | JSON is serialized by bruteforce.py before insert; these are read-only aggregates, never re-parsed and re-executed; no SQL injection vector |
| T-07-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 7 introduces zero new packages; no install tasks in this plan |
</threat_model>

<verification>
After both tasks:
- python -m pytest test_phase7.py -x -q passes (1 test)
- python -c "import db; conn = db.init_db(':memory:'); print(conn.execute(\"SELECT COUNT(*) FROM sqlite_master WHERE name='bruteforce_runs'\").fetchone())" prints (1,)
- grep -c "insert_bruteforce_run\|update_bruteforce_run" db.py returns 2 (function definitions)
- grep -c "bruteforce_runs" db.py returns at minimum 3 (CREATE TABLE, CREATE INDEX, and function body)
</verification>

<success_criteria>
- bruteforce_runs table with 17 columns and idx_bruteforce_runs_run index created by init_db
- insert_bruteforce_run(conn, row) -> int and update_bruteforce_run(conn, rowid, updates) -> None implemented
- test_phase7.py scaffold present with fresh_db fixture, 3 make_mock_* helpers, and 1 passing test
- pytest test_phase7.py -x exits 0
- No modifications to any existing db.py function signatures
</success_criteria>

<output>
Create `.planning/phases/07-brute-force-tool-tool-b/07-01-SUMMARY.md` when done
</output>
