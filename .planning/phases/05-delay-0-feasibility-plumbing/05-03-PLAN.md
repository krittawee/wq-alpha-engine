---
phase: 05-delay-0-feasibility-plumbing
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - probe_delay.py
  - delay0_candidates.py
  - .planning/phases/05-delay-0-feasibility-plumbing/05-VERIFICATION.md
autonomous: false
requirements:
  - DLY-01
  - DLY-02

must_haves:
  truths:
    - "A single delay-0 simulation from code produces a BRAIN-returned delay value (0 or 1) that is captured and logged"
    - "If BRAIN returns delay=0 the result is recorded as confirmed evidence; if BRAIN returns delay=1 the coercion is logged with the full probe context (payload used, returned settings)"
    - "The probe verdict is documented in a verification record the user can inspect"
    - "D-02: If Test A is coerced, Test B runs using the captured UI delay-0 payload verbatim — each of the three payload differences (maxTrade, decay, testPeriod) is tested independently before any combination, confirming which setting trips coercion"
    - "The verification path uses probe_delay.run_probe() (not ad-hoc _simulate_to_alpha calls) so the path being verified is the same path future code uses"
  artifacts:
    - path: ".planning/phases/05-delay-0-feasibility-plumbing/05-VERIFICATION.md"
      provides: "Probe outcome record: Test A result, Test B result if needed, final verdict"
      contains: "BRAIN returned delay="
  key_links:
    - from: "verify_delay0.py (ephemeral script)"
      to: "probe_delay.run_probe()"
      via: "run_probe returns ProbeResult with returned_settings and settings_sent — no ad-hoc _simulate_to_alpha"
    - from: "Test B settings"
      to: "05-CONTEXT.md captured UI delay-0 payload"
      via: "verbatim copy of the 13-key settings object from the <specifics> section, passed via settings_override="
---

<objective>
Run the empirical verification experiment (D-01) using probe_delay.run_probe() to confirm whether BRAIN actually returns delay=0 when requested from our code. The experiment runs as a SINGLE Python script (verify_delay0.py) that the user starts after authenticating — auth happens once at script start, then both Test A and Test B (if needed) run in the same process without interruption.

- Test A (minimal change): use our proven _BASE_SETTINGS with delay=0, fire one sim via run_probe(), read BRAIN's returned delay and full returned_settings.
- Test B (fallback, only if A is coerced): use the captured UI delay-0 payload verbatim (known to produce real delay-0 in the web UI), verify BRAIN returns delay=0. Test B bisects the three payload differences INDEPENDENTLY (not in a fixed stack): first test each differing key alone (maxTrade absent, then decay=3, then testPeriod=P1Y), then optionally test combinations if needed to isolate the trigger.

Document the outcome in 05-VERIFICATION.md. This is the gate that determines whether delay-0 is usable from code without special payload matching.

Purpose: ROADMAP success criterion 1 — "a single delay-0 simulation from code shows BRAIN-returned delay=0; if delay=1, the discrepancy is logged as a coercion warning."

Output: 05-VERIFICATION.md with probe verdict; probe_delay.py and delay0_candidates.py updated if Test B bisection reveals the coercion trigger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-delay-0-feasibility-plumbing/05-CONTEXT.md
@.planning/phases/05-delay-0-feasibility-plumbing/05-01-SUMMARY.md
@.planning/phases/05-delay-0-feasibility-plumbing/05-02-SUMMARY.md

<interfaces>
From 05-CONTEXT.md <specifics> — captured UI delay-0 settings object (Test B input):
  {"nanHandling":"OFF","instrumentType":"EQUITY","delay":0,"universe":"TOP3000",
   "truncation":0.08,"unitHandling":"VERIFY","testPeriod":"P1Y","pasteurization":"ON",
   "region":"USA","language":"FASTEXPR","decay":3,"neutralization":"SUBINDUSTRY",
   "visualization":false}
  This is a 13-key object. Our _BASE_SETTINGS has 14 keys (extra: maxTrade:"ON").

Payload diff (05-CONTEXT.md):
  | Setting    | UI sends | Our code | Action for Test B |
  |------------|----------|----------|-------------------|
  | maxTrade   | absent   | "ON"     | omit in Test B    |
  | decay      | 3        | 15       | use 3 in Test B   |
  | testPeriod | P1Y      | P1Y6M    | use P1Y in Test B |
  | delay      | 0        | 0        | same              |

From probe_delay.py (Plan 02 output):
  run_probe(client, conn, requested_delay=0, settings_override=None) -> ProbeResult
    Always returns ProbeResult (never raises). ProbeResult has:
      returned_delay: int, alpha_id: str, requested_delay: int,
      coerced: bool, returned_settings: dict, settings_sent: dict
  probe_and_gate(client, conn, requested_delay=0, settings_override=None) -> ProbeResult
    Raises DelayCoercedError(requested_delay, returned_delay, alpha_id) if coerced.
    DelayCoercedError has .requested_delay, .returned_delay, .alpha_id fields.

From grade.py (Plan 01 output):
  _simulate_to_alpha(client, expression, settings=None, attempts=3)
    Does NOT persist to alpha_kb.db — purely simulation/result-fetching.
    Returns (sim, alpha_dict); alpha_dict["settings"]["delay"] = BRAIN's returned delay.
  Coercion detection emits "[grade] COERCION WARNING" to stderr (grade.py production code).
  Probe coercion surfaces as DelayCoercedError or "[probe_delay]" prefix on stderr —
  NOT as grade.py's "COERCION WARNING" (that fires only in grade_one/grade_many, not in run_probe).

BRAIN session constraint (CLAUDE.md):
  client = login() ONCE before any sims. Never call login() again mid-run.
  Concurrency cap: <= 3 concurrent sims on one session.
  Each probe sim takes ~2 minutes to complete.
</interfaces>

IMPORTANT: This plan requires a live authenticated BRAIN session. The user must run
wq_login.login() once at the start of verify_delay0.py. The script must NOT call
login() a second time under any circumstance.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write verify_delay0.py — single-process Test A + conditional Test B script</name>
  <files>.planning/phases/05-delay-0-feasibility-plumbing/05-VERIFICATION.md, verify_delay0.py (ephemeral script at project root — created and run by this task)</files>
  <read_first>
    - probe_delay.py (the module created in Plan 02) — understand run_probe() interface and ProbeResult fields (returned_settings, settings_sent, coerced)
    - grade.py lines 44-59 (_BASE_SETTINGS) — understand what settings are sent in Test A
    - 05-CONTEXT.md D-01 section — the ordered experiment protocol; Test A uses our proven settings with delay flipped
    - 05-CONTEXT.md "CAPTURED UI delay-0 request" <specifics> — the exact 13-key dict for Test B
    - CLAUDE.md § Constraints — never re-auth in-loop; 401 stops the run
  </read_first>
  <action>
    Create an ephemeral script verify_delay0.py in the project root. The script is the unit of work for this task; 05-VERIFICATION.md is the durable output.

    Script structure (verify_delay0.py):

    Section 1 — Auth (user triggers biometric once):
      from wq_login import login
      import sqlite3, sys, json, datetime
      import grade, probe_delay
      from pathlib import Path

      print("Starting verify_delay0.py — authenticate with Persona when prompted...")
      client = login()  # ONE call; triggers biometric flow
      conn = sqlite3.connect("alpha_kb.db")
      out = Path(".planning/phases/05-delay-0-feasibility-plumbing/05-VERIFICATION.md")
      lines = [f"# Delay-0 Verification — {datetime.datetime.now().isoformat()}"]

    Section 2 — Test A:
      Call probe_delay.run_probe(client, conn, requested_delay=0) — this uses _BASE_SETTINGS with delay flipped to 0 (no settings_override).
      Capture the ProbeResult. Extract:
        - result.settings_sent (full dict sent to BRAIN)
        - result.returned_settings (full dict returned from BRAIN)
        - result.returned_delay
        - result.alpha_id
        - result.coerced
      Append to lines:
        "## Test A — _BASE_SETTINGS with delay=0"
        f"Settings sent: {json.dumps(result.settings_sent, indent=2)}"
        f"Settings returned: {json.dumps(result.returned_settings, indent=2)}"
        f"BRAIN returned delay: {result.returned_delay}"
        "Verdict: Test A PASS: delay=0 confirmed" OR "Verdict: Test A COERCED: BRAIN returned delay={result.returned_delay}"
      Write lines to out immediately after Test A.

    If result.coerced is False: append "Test B skipped (Test A PASS)" to out, write file, print summary, exit.

    Section 3 — Test B (only if Test A coerced):
      Build the verbatim UI settings dict from 05-CONTEXT.md (13 keys, no maxTrade, decay=3, testPeriod="P1Y", delay=0).
      Append "## Test B — UI-verbatim settings" to lines.
      Call probe_delay.run_probe(client, conn, requested_delay=0, settings_override=ui_settings).
      Record result in same format as Test A.

      Then run INDEPENDENT bisection — test each of the 3 differing keys separately from the UI baseline, not stacked:
        Test B1 — "Does maxTrade cause coercion?": start from UI settings, add back maxTrade="ON". Call run_probe with settings_override.
        Test B2 — "Does decay cause coercion?": start from UI settings, change decay to 15 (our value). Call run_probe.
        Test B3 — "Does testPeriod cause coercion?": start from UI settings, change testPeriod to "P1Y6M" (our value). Call run_probe.
        If all three independent tests pass (no coercion), run Test B4 to test combinations:
          Test B4 — all three differences applied simultaneously (maxTrade="ON", decay=15, testPeriod="P1Y6M"): call run_probe.

      Record each step in lines with:
        - Settings object sent (full dict)
        - BRAIN returned delay
        - PASS or COERCED verdict
        - If COERCED: "Trigger identified: {key}" or "Trigger: combination of {keys}"

      Write lines to out after each sim (not just at the end) so partial results are preserved if a 401 interrupts.

    Section 4 — Conclusion:
      Based on Test A / Test B results, append a "## Conclusion" section:
        - Whether delay-0 is feasible from code
        - Which settings (if any) are required to avoid coercion
        - Recommended action for probe_delay.py (e.g., "update default settings", "no change needed")

      Write out.write_text("\n\n".join(lines)) and print the conclusion to stdout.

    Section 5 — Cleanup:
      conn.close()

    IMPORTANT constraints:
    - login() called exactly ONCE at script start.
    - No try/except around run_probe calls that would swallow 401 errors — let them propagate and terminate the script.
    - Each sim takes ~2 min; do not run more than 3 concurrently (they run sequentially in this script).
    - Do NOT call grade_one or grade_many — use run_probe only. run_probe uses _simulate_to_alpha which does NOT persist to DB.
    - After writing verify_delay0.py, immediately run Test A by executing the script (the user will be present for the biometric auth).
  </action>
  <verify>
    <automated>ls /Users/winter.__.kor/quant/.planning/phases/05-delay-0-feasibility-plumbing/05-VERIFICATION.md && grep -q "BRAIN returned delay=" /Users/winter.__.kor/quant/.planning/phases/05-delay-0-feasibility-plumbing/05-VERIFICATION.md && echo "Verification record written" || echo "MISSING verification record"</automated>
  </verify>
  <acceptance_criteria>
    - verify_delay0.py exists at project root with exactly one login() call at the top
    - 05-VERIFICATION.md exists and contains "BRAIN returned delay="
    - 05-VERIFICATION.md records the full settings_sent and returned_settings dicts (not just the delay int)
    - The verdict is recorded as "Test A PASS: delay=0 confirmed" OR "Test A COERCED: BRAIN returned delay=1"
    - Coercion verification check: grep for "[probe_delay]" in script stderr output, NOT "COERCION WARNING" (the latter is grade.py's signal, not probe_delay.py's signal)
    - If Test A PASS: "Test B skipped (Test A PASS)" is also in 05-VERIFICATION.md
  </acceptance_criteria>
  <done>
    verify_delay0.py written and run; 05-VERIFICATION.md exists with Test A result including full settings round-trip.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run Test B bisection and update probe_delay.py / delay0_candidates.py (conditional on Test A coercion)</name>
  <files>.planning/phases/05-delay-0-feasibility-plumbing/05-VERIFICATION.md, probe_delay.py, delay0_candidates.py</files>
  <read_first>
    - 05-VERIFICATION.md result from Task 1 — confirm Test A was coerced before proceeding; if not coerced, this task is a no-op
    - probe_delay.py — understand run_probe() and how settings_override works
    - delay0_candidates.py — understand CLAIMED_DELAY0_FIELDS to know what to update
    - 05-CONTEXT.md payload diff table — the three independently-testable differences
  </read_first>
  <action>
    Skip this task entirely if 05-VERIFICATION.md records "Test A PASS". Write "## Task 2 Status: skipped (Test A PASS — no coercion trigger to find)" to 05-VERIFICATION.md and stop.

    If Test A was COERCED, verify_delay0.py (from Task 1) already ran Test B and the bisection steps B1–B4. This task's job is to interpret the results and update the code artifacts based on what was found:

    If Test B found no coercion trigger (all bisection sims returned delay=0, even with our settings):
      - This means the session-level delay-0 capability was unavailable during Test A but became available during Test B, OR the expression matters more than the settings. Record this finding in 05-VERIFICATION.md's Conclusion.
      - No code changes needed to probe_delay.py or delay0_candidates.py.

    If bisection identified a single key as the trigger (e.g., maxTrade="ON" causes coercion):
      - Update probe_delay.py: add a SAFE_PROBE_SETTINGS constant that overrides the identified key(s) from _BASE_SETTINGS. Update probe_and_gate and run_probe to use SAFE_PROBE_SETTINGS as the default instead of {**grade._BASE_SETTINGS, "delay": requested_delay}. Add a comment explaining which key was identified as the coercion trigger and when (date, which test).
      - Update delay0_candidates.py CLAIMED_DELAY0_FIELDS comment to reflect: "Bisection on YYYY-MM-DD identified {key} as coercion trigger. Avoid {key}={value} in delay-0 settings."

    If bisection identified multiple keys (combination triggers):
      - Update probe_delay.py SAFE_PROBE_SETTINGS to exclude all identified keys.
      - Update delay0_candidates.py comment with findings.

    If BOTH Test A AND Test B (UI-verbatim) returned coerced:
      - This means the BRAIN session/account does not support delay-0 at all. Record in 05-VERIFICATION.md.
      - No probe_delay.py changes (the module is correctly designed; the session is the constraint).
      - delay0_candidates.py: add a comment "Session tested on {date}: delay-0 not available. Re-test on a different session or after BRAIN settings change."

    In all cases: append a "## Task 2 Outcome" section to 05-VERIFICATION.md with the final code-change decision and rationale.
  </action>
  <verify>
    <automated>grep -q "Test B\|Task 2" /Users/winter.__.kor/quant/.planning/phases/05-delay-0-feasibility-plumbing/05-VERIFICATION.md && echo "Test B/Task 2 results recorded" || echo "Test B skipped (Test A passed) or MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - 05-VERIFICATION.md contains a "## Task 2 Outcome" section with the code-change decision
    - If a coercion trigger was identified: probe_delay.py has a SAFE_PROBE_SETTINGS constant and a comment naming the trigger, dated
    - If a coercion trigger was identified: delay0_candidates.py CLAIMED_DELAY0_FIELDS comment updated with empirical finding
    - If Test B bisection could not isolate a trigger: finding is documented ("inconclusive bisection — record each step's result for future investigation")
    - Bisection was independent (each key tested alone first) — 05-VERIFICATION.md shows Test B1, B2, B3 as separate entries before any combination test
    - No "COERCION WARNING" in verification acceptance checks — the probe signal is "[probe_delay]" prefix on stderr or DelayCoercedError; grade.py's COERCION WARNING is only relevant to grade_one/grade_many tests
  </acceptance_criteria>
  <done>
    Either probe_delay.py updated with empirical coercion trigger, OR no-op with skip recorded. 05-VERIFICATION.md has complete bisection trace.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies probe verdict and phase success criteria</name>
  <what-built>
    Tasks 1-2 ran the empirical probe experiment using verify_delay0.py (single script, one login, same process). 05-VERIFICATION.md now contains the full result: whether BRAIN returns delay=0 from our code, the complete settings round-trip, and if coerced, the bisection trace identifying which setting(s) trip the coercion.
  </what-built>
  <how-to-verify>
    1. Review 05-VERIFICATION.md for the probe outcome:
       cat .planning/phases/05-delay-0-feasibility-plumbing/05-VERIFICATION.md

       Confirm it contains:
       - "BRAIN returned delay=" (Test A result)
       - Full settings_sent and returned_settings dicts for Test A
       - A Verdict line ("Test A PASS" or "Test A COERCED")
       - If coerced: Test B section with independent bisection steps (B1, B2, B3 separate)

    2. Verify the coercion-warning plumbing in grade.py (no BRAIN call needed):
       python -c "
       import grade, inspect
       src = inspect.getsource(grade.grade_one)
       assert 'COERCION WARNING' in src, 'Missing coercion warning in grade_one'
       assert 'coerced' in src, 'Missing coerced status return'
       assert 'resolved_delay_raw' in src, 'Missing None-safe normalization'
       print('grade.py coercion plumbing: OK')
       "

    3. Check --delay is wired in hunt and that dry-run is safe:
       python hunt.py --help | grep delay
       python hunt.py --delay 0 --max-sims 0
       # Expected: exits cleanly; no live probe fires (max_sims == 0 guard)

    4. Run the full test suite:
       python -m pytest test_phase4.py test_phase3.py test_phase2.py -q
       # All tests must pass.

    5. Verify probe signal wording (this is a documentation check, not a live call):
       python -c "
       import probe_delay, inspect
       src = inspect.getsource(probe_delay.run_probe)
       assert '[probe_delay]' in src, 'Missing [probe_delay] stderr prefix in run_probe'
       print('probe_delay stderr signal: OK')
       "
       # Confirms the probe emits [probe_delay] on stderr, NOT grade.py's COERCION WARNING.

    Expected outcomes:
    - If probe returned delay=0: Phase 5 feasibility goal confirmed. The system can run delay-0 alphas. probe_delay.py default settings are already correct.
    - If probe returned delay=1 and coercion trigger identified: The triggering setting is documented. probe_delay.py and delay0_candidates.py were updated. Future hunt runs will get probe-gated. Users will see DelayCoercedError instead of silently burning slots.
    - If probe returned delay=1 with no trigger isolated: Delay-0 is session-constrained. The feasibility gate (D-04) remains the protection. Phase 5's coercion infrastructure is still correct and functional.
  </how-to-verify>
  <resume-signal>Type "verified" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live BRAIN session | Single authenticated client created at verify_delay0.py start; reused across all probe sims in the script |
| Probe sim results | BRAIN's returned settings object is the source of truth; never trust the request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Denial of Service | Biometric throttle (429 BIOMETRICS_THROTTLED) | mitigate | verify_delay0.py calls login() exactly once at the top; Tasks 1 and 2 use run_probe() which never calls login(); a 401 mid-run propagates to the top level and terminates the script cleanly |
| T-05-08 | Denial of Service | Wasted ~2-min sim slots from unbounded bisection | mitigate | Independent bisection (B1, B2, B3 + optional B4) is bounded at max 5 sims: Test A + Test B + B1 + B2 + B3 (B4 only if all three independent tests pass). Stops as soon as the trigger is identified. |
| T-05-09 | Information Disclosure | 05-VERIFICATION.md contains alpha_ids and settings | accept | Local file, not committed to any public repo; contains only BRAIN-assigned opaque IDs and non-sensitive settings values |
| T-05-10 | Tampering | Persisting mislabeled delay alphas from probe sims | accept | Probe sims use run_probe() which calls _simulate_to_alpha() — this does NOT persist to alpha_kb.db. Probe sims are ephemeral and never write DB rows. No mislabeling risk from this plan. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |

No HIGH-severity threats. The plan contains one mandatory human checkpoint (Task 3 for verification). Auth is a single login() at script start; the remainder of the script uses the live client object in the same process without re-authentication.
</threat_model>

<verification>
Phase 5 is complete when 05-VERIFICATION.md exists with the probe verdict AND all three ROADMAP success criteria hold:

1. ROADMAP criterion 1: "Running a single delay-0 simulation from code and inspecting BRAIN's returned settings shows delay=0 — if BRAIN returns delay=1 instead, the discrepancy is logged as a coercion warning before the phase proceeds."
   Check: 05-VERIFICATION.md records the probe outcome including full settings round-trip. Probe coercion signal is DelayCoercedError / "[probe_delay]" stderr line, NOT grade.py's "COERCION WARNING" (which is the grade_one path, not the run_probe path).

2. ROADMAP criterion 2: "grade.py compares requested delay against BRAIN's returned delay and raises a visible warning whenever they differ — the DB always stores BRAIN's actual returned delay."
   Check: grep -c "COERCION WARNING" grade.py == 1; pytest test_phase4.py::test_grade_coercion_warning exits 0.

3. ROADMAP criterion 3: "/hunt --delay 0 passes the delay parameter end-to-end from CLI to simulate call without silent override."
   Check: python hunt.py --help shows --delay; grep -n "delay" hunt.py shows delay forwarded to build_thesis and grade_many; python hunt.py --delay 0 --max-sims 0 exits without firing a live probe.
</verification>

<success_criteria>
1. 05-VERIFICATION.md exists and contains "Test A" result with BRAIN's returned delay value AND full settings_sent and returned_settings dicts
2. If Test A was coerced: Test B section is present with independent bisection results (B1, B2, B3 each tested separately before any combination)
3. Human checkpoint (Task 3) approved — user has reviewed the probe verdict and run the regression suite
4. python -m pytest test_phase4.py -q exits 0 (all tests pass including new coercion-warning tests from Plan 01)
5. python hunt.py --delay 0 --max-sims 0 exits cleanly (no live probe, no argparse error)
6. The coercion signal verified in this plan is "[probe_delay]" on stderr or DelayCoercedError (from run_probe/probe_and_gate) — not "COERCION WARNING" (that is grade.py's grade_one signal, tested separately in test_phase4.py)
</success_criteria>

<output>
Create `.planning/phases/05-delay-0-feasibility-plumbing/05-03-SUMMARY.md` when done
</output>
