---
phase: 05-delay-0-feasibility-plumbing
plan: 01
type: execute
wave: 2
depends_on: [05-02]
# wave: 2 and depends_on: [05-02] because hunt.py imports probe_delay.
# probe_delay.py (created in Plan 05-02) must exist before hunt.py can import it.
# The two plans are NOT independently runnable despite roadmap narrative; 05-01 depends on 05-02 output.
files_modified:
  - grade.py
  - researcher.py
  - find_alphas.py
  - hunt.py
  - .claude/commands/hunt.md
  - test_phase4.py
autonomous: true
requirements:
  - DLY-01
  - DLY-02

must_haves:
  truths:
    - "grade_one/grade_many accept a delay parameter that defaults to 1 and forward it to simulate"
    - "When BRAIN returns a different delay than requested, grade_one prints a visible stderr warning naming the alpha_id and the mismatch, then returns without persisting the alpha to the DB (warn + discard per D-03)"
    - "researcher.read_catalog queries datafields with the requested delay, not hardcoded delay=1"
    - "hunt.hunt() accepts a delay parameter; python hunt.py --delay 0 passes delay=0 to grade_many"
    - "When delay != 1 AND max_sims > 0, hunt.hunt() calls probe_delay.probe_and_gate(client, conn, requested_delay=delay) BEFORE the main simulation loop; DelayCoercedError propagates to the caller unchanged"
    - "When delay == 1 OR max_sims == 0, probe_delay.probe_and_gate is NOT called (zero extra sim cost on normal hunts and dry-run invocations)"
    - "When settings is passed to grade_one, settings['delay'] takes precedence over the delay= argument; a warning is emitted if both are supplied and differ"
    - "The coercion warning+discard test passes: mock BRAIN returns delay=1 when delay=0 was requested; test asserts stderr warning and NO DB row inserted"
    - "grade_many forwards delay to grade_one; a test confirms grade_many(..., delay=0) propagates delay=0 to each grade_one call"
  artifacts:
    - path: "grade.py"
      provides: "delay param in settings builder, coercion warn+discard at resolution block, precedence warning when settings+delay differ"
      contains: "def grade_one.*delay"
    - path: "researcher.py"
      provides: "read_catalog accepts delay param; build_thesis propagates it"
      contains: "def read_catalog.*delay"
    - path: "hunt.py"
      provides: "--delay CLI flag; hunt() function accepts delay param; probe_and_gate called before main loop when delay != 1 and max_sims > 0"
      contains: "add_argument.*--delay"
    - path: "test_phase4.py"
      provides: "coercion warn+discard regression tests, grade_many delay-forwarding test"
      contains: "test_grade_coercion_warning"
  key_links:
    - from: "hunt.py CLI args"
      to: "hunt.hunt(delay=)"
      via: "args.delay forwarded to hunt()"
    - from: "hunt.hunt()"
      to: "probe_delay.probe_and_gate(client, conn, requested_delay=delay)"
      via: "called before main simulation loop only when delay != 1 AND max_sims > 0; DelayCoercedError propagates"
    - from: "hunt.hunt()"
      to: "grade.grade_many(delay=)"
      via: "delay forwarded through hunt to grade_many"
    - from: "grade.py resolution block (Step 5)"
      to: "early return without DB write"
      via: "requested_delay != resolved_delay triggers warn+discard before db.upsert_alpha"
---

<objective>
Thread a `delay` parameter end-to-end from the CLI through hunt.py → grade.py → simulate, replacing every hardcoded `delay=1` with a parameter that defaults to 1. Add the D-03 coercion response at the BRAIN response resolution point in grade.py: when BRAIN returns a different delay than requested, print a loud stderr warning and return WITHOUT persisting the alpha to the DB (warn + discard). Wire `probe_delay.probe_and_gate` (created in Plan 05-02) into `hunt.hunt()` as the D-04 fail-fast guard that fires before the main simulation loop whenever `delay != 1` AND at least one simulation will be run.

Purpose: DLY-01 (delay-0 requestable via --delay 0) and DLY-02 (coercion detected and surfaced; mislabeled alpha never persisted — this is what prevented the "11 mislabeled rows" mess from recurring). The probe-gate (D-04) prevents burning ~2-minute sim slots when BRAIN silently coerces delay-0 to delay-1 at the session level.

Output: Parameterized delay in grade.py, researcher.py, find_alphas.py, hunt.py; probe_and_gate wired into hunt() for delay != 1 when max_sims > 0; updated hunt.md command docs; regression tests for coercion warning+discard and grade_many delay forwarding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-delay-0-feasibility-plumbing/05-CONTEXT.md

<!-- Key interfaces extracted from codebase for executor use -->
<interfaces>
From grade.py:44-59 — _BASE_SETTINGS dict (hardcoded delay=1 at line 48):
  Keys: instrumentType, region, universe, delay, decay, neutralization,
        truncation, maxTrade, pasteurization, testPeriod, unitHandling,
        nanHandling, language, visualization

From grade.py:62 — _simulate_to_alpha(client, expression, settings=None, attempts=3)
  active_settings = settings if settings is not None else _BASE_SETTINGS

From grade.py:96-103 — grade_one signature:
  grade_one(client, conn, expression, run_id, parent_alpha_id=None, settings=None)
  Already accepts settings=. The plan adds delay as an explicit param that
  builds the settings dict to pass down, instead of callers mutating _BASE_SETTINGS.

  PRECEDENCE RULE (new): when settings is not None, settings["delay"] wins over
  the delay= argument. If the caller supplies BOTH settings (with settings["delay"])
  AND a conflicting delay= value, grade_one must emit a warning to stderr:
    "[grade] WARNING: delay={delay} argument ignored because settings['delay']={settings['delay']} was also supplied"
  This prevents silent surprise for callers who pass both.

From grade.py:186-204 — BRAIN-actual resolution block (Step 5):
  active_settings = settings if settings is not None else _BASE_SETTINGS
  brain_settings = alpha.get("settings") or {}
  resolved_delay = brain_settings.get("delay", active_settings.get("delay"))
  [D-03 coercion response belongs here:
    Normalize BEFORE comparing:
      requested_delay_int = int(active_settings.get("delay", 1))
      resolved_delay_raw = brain_settings.get("delay")
      resolved_delay_int = int(resolved_delay_raw) if resolved_delay_raw is not None else requested_delay_int
    if requested_delay_int != resolved_delay_int:
      print warning to stderr (with alpha_id + both delay values)
      return {"expression": expression, "status": "coerced", "alpha_id": alpha_id}
    (early return — do NOT call db.upsert_alpha; no DB row written)]

From grade.py:207-238 — after Step 5: db.upsert_alpha(conn, alpha_dict) at line 238
  This is what the early return must skip.

From researcher.py:83-110 — read_catalog(conn):
  Hardcodes delay=1 at line 103 in the datafields SQL WHERE clause.
  Signature needs: read_catalog(conn, delay: int = 1)

From researcher.py:252-256 — build_thesis(conn, archetype=None, avoid_motifs=None):
  Returns dict with hardcoded "delay": 1 at line 332.
  Needs: delay: int = 1 param, pass to read_catalog, emit in returned dict.

From find_alphas.py:361-365 — find_alphas(db_path, archetype=None, prose=None):
  Calls researcher.build_thesis(conn, archetype=archetype, avoid_motifs=avoid_motifs).
  Needs: delay: int = 1 param, forwarded to build_thesis.

From hunt.py:111-116 — hunt(client, db_path, max_depth, max_sims):
  Calls researcher.build_thesis(conn, avoid_motifs=avoid_motifs).
  Calls grade.grade_many(client, conn, queue, run_id, max_workers=3, db_path=db_path).
  Needs: delay: int = 1 param, forwarded to build_thesis and grade_many.

PROBE SKIP CONDITION (new): probe_and_gate fires ONLY when delay != 1 AND max_sims > 0.
  When max_sims == 0 (dry run / argparse smoke test), skip the probe entirely.
  hunt.py --delay 0 --max-sims 0 must exit cleanly without firing any live API call.

From hunt.py:379-400 — argparse CLI:
  Current flags: --db, --max-depth, --max-sims
  Needs: --delay (type=int, default=1)

From probe_delay.py (created in Plan 05-02) — probe_and_gate signature:
  def probe_and_gate(client, conn, requested_delay: int = 0) -> ProbeResult
  Raises: DelayCoercedError(RuntimeError) if BRAIN returns a different delay than requested.
  The function receives a pre-authenticated client and fires ONE probe simulation.
  Never call login() inside; 401 propagates to caller.
  Import: import probe_delay

From test_phase4.py:498-591 — test_grade_records_brain_actual_settings():
  Existing test: passes settings={"delay": 0, ...}, mock BRAIN returns delay=1,
  asserts DB column and settings_json["delay"] == 1.
  NOTE: after this plan's changes, that test's expected behavior changes — when
  delay is coerced, grade_one returns early WITHOUT writing a DB row. The existing
  test must be updated to reflect the new discard behavior (or the mock must show
  delay match to keep the recording test valid). See Task 3.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add delay param to grade.py with coercion warn+discard and precedence rule</name>
  <files>grade.py</files>
  <read_first>
    - grade.py full file — understand _BASE_SETTINGS (lines 44-59), grade_one signature (lines 96-103), Step 5 BRAIN-actual resolution block (lines 186-204), and db.upsert_alpha call (line 238)
    - .planning/phases/05-delay-0-feasibility-plumbing/05-CONTEXT.md — D-03 (warn + discard: do NOT persist the coerced alpha), D-02 (confirm settings reach BRAIN — coercion means they didn't work)
    - CLAUDE.md — simulate() regular-param trap: NEVER pass regular= keyword; never re-auth in-loop
  </read_first>
  <behavior>
    - grade_one(client, conn, expression, run_id, parent_alpha_id=None, settings=None, delay: int = 1) — new delay param defaults to 1
    - PRECEDENCE: when settings is not None, settings["delay"] wins over delay=. If both are supplied AND settings["delay"] != delay, emit to stderr: "[grade] WARNING: delay={delay} argument ignored because settings['delay']={settings['delay']} was also supplied". Then continue using settings as-is.
    - When settings is None: build active_settings = {**_BASE_SETTINGS, "delay": delay}. When settings is not None: use it as-is (backward compat for optimizer.py callers that pass full settings dicts).
    - grade_many(client, conn, expressions, run_id, ..., delay: int = 1) — passes delay down to each grade_one call.
    - At Step 5, after computing resolved_delay — NORMALIZE DEFENSIVELY before comparing:
        requested_delay_int = int(active_settings.get("delay", 1))
        resolved_delay_raw = brain_settings.get("delay")
        resolved_delay_int = int(resolved_delay_raw) if resolved_delay_raw is not None else requested_delay_int
        if requested_delay_int != resolved_delay_int:
          print to sys.stderr: "[grade] COERCION WARNING: alpha_id={alpha_id} requested delay={requested_delay_int} but BRAIN returned delay={resolved_delay_int} — discarding (not persisted)"
          return {"expression": expression, "status": "coerced", "alpha_id": alpha_id}
      The return happens BEFORE db.upsert_alpha — no DB row written for coerced alphas.
    - When delay matches: normal flow continues (existing behavior unchanged).
    - Test A: mock BRAIN returns delay=1 when grade_one called with delay=0; assert stderr contains "COERCION WARNING", assert NO row in DB for that alpha_id, assert return dict has status="coerced"
    - Test B: mock BRAIN returns delay=0 when grade_one called with delay=0; assert NO coercion warning, assert DB row IS written
    - Test C: existing test_grade_records_brain_actual_settings must be updated — it previously assumed a coerced alpha gets recorded; after this plan it gets discarded. Update the mock to return delay=1 when requested delay is also 1 (no coercion) so the recording assertion still passes for the matched case.
    - Test D (new): grade_many(..., delay=0) must forward delay=0 to each grade_one call. Mock grade_one, call grade_many with delay=0, assert every grade_one call received delay=0.
  </behavior>
  <action>
    In grade.py: add delay: int = 1 to grade_one() signature. When settings is None, build active_settings as {**_BASE_SETTINGS, "delay": delay}. When settings is not None, keep existing assignment, but first check: if delay != 1 and settings.get("delay") != delay, print the precedence warning to stderr.

    Add delay: int = 1 to grade_many() and forward it to each grade_one(delay=delay) call.

    In the Step 5 BRAIN-actual resolution block, replace the existing resolved_delay computation with the defensive normalization described in the behavior block above. Insert the if-branch that prints the COERCION WARNING to sys.stderr and returns the coerced dict immediately, before db.upsert_alpha and before db.upsert_checks.

    When there is no coercion (delays match), execution continues as today — settings_json is built, alpha_dict is assembled, db.upsert_alpha is called.

    Do NOT pass regular= to client.simulate() — only simulate(expression, settings=active_settings).
    Do NOT change the existing Step 5 resolved_delay structure beyond adding the normalization and the conditional return.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -m pytest test_phase4.py::test_grade_coercion_warning test_phase4.py::test_grade_no_coercion_when_delay_matches test_phase4.py::test_grade_many_forwards_delay -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "def grade_one" grade.py shows "delay: int = 1" in the signature
    - grep -n "def grade_many" grade.py shows "delay: int = 1" in the signature
    - grep -n "COERCION WARNING" grade.py shows the stderr print statement with "discarding"
    - grep -n "status.*coerced" grade.py shows the early return dict before db.upsert_alpha
    - grep -n "resolved_delay_raw" grade.py shows the None-safe normalization (int(x) is only called when x is not None)
    - grep -n "settings\[.delay.\].*ignored" grade.py shows the precedence warning
    - pytest test_phase4.py::test_grade_coercion_warning exits 0
    - pytest test_phase4.py::test_grade_no_coercion_when_delay_matches exits 0
    - pytest test_phase4.py::test_grade_many_forwards_delay exits 0
  </acceptance_criteria>
  <done>
    grade.py has delay param in grade_one/grade_many with coercion warn+discard, defensive None normalization, and precedence warning. All three new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Thread --delay through researcher.py, find_alphas.py, hunt.py, and hunt.md; wire probe_and_gate into hunt() with max_sims guard</name>
  <files>researcher.py, find_alphas.py, hunt.py, .claude/commands/hunt.md</files>
  <read_first>
    - researcher.py lines 83-110 (read_catalog — hardcoded delay=1 at line 103) and lines 252-333 (build_thesis — hardcoded "delay": 1 at line 332)
    - find_alphas.py lines 361-462 (find_alphas function — calls build_thesis)
    - hunt.py lines 111-200 (hunt() function — calls build_thesis and grade_many; also check where max_sims is used to determine the guard condition) and lines 379-430 (argparse CLI)
    - .claude/commands/hunt.md (flags table and hunt() invocation block)
    - .planning/phases/05-delay-0-feasibility-plumbing/05-02-PLAN.md — probe_and_gate signature: probe_and_gate(client, conn, requested_delay: int = 0); raises DelayCoercedError(RuntimeError)
    - 05-CONTEXT.md Claude's Discretion — delay-1 is default; delay-0 is explicit opt-in; researcher queries correct delay slice from datafields
    - 05-CONTEXT.md D-04 — probe fires once before the main loop when delay != 1; DelayCoercedError must propagate to caller (never swallowed)
  </read_first>
  <action>
    In researcher.py:
    - Add delay: int = 1 param to read_catalog(conn, delay: int = 1). Change the datafields SQL WHERE clause at line 103 from "AND delay=1" to "AND delay=?" and pass delay as a positional bind value in the tuple.
    - Add delay: int = 1 param to build_thesis(conn, archetype=None, avoid_motifs=None, delay: int = 1). Call read_catalog(conn, delay=delay). Change the returned dict's hardcoded "delay": 1 at line 332 to "delay": delay.

    In find_alphas.py:
    - Add delay: int = 1 param to find_alphas(db_path, archetype=None, prose=None, delay: int = 1).
    - Forward delay=delay to the researcher.build_thesis() call.

    In hunt.py:
    - Add "import probe_delay" at the top of hunt.py (with other project-local imports).
    - Add delay: int = 1 param to hunt(client, db_path, max_depth, max_sims, delay: int = 1).
    - Inside hunt.hunt(), BEFORE the main simulation loop begins, add the probe guard:
        if delay != 1 and max_sims > 0:
            probe_delay.probe_and_gate(client, conn, requested_delay=delay)
      The guard condition is delay != 1 AND max_sims > 0. When max_sims == 0, skip the probe entirely — this prevents hunt.py --delay 0 --max-sims 0 from burning a live sim slot during an argparse smoke test or dry run.
      Do NOT wrap this in try/except — let DelayCoercedError propagate out of hunt() to the caller so the CLI prints the error and exits without burning any further sim slots.
    - Forward delay=delay to researcher.build_thesis() call.
    - Forward delay=delay to all grade.grade_many() calls.
    - In the argparse section, add: parser.add_argument("--delay", type=int, default=1, help="Simulation delay in days (0 or 1; default: 1). Use --delay 0 for delay-0 alphas.")
    - Forward args.delay to hunt(delay=args.delay).

    In .claude/commands/hunt.md:
    - Add --delay to the Flags table: default=1, description "Simulation delay. Use --delay 0 for delay-0 alphas (explicit opt-in; default is delay-1). Fires a one-shot probe before the hunt loop when delay != 1 and max-sims > 0."
    - Update the hunt() invocation example to include delay=1 as a parameter.
    - Add a one-line note: "delay-0 requires --delay 0; omitting --delay defaults to delay-1. A probe sim fires first to confirm BRAIN supports delay-0 on this session (fails fast if coerced). No probe fires when --max-sims 0 is used (dry run)."

    Default delay=1 everywhere. Never change any existing call site to delay=0. Never swallow DelayCoercedError.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "import researcher, inspect; sig = inspect.signature(researcher.read_catalog); assert 'delay' in sig.parameters; sig2 = inspect.signature(researcher.build_thesis); assert 'delay' in sig2.parameters; print('researcher OK')" && python -c "import hunt, inspect; sig = inspect.signature(hunt.hunt); assert 'delay' in sig.parameters; print('hunt OK')" && python hunt.py --help 2>&1 | grep -q '\-\-delay' && echo "hunt --delay flag present" || echo "MISSING --delay flag" && python -c "import hunt, inspect; src = inspect.getsource(hunt.hunt); assert 'probe_delay.probe_and_gate' in src; assert 'delay != 1' in src; assert 'max_sims' in src; print('probe_and_gate wired with max_sims guard OK')" && grep -q 'import probe_delay' hunt.py && echo "probe_delay imported OK" || echo "MISSING probe_delay import"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "def read_catalog" researcher.py shows "delay: int = 1" in signature
    - grep -n "def build_thesis" researcher.py shows "delay: int = 1" in signature
    - grep -n "def hunt" hunt.py shows "delay: int = 1" in signature
    - grep -n "import probe_delay" hunt.py exits 0 (import present)
    - grep -n "probe_delay.probe_and_gate" hunt.py exits 0 (call present inside hunt())
    - grep -n "max_sims" hunt.py shows the guard condition "delay != 1 and max_sims > 0" (or equivalent) immediately before the probe call
    - python hunt.py --help output includes "--delay"
    - python hunt.py --delay 0 --max-sims 0 exits cleanly (argparse smoke test with no live API call)
    - grep "delay" .claude/commands/hunt.md shows the flag documented with "max-sims 0" dry-run note
    - python -m pytest test_phase2.py test_phase3.py test_phase4.py -q exits 0 (no regressions)
  </acceptance_criteria>
  <done>
    delay threaded through all four files; probe wired with dual guard (delay != 1 AND max_sims > 0); hunt.md updated; no regressions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add coercion regression tests and grade_many forwarding test to test_phase4.py</name>
  <files>test_phase4.py</files>
  <read_first>
    - test_phase4.py lines 495-591 (existing test_grade_records_brain_actual_settings — this test must be updated: it tested coerced recording, but after Task 1, coerced alphas are discarded, not recorded; update it to test the matched-delay recording case instead)
    - grade.py after Task 1 changes — understand the exact WARNING format string and the "coerced" status return; understand the None-safe normalization path
  </read_first>
  <behavior>
    Update test_grade_records_brain_actual_settings: change the test to use a mock where requested delay = 1 and BRAIN also returns delay = 1 (no coercion). The test still asserts DB row shows delay=1 from BRAIN's response, but now uses a scenario where the alpha is NOT discarded. This preserves the test's intent (BRAIN's returned value is stored) while matching the new behavior.

    Add test_grade_coercion_warning:
    - grade_one called with delay=0 (settings=None path)
    - Mock BRAIN response carries "settings": {"delay": 1, ...} — coercion scenario
    - Use contextlib.redirect_stderr + io.StringIO to capture sys.stderr
    - Assert captured stderr contains "COERCION WARNING" AND "TEST_COERCE_WARN"
    - Assert no DB row exists for alpha_id "TEST_COERCE_WARN" (discard was applied)
    - Assert return dict has status="coerced"

    Add test_grade_no_coercion_when_delay_matches:
    - grade_one called with delay=0 (settings=None path)
    - Mock BRAIN response carries "settings": {"delay": 0, ...} — no coercion
    - Assert captured stderr does NOT contain "COERCION WARNING"
    - Assert DB row IS written for that alpha_id

    Add test_grade_many_forwards_delay:
    - Call grade_many with a list of two expressions and delay=0
    - Mock grade_one (patch grade.grade_one)
    - Assert every call to the mocked grade_one received keyword delay=0
    - No real BRAIN sims in this test

    Add test_grade_coercion_with_none_returned_delay:
    - grade_one called with delay=0
    - Mock BRAIN response has "settings": {} (delay key absent, returns None from .get("delay"))
    - Assert the None-safe normalization does not raise TypeError — falls back cleanly
    - This test exercises the "resolved_delay_raw is None → use requested_delay_int" branch

    All tests must pass without any BRAIN API calls.
  </behavior>
  <action>
    In test_phase4.py, update test_grade_records_brain_actual_settings to use a no-coercion scenario (requested delay=1, BRAIN returns delay=1) — the test intent (BRAIN's value is recorded) is preserved; only the mock's delay values change to avoid triggering the new discard logic.

    Add test_grade_coercion_warning after the existing test. Use the same mock structure (MagicMock client + sim, patch validate.validate, selfcorr.proxy_gate, selfcorr.fetch_and_cache_pnl, grade.trigger_correlation_check, grade.poll_correlation). Set mock alpha_id = "TEST_COERCE_WARN", mock BRAIN response settings delay=1. Call grade_one with delay=0 using the new delay= param (not settings=). Capture stderr via contextlib.redirect_stderr. Assert stderr contains "COERCION WARNING" and "TEST_COERCE_WARN". Assert db has no row for "TEST_COERCE_WARN". Assert result["status"] == "coerced".

    Add test_grade_no_coercion_when_delay_matches. Set mock alpha_id = "TEST_NO_COERCE", mock BRAIN response settings delay=0. Call grade_one with delay=0. Assert stderr does NOT contain "COERCION WARNING". Assert db HAS a row for "TEST_NO_COERCE".

    Add test_grade_many_forwards_delay. Patch grade.grade_one with a mock that returns a fake success dict. Call grade.grade_many(client, conn, ["rank(vwap)", "rank(volume)"], run_id="r1", delay=0). Assert mock_grade_one.call_args_list shows every call had delay=0 as a keyword argument.

    Add test_grade_coercion_with_none_returned_delay. Mock BRAIN response settings={} (no "delay" key). Call grade_one with delay=0. Assert no TypeError raised, assert return dict has status="coerced" (since None-normalized delay defaults to requested_delay_int=0, which matches, so verify the exact branch behavior: if None falls back to requested delay, no coercion fires and the alpha IS persisted — document this edge case explicitly in the test).

    All five tests must pass without any BRAIN API calls.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -m pytest test_phase4.py::test_grade_records_brain_actual_settings test_phase4.py::test_grade_coercion_warning test_phase4.py::test_grade_no_coercion_when_delay_matches test_phase4.py::test_grade_many_forwards_delay test_phase4.py::test_grade_coercion_with_none_returned_delay -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All five tests exit 0
    - python -m pytest test_phase4.py -q exits 0 (full suite including updated existing test)
    - The updated test_grade_records_brain_actual_settings no longer tests a coercion scenario (it tests a matched-delay recording scenario)
    - test_grade_many_forwards_delay confirms every grade_one call received delay=0
    - test_grade_coercion_with_none_returned_delay documents the None-fallback behavior without raising TypeError
  </acceptance_criteria>
  <done>
    Five tests pass; full test_phase4.py suite clean; coercion warn+discard, grade_many forwarding, and None-safety are all regression-covered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grade.py → BRAIN API | Settings dict sent; returned settings object is source of truth |
| CLI args → hunt() | --delay int from argparse; constrained to type=int |
| hunt.py → probe_delay.probe_and_gate | Passes caller's authenticated client; probe fires one sim before main loop only when delay != 1 and max_sims > 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | grade._BASE_SETTINGS mutation | mitigate | Build active_settings as {**_BASE_SETTINGS, "delay": delay} copy in grade_one when settings=None; never mutate the module-level dict (eliminates the run_delay0.py mutation-and-restore pattern from the main grading path) |
| T-05-02 | Information Disclosure | Coercion warning names alpha_id | accept | Warning printed to stderr with alpha_id and delay values is intentional; alpha_id is a BRAIN-assigned opaque ID with no PII; stderr is local only |
| T-05-03 | Denial of Service | Repeated biometric auth | mitigate | No auth call added in this plan; hunt.py auth pattern unchanged; 401 propagates and stops cleanly; probe_and_gate receives pre-authenticated client |
| T-05-04 | Tampering | Mislabeled alpha persisted (the "11 rows" failure mode) | mitigate | The discard return in grade_one prevents any coerced alpha from reaching db.upsert_alpha — re-creating the mislabeling mess is now structurally impossible |
| T-05-07 | Denial of Service | Burning sim slots on a session where delay-0 is being coerced | mitigate | probe_and_gate fires BEFORE the main hunt loop when delay != 1 and max_sims > 0; DelayCoercedError propagates out of hunt() without entering the loop — zero additional slots burned. When max_sims == 0, no probe fires — argparse smoke tests are safe |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; all changes are pure Python in existing modules |

No HIGH-severity threats exist in this plan. The discard logic (T-05-04 mitigation) and probe-gate (T-05-07 mitigation) are the key protective improvements of this plan.
</threat_model>

<verification>
Full regression check after all three tasks:

```bash
cd /Users/winter.__.kor/quant
python -m pytest test_phase4.py -q                    # all tests pass (updated + new)
python -m pytest test_phase2.py test_phase3.py -q    # no regressions
python hunt.py --help | grep -q "\-\-delay" && echo "OK" || echo "FAIL"
python hunt.py --delay 0 --max-sims 0 && echo "dry-run OK" || echo "FAIL dry-run"
python -c "import researcher, inspect; print('delay' in str(inspect.signature(researcher.read_catalog)))"
python -c "import grade; import inspect; src = inspect.getsource(grade.grade_one); assert 'COERCION WARNING' in src and 'coerced' in src; print('warn+discard present')"
python -c "import grade; import inspect; src = inspect.getsource(grade.grade_one); assert 'resolved_delay_raw' in src; print('None-safe normalization present')"
python -c "import hunt, inspect; src = inspect.getsource(hunt.hunt); assert 'probe_delay.probe_and_gate' in src and 'delay != 1' in src and 'max_sims' in src; print('probe_and_gate wired with max_sims guard OK')"
grep -n 'import probe_delay' hunt.py
```
</verification>

<success_criteria>
1. python hunt.py --delay 0 is a valid invocation (no argparse error)
2. python hunt.py --delay 0 --max-sims 0 exits cleanly without firing any live probe (argparse smoke test safe)
3. python -m pytest test_phase4.py -q exits 0 — all tests pass including updated test_grade_records_brain_actual_settings and new test_grade_coercion_warning, test_grade_no_coercion_when_delay_matches, test_grade_many_forwards_delay, test_grade_coercion_with_none_returned_delay
4. grep -n "delay: int = 1" grade.py researcher.py hunt.py find_alphas.py — all four files show the parameterized default
5. grep -n "COERCION WARNING" grade.py — shows exactly one match with "discarding" in the same print line
6. grep -n "status.*coerced" grade.py — shows the early return dict before db.upsert_alpha
7. grep -n "resolved_delay_raw" grade.py — shows the None-safe normalization (int() only called when not None)
8. grep -n "settings\[.delay.\].*ignored" grade.py — shows the precedence warning when settings and delay= conflict
9. grep -v "^#" researcher.py | grep "delay=1" — no hardcoded delay=1 in SQL or return dict (only in function signatures where 1 is the default value)
10. grep -n "probe_delay.probe_and_gate" hunt.py — shows exactly one call inside hunt() with requested_delay=delay
11. grep -n "max_sims" hunt.py — shows the guard condition (delay != 1 and max_sims > 0) immediately before the probe_and_gate call
12. grep -n "import probe_delay" hunt.py — import is present
</success_criteria>

<output>
Create `.planning/phases/05-delay-0-feasibility-plumbing/05-01-SUMMARY.md` when done
</output>
