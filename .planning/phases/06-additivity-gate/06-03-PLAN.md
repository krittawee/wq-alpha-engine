---
phase: 06-additivity-gate
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - hunt.py
  - test_phase4.py
autonomous: true
requirements:
  - ADD-03
  - ADD-04

must_haves:
  truths:
    - "No alpha is labeled best_submittable in hunt() unless confirm_additive returns additive=True — ADD-03 enforced at both best_submittable assignment sites (line 257 and line 351)"
    - "When all_pass_ids is empty, the gate block is skipped and best_submittable remains None (no error)"
    - "When all proxy survivors have additive=False or additive=None, best_submittable is None even if there are IS-passing alphas"
    - "The gate calls confirm_additive sequentially on at most CONFIRM_LIMIT finalists; it never runs inside the grade_many thread pool"
    - "401 from confirm_additive propagates up through hunt() to the CLI layer — never swallowed"
    - "Two integration tests pass: test_phase6_plan3_gate_blocks_nonaddititve and test_phase6_plan3_gate_passes_additive"
  artifacts:
    - path: "hunt.py"
      provides: "Additivity gate block inserted before both best_submittable assignments; CONFIRM_LIMIT used from additivity module"
      contains: "additivity.rank_by_proxy"
    - path: "test_phase4.py"
      provides: "Two integration tests for hunt gate — mocked BRAIN, in-memory DB, confirms gate withholds/allows best_submittable"
      contains: "phase6_plan3"
  key_links:
    - from: "hunt.py gate block"
      to: "additivity.rank_by_proxy"
      via: "ranked = additivity.rank_by_proxy(pass_candidates, conn)"
      pattern: "additivity\\.rank_by_proxy"
    - from: "hunt.py gate block"
      to: "additivity.confirm_additive"
      via: "result = additivity.confirm_additive(client, r.alpha_id, conn)"
      pattern: "additivity\\.confirm_additive"
    - from: "hunt.py"
      to: "additivity.CONFIRM_LIMIT"
      via: "proxy_survivors[:additivity.CONFIRM_LIMIT]"
      pattern: "additivity\\.CONFIRM_LIMIT"
---

<objective>
Wire the additivity gate into hunt.py at both best_submittable assignment sites so no alpha reaches submit-ready status on IS checks alone.

Purpose: ADD-03 requires the gate on ALL submit-recommendation paths. hunt.py has exactly two: line 257 (inside the editor loop, after each generation's results are classified) and line 351 (final pass after the last generation). Both must route through additivity.rank_by_proxy → soft pre-filter → confirm_additive on top-N finalists → _rank_best on confirmed-additive IDs only.

Output: hunt.py with a gate helper function (or inline block extracted to a shared helper) applied at both sites; two integration tests in test_phase4.py.
</objective>

<execution_context>
@/Users/winter.__.kor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/winter.__.kor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/PROJECT.md
@/Users/winter.__.kor/quant/.planning/ROADMAP.md
@/Users/winter.__.kor/quant/.planning/phases/06-additivity-gate/06-CONTEXT.md
@/Users/winter.__.kor/quant/.planning/phases/06-additivity-gate/06-RESEARCH.md
@/Users/winter.__.kor/quant/.planning/phases/06-additivity-gate/06-01-SUMMARY.md
@/Users/winter.__.kor/quant/.planning/phases/06-additivity-gate/06-02-SUMMARY.md

<interfaces>
From additivity.py (Plan 2):

CONFIRM_LIMIT: int = 3   # max finalists to confirm per run

AdditivityResult dataclass fields relevant to hunt.py:
  alpha_id: str
  combined_corr: Optional[float]
  proxy_drop: bool          # True = hard-dropped by pre-filter
  skipped: bool             # True = no PnL available
  additive: Optional[bool]  # True = BRAIN confirmed additive; None = inconclusive

rank_by_proxy(candidates: list, conn, margin=PROXY_MARGIN) -> list[AdditivityResult]
  candidates: [{"alpha_id": str, "pnl_path": Optional[str]}, ...]
  Returns sorted ascending by combined_corr (most additive first); skipped appended last.

confirm_additive(client, alpha_id: str, conn, timeout=300, interval=15) -> AdditivityResult
  Makes ONE BRAIN GET /check call. 401 propagates. TimeoutError → additive=None.

From hunt.py (verified lines 46-391):

_rank_best(alpha_ids: list, conn) -> Optional[str]
  Selects highest-Sharpe alpha from alpha_ids where status='pass'.

Two best_submittable sites:
  Line 257: best_submittable = _rank_best(all_pass_ids, conn)
            Context: inside for gen in range(max_depth): loop, after pass_ids.extend
  Line 351: best_submittable = _rank_best(all_pass_ids, conn)
            Context: final pass block after the loop, after last-gen results classified

hunt() imports at top of file: grade, editor, researcher, ideator, fsa, selfcorr, probe_delay, db

DB query to get pnl_path for an alpha_id:
  conn.execute("SELECT pnl_path FROM alphas WHERE alpha_id=?", (alpha_id,)).fetchone()
  Returns (pnl_path_str,) or None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additivity gate to hunt.py at both best_submittable sites</name>
  <files>hunt.py</files>
  <read_first>hunt.py (full file — read before editing, especially lines 230-360 spanning both assignment sites and the final pass block); additivity.py (confirm CONFIRM_LIMIT name and rank_by_proxy/confirm_additive signatures)</read_first>
  <behavior>
    - Add "import additivity" to hunt.py imports at the top of the file (alongside existing import grade, import selfcorr, etc.)
    - Extract a private helper _apply_additivity_gate(client, all_pass_ids: list, conn) -> Optional[str]:
        1. If not all_pass_ids: return None immediately (guard against empty list).
        2. Build pass_candidates: for each aid in all_pass_ids, query pnl_path from DB; build {"alpha_id": aid, "pnl_path": row[0] if row else None}.
        3. ranked = additivity.rank_by_proxy(pass_candidates, conn).
        4. proxy_survivors = [r for r in ranked if not r.proxy_drop].
        5. confirmed_ids = [].
        6. For r in proxy_survivors[:additivity.CONFIRM_LIMIT]: call result = additivity.confirm_additive(client, r.alpha_id, conn). If result.additive is True: confirmed_ids.append(r.alpha_id). 401 from confirm_additive propagates (not caught here).
        7. If not confirmed_ids: print("[hunt] additivity gate: no confirmed-additive candidates — best_submittable=None"); return None.
        8. return _rank_best(confirmed_ids, conn).
    - Replace BOTH occurrences of "best_submittable = _rank_best(all_pass_ids, conn)" with "best_submittable = _apply_additivity_gate(client, all_pass_ids, conn)".
    - Add a print line before each confirm_additive call: print(f"[hunt] additivity confirm: checking {r.alpha_id} (combined_corr={r.combined_corr:.3f})") — only when r.combined_corr is not None, else print without the corr value.
    - DO NOT modify _rank_best, the sim pool, grade_many, or any other logic.
  </behavior>
  <action>
    Read hunt.py fully before editing. Add import additivity at the top with the other imports (alphabetical order with existing imports is fine; do not displace existing lines).

    Place _apply_additivity_gate immediately before _rank_best (around line 46) or immediately after it — keep private helpers grouped. The function signature: def _apply_additivity_gate(client, all_pass_ids: list, conn: sqlite3.Connection) -> Optional[str].

    When replacing the two best_submittable lines, ensure the replacement is syntactically correct in context (both are simple assignment statements; the replacement is also a simple assignment). Use Edit tool to make targeted replacements — do not rewrite the whole file.

    The 401 propagation path: confirm_additive → grade.trigger_correlation_check → r.raise_for_status() → HTTPError bubbles up through _apply_additivity_gate → through the hunt() try block → to the CLI except handler at hunt.py:447. No try/except should be added inside _apply_additivity_gate for HTTPError.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && grep -n "additivity" hunt.py && grep -c "_apply_additivity_gate" hunt.py</automated>
  </verify>
  <done>
    - "import additivity" appears in hunt.py imports
    - _apply_additivity_gate is defined
    - grep -c "_apply_additivity_gate" returns at least 3 (1 def + 2 call sites)
    - grep -c "_rank_best(all_pass_ids" returns 0 (old pattern replaced at both sites)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for the hunt additivity gate</name>
  <files>test_phase4.py</files>
  <read_first>test_phase4.py (last 100 lines — append after Phase 6 Plan 2 section); hunt.py (updated Task 1 version, _apply_additivity_gate signature); additivity.py (AdditivityResult fields)</read_first>
  <behavior>
    Append a Phase 6 Plan 3 section to test_phase4.py. Two integration tests — both patch BRAIN entirely (zero real API calls). Use db.init_db(':memory:').

    test_phase6_plan3_gate_blocks_nonaddititve:
      Setup: insert one alpha in conn with status='pass', sharpe=1.5, pnl_path pointing to a valid PnL file in tmp_path.
      Patch: additivity.rank_by_proxy to return [AdditivityResult(alpha_id=aid, pnl_path=pnl_path, combined_corr=0.85, max_pairwise_corr=0.9, proxy_drop=True, skipped=False, additive=None)].
      Patch: additivity.confirm_additive to return AdditivityResult(..., additive=False) (should NOT be called for proxy_drop=True candidates anyway, but provide a return value defensively).
      Call _apply_additivity_gate(mock_client, [aid], conn) — import it from hunt via "from hunt import _apply_additivity_gate".
      Assert result is None (gate withheld submit-ready because proxy_drop=True → no survivors → no confirms).
      Assert confirm_additive was NOT called (proxy_drop filtered it out).

    test_phase6_plan3_gate_passes_additive:
      Setup: insert one alpha in conn with status='pass', sharpe=1.8, pnl_path pointing to a valid PnL file in tmp_path.
      Patch: additivity.rank_by_proxy to return [AdditivityResult(alpha_id=aid, pnl_path=pnl_path, combined_corr=0.15, max_pairwise_corr=0.20, proxy_drop=False, skipped=False)].
      Patch: additivity.confirm_additive to return AdditivityResult(alpha_id=aid, pnl_path=pnl_path, combined_corr=None, max_pairwise_corr=None, proxy_drop=False, skipped=False, additive=True).
      Call _apply_additivity_gate(mock_client, [aid], conn).
      Assert result == aid (gate passed the alpha through).
      Assert confirm_additive was called exactly once with the correct alpha_id.
  </behavior>
  <action>
    Append the Phase 6 Plan 3 section after the Phase 6 Plan 2 section in test_phase4.py.

    Import _apply_additivity_gate from hunt at the section header: "from hunt import _apply_additivity_gate". If hunt imports break (e.g. heavy side effects at import time), use importlib or unittest.mock.patch at the module level to isolate.

    For patching rank_by_proxy and confirm_additive: patch "additivity.rank_by_proxy" and "additivity.confirm_additive" at the additivity module level (since hunt.py calls additivity.rank_by_proxy, the patch must be on the additivity module's attribute, not on a hunt-local reference).

    Use unittest.mock.patch as context manager (with patch(...) as mock_fn:). Verify call counts with mock_fn.call_count and assert_called_once_with.

    For tmp_path PnL files: write a minimal valid PnL JSON: {"dates": ["2022-01-03", "2022-01-04"], "pnls": [0.0, 100.0]}. The pnl_path column in the DB must be the str path of this file.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && ./venv/bin/python -m pytest test_phase4.py -k "phase6_plan3" --tb=short -x -q</automated>
  </verify>
  <done>
    Both test_phase6_plan3_* tests pass. Zero real BRAIN API calls. Gate correctly withholds best_submittable when proxy_drop=True and confirms it when additive=True.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirm_additive → hunt._apply_additivity_gate | additive=None (inconclusive) must be treated as non-additive by the gate |
| hunt() 401 propagation | HTTPError from BRAIN /check must reach CLI, not be swallowed in the gate helper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Spoofing | additive=None treated as True (inconclusive → submit-ready) | mitigate | _apply_additivity_gate only appends to confirmed_ids when result.additive is True (strict equality); None and False both excluded |
| T-06-10 | Denial of Service | confirm_additive called in sim thread pool | accept (by design prevention) | confirm_additive is called sequentially inside _apply_additivity_gate, never inside grade_many concurrency pool; constraint documented in helper |
| T-06-11 | Elevation of Privilege | best_submittable bypassing gate via stale DB self_corr | mitigate | confirm_additive always issues a FRESH /check call (D-05 locked decision); DB self_corr from grading time is not used |
| T-06-12 | Denial of Service | CONFIRM_LIMIT=3, each poll up to 300s = 900s worst case | accept | Named constant; user can lower. Sequential confirms are the cost of authoritative gate. Document in hunt() docstring. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
Full phase 6 test run:
./venv/bin/python -m pytest test_phase4.py -k "phase6" --tb=short -q

Expected: 15 passed (4 plan1 + 9 plan2 + 2 plan3), 0 failed

Gate wiring sanity check (both old patterns replaced):
grep -c "_rank_best(all_pass_ids" hunt.py
# Expected: 0

Gate call sites confirmed:
grep -c "_apply_additivity_gate" hunt.py
# Expected: 3 (1 def + 2 call sites)
</verification>

<success_criteria>
- import additivity present in hunt.py
- _apply_additivity_gate defined and called at both former _rank_best(all_pass_ids, conn) sites
- additive=None treated as non-additive (not submitted-ready)
- 401 propagates through gate to CLI
- 15 total phase6 offline tests pass
- No old _rank_best(all_pass_ids, conn) pattern remains in hunt.py
</success_criteria>

<output>
Create .planning/phases/06-additivity-gate/06-03-SUMMARY.md when done
</output>
