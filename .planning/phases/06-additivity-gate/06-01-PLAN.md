---
phase: 06-additivity-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - selfcorr.py
  - test_phase4.py
autonomous: true
requirements:
  - ADD-01
  - ADD-04

must_haves:
  truths:
    - "get_book_pnl_paths(conn) returns only ACTIVE-status alpha pnl_paths (the 16 submitted competition alphas), never 'pass' or 'UNSUBMITTED' rows"
    - "backfill_active_pnl no longer silently skips alphas whose pnl_path is set in the DB but whose file does not exist on disk"
    - "_null_stale_pnl_paths nulls the pnl_path column for every row where the cached file is missing, before the backfill SELECT runs"
    - "All four new tests pass with ./venv/bin/python -m pytest test_phase4.py -k 'phase6_plan1' --tb=short -x"
    - "Decisions covered: D-03 (the book = ACTIVE-status alphas only, via the new get_book_pnl_paths) and D-04 (stale pnl_path nulled so backfill_active_pnl re-fetches instead of no-op'ing)"
  artifacts:
    - path: "selfcorr.py"
      provides: "get_book_pnl_paths function (status='ACTIVE' only) + _null_stale_pnl_paths helper + backfill_active_pnl fix"
      contains: "get_book_pnl_paths"
    - path: "test_phase4.py"
      provides: "Offline tests for get_book_pnl_paths + stale-path nulling + backfill behavior"
      contains: "phase6_plan1"
  key_links:
    - from: "additivity.py (Plan 2)"
      to: "selfcorr.get_book_pnl_paths"
      via: "import selfcorr; selfcorr.get_book_pnl_paths(conn)"
      pattern: "get_book_pnl_paths"
    - from: "selfcorr.backfill_active_pnl"
      to: "selfcorr._null_stale_pnl_paths"
      via: "called at top of backfill_active_pnl before SELECT query"
      pattern: "_null_stale_pnl_paths"
---

<objective>
Add two primitives to selfcorr.py that the additivity module requires, and fix the pnl_path no-op bug that makes the proxy gate silently a no-op.

Purpose: The combined-book correlation (ADD-01) must correlate against only submitted/active competition alphas (D-03), not locally-passing candidates. The D-04 bug means all 16 ACTIVE alphas have pnl_path set in the DB even though pnl_cache/ is gone on disk — backfill skips them, so every subsequent proxy correlation sees an empty reference set and returns None for every candidate.

Output: get_book_pnl_paths (new standalone function), _null_stale_pnl_paths (private helper), backfill_active_pnl (fixed to call _null_stale_pnl_paths first), four offline tests appended to test_phase4.py.
</objective>

<execution_context>
@/Users/winter.__.kor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/winter.__.kor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/PROJECT.md
@/Users/winter.__.kor/quant/.planning/ROADMAP.md
@/Users/winter.__.kor/quant/.planning/STATE.md
@/Users/winter.__.kor/quant/.planning/phases/06-additivity-gate/06-CONTEXT.md
@/Users/winter.__.kor/quant/.planning/phases/06-additivity-gate/06-RESEARCH.md

<interfaces>
From selfcorr.py (verified lines 288-464):

get_reference_pnl_paths(conn) -> list[str]
  SELECT pnl_path FROM alphas WHERE pnl_path IS NOT NULL AND status IN ('pass', 'ACTIVE')
  DO NOT modify — proxy_gate depends on this including 'pass' rows for dedup.

backfill_active_pnl(client, conn, db_path='alpha_kb.db', pnl_dir='pnl_cache') -> int
  Line 439: SELECT alpha_id FROM alphas WHERE status='ACTIVE' AND pnl_path IS NULL
  This is the bug site: pnl_path is set in DB but file is gone on disk → finds 0 rows.

get_selfcorr_limit(conn) -> Optional[float]
  SELECT limit_val FROM checks WHERE name='SELF_CORRELATION' AND limit_val IS NOT NULL LIMIT 1

_pnls_to_daily_returns(pnls: list) -> list   # line 70
_pearson(x: list, y: list) -> float          # line 98
_date_overlap_returns(path_a, path_b) -> tuple  # line 124
load_returns(pnl_path) -> list               # line 268
max_pearson(candidate_path, reference_paths) -> float  # line 325

DB status values (verified against live alpha_kb.db):
  'ACTIVE'       — synced from BRAIN; submitted competition alphas (16 rows)
  'UNSUBMITTED'  — synced from BRAIN; not in competition
  'pass'         — local simulation survivor; NOT submitted
  'fail', 'near', 'queued', 'duplicate', 'error', 'timeout' — local grading states
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add get_book_pnl_paths and _null_stale_pnl_paths to selfcorr.py</name>
  <files>selfcorr.py</files>
  <read_first>selfcorr.py (full file — read before editing); .planning/phases/06-additivity-gate/06-RESEARCH.md §Finding 3 and §Finding 4</read_first>
  <behavior>
    - get_book_pnl_paths(conn) returns only rows where status='ACTIVE' AND pnl_path IS NOT NULL; never includes 'pass' or 'UNSUBMITTED'; returns list[str]
    - _null_stale_pnl_paths(conn) queries ALL rows where pnl_path IS NOT NULL (not just ACTIVE), checks Path(pnl_path).exists() for each, issues UPDATE alphas SET pnl_path=NULL WHERE alpha_id=? for stale rows, commits, returns count of rows nulled
    - backfill_active_pnl calls _null_stale_pnl_paths(conn) as its FIRST statement (before the existing SELECT at line 439), prints "[selfcorr] backfill: nulled {n} stale pnl_path entries" when n > 0
    - get_reference_pnl_paths is NOT modified — it keeps status IN ('pass', 'ACTIVE') for proxy_gate dedup
  </behavior>
  <action>
    Place get_book_pnl_paths immediately after get_reference_pnl_paths (around line 305). Docstring: "Return pnl_path for submitted/active competition alphas only (status='ACTIVE'). Used by additivity.rank_by_proxy so the book matches the team competition score (D-03). Do NOT use get_reference_pnl_paths here — that includes locally-passing candidates."

    Place _null_stale_pnl_paths as a private helper immediately before backfill_active_pnl. It accepts only conn (no pnl_dir arg — pnl_path in the DB already contains the full relative path). Query: SELECT alpha_id, pnl_path FROM alphas WHERE pnl_path IS NOT NULL. For each row, call Path(pnl_path).exists(); if False, add (alpha_id,) to stale list. Run executemany("UPDATE alphas SET pnl_path=NULL WHERE alpha_id=?", stale) then conn.commit(). Return len(stale).

    In backfill_active_pnl, add as first body line: n_stale = _null_stale_pnl_paths(conn), then: if n_stale: print(f"[selfcorr] backfill: nulled {n_stale} stale pnl_path entries"). The rest of the function body is unchanged.

    No new imports needed (Path is already imported via pathlib at the top of selfcorr.py).
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && grep -n "def get_book_pnl_paths" selfcorr.py && grep -n "def _null_stale_pnl_paths" selfcorr.py && grep -n "_null_stale_pnl_paths(conn)" selfcorr.py</automated>
  </verify>
  <done>
    - get_book_pnl_paths defined in selfcorr.py, queries status='ACTIVE' only
    - _null_stale_pnl_paths defined in selfcorr.py, nulls stale paths and returns count
    - backfill_active_pnl calls _null_stale_pnl_paths(conn) as its first statement
    - grep confirms all three function names present and the call site wired
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline tests for get_book_pnl_paths and stale-path fix</name>
  <files>test_phase4.py</files>
  <read_first>test_phase4.py (lines 1-60 for import pattern and fixture style); selfcorr.py (updated Task 1 version)</read_first>
  <behavior>
    - test_phase6_plan1_get_book_pnl_paths_active_only: insert one ACTIVE alpha with pnl_path set, one 'pass' alpha with pnl_path set, one UNSUBMITTED alpha with pnl_path set; assert get_book_pnl_paths returns exactly the ACTIVE one's path
    - test_phase6_plan1_get_book_pnl_paths_none_when_empty: empty DB; assert get_book_pnl_paths returns []
    - test_phase6_plan1_null_stale_pnl_paths: insert two ACTIVE alphas with pnl_path pointing to non-existent files; call _null_stale_pnl_paths(conn); assert both rows now have pnl_path IS NULL and the function returned 2
    - test_phase6_plan1_backfill_nulls_stale_before_fetch: in-memory DB with one ACTIVE alpha whose pnl_path points to a non-existent file; mock client with get_pnl returning synthetic PnL JSON; call backfill_active_pnl(mock_client, conn, ':memory:'); assert pnl_path is re-set after backfill (the row was found because _null_stale_pnl_paths nulled it first); verify client.get_pnl was called exactly once
  </behavior>
  <action>
    Append a new section "# ---------------------------------------------------------------------------\n# Phase 6 Plan 1: selfcorr book-reference primitives\n# ---------------------------------------------------------------------------" to test_phase4.py.

    All tests use db.init_db(':memory:') for the SQLite connection (consistent with existing test_phase4.py pattern). Import selfcorr at the top of the new section (use importlib.import_module or direct import; the module exists).

    For the backfill mock: create a MagicMock client; configure client.get_pnl.return_value to return a dict {"pnl": [{"date": "2024-01-02", "pnl": 100.0}], "alpha": {"id": "testid"}}. Inspect selfcorr.fetch_and_cache_pnl's actual call signature to wire the mock correctly — do not assume the interface, read it first.

    Name all four tests with the prefix test_phase6_plan1_ so the pytest -k 'phase6_plan1' filter selects exactly them.

    Use tmp_path fixture (pytest built-in) for any test that needs a real file on disk (the backfill test where fetch_and_cache_pnl writes to pnl_cache/).
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && ./venv/bin/python -m pytest test_phase4.py -k "phase6_plan1" --tb=short -x -q</automated>
  </verify>
  <done>
    All four test_phase6_plan1_* tests pass. Zero BRAIN API calls. No disk writes outside tmp_path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → get_book_pnl_paths | pnl_path strings come from DB (written by sync.py from BRAIN response); treated as file paths |
| Filesystem → _null_stale_pnl_paths | Path.exists() call on DB-stored paths; read-only filesystem probe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | _null_stale_pnl_paths: executemany UPDATE | mitigate | Only nulls rows where file is confirmed absent via Path.exists(); never deletes rows; conn.commit() called after batch update |
| T-06-02 | Information Disclosure | get_book_pnl_paths path strings | accept | pnl_path values are local filesystem paths already in DB; no new exposure |
| T-06-03 | Denial of Service | _null_stale_pnl_paths iterating all non-null pnl_path rows | accept | Upper bound is total alpha count (~400 rows); Path.exists() is fast local I/O; no network |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; stdlib only |
</threat_model>

<verification>
./venv/bin/python -m pytest test_phase4.py -k "phase6_plan1" --tb=short -x -q

Expected: 4 passed, 0 failed, 0 errors

Also verify get_reference_pnl_paths is unchanged (proxy_gate dedup must still include 'pass' rows):
grep -A5 "def get_reference_pnl_paths" selfcorr.py | grep "pass"
</verification>

<success_criteria>
- get_book_pnl_paths(conn) returns only ACTIVE-status pnl_paths
- _null_stale_pnl_paths nulls stale DB entries before backfill SELECT runs
- backfill_active_pnl calls _null_stale_pnl_paths as its first action
- get_reference_pnl_paths unchanged (still includes 'pass' for proxy_gate)
- 4 offline tests pass with ./venv/bin/python
</success_criteria>

<output>
Create .planning/phases/06-additivity-gate/06-01-SUMMARY.md when done
</output>
