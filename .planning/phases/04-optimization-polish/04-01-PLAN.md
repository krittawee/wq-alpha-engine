---
phase: 04-optimization-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - grade.py
  - db.py
autonomous: true
requirements:
  - OPT-01
  - OPT-02
  - OPT-03

must_haves:
  truths:
    - "grade_one and grade_many accept an optional settings dict without breaking existing callers"
    - "checks_history table exists in alpha_kb.db and append_checks_history appends rows, never overwrites"
    - "alphas.note_path column exists after init_db runs on an existing DB"
    - "_ALPHA_COLS includes note_path so upsert_alpha covers the new column"
  artifacts:
    - path: "grade.py"
      provides: "settings-param-aware _simulate_to_alpha, grade_one, grade_many"
      contains: "settings: Optional[dict] = None"
    - path: "db.py"
      provides: "checks_history DDL + append_checks_history + note_path migration"
      contains: "CREATE TABLE IF NOT EXISTS checks_history"
  key_links:
    - from: "optimizer.py (Plan 03)"
      to: "grade.grade_many"
      via: "settings_map parameter"
      pattern: "settings_map"
    - from: "decay_monitor.py (Plan 04)"
      to: "db.append_checks_history"
      via: "direct import"
      pattern: "append_checks_history"
    - from: "obsidian.py (Plan 05)"
      to: "alphas.note_path"
      via: "UPDATE alphas SET note_path=?"
      pattern: "note_path"
---

<objective>
Extend grade.py and db.py with the two prerequisite changes that all Phase 4 features depend on: an optional settings override in the grading pipeline (OPT-01 blocker) and the checks_history table plus note_path column in the data layer (OPT-02, OPT-03 blocker).

Purpose: Plans 03, 04, and 05 cannot proceed until these two files are modified. The grade.py change is a hard prerequisite for the Settings Optimizer (without it, every variant simulation falls back to _BASE_SETTINGS silently). The db.py changes are hard prerequisites for decay history persistence and Obsidian two-way linking.

Output: Modified grade.py (backward-compatible settings param in 3 functions), modified db.py (new DDL entry + append helper + idempotent ALTER migration + _ALPHA_COLS update).
</objective>

<execution_context>
@/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-RESEARCH.md
@/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-PATTERNS.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/PROJECT.md
@/Users/winter.__.kor/quant/.planning/ROADMAP.md

<interfaces>
<!-- Key signatures extracted from live codebase. Read the actual files before editing. -->

From grade.py (lines to modify):
  Line 44: _BASE_SETTINGS dict (decay/neutralization/truncation/region/universe/delay keys)
  Line 62: def _simulate_to_alpha(client, expression: str, attempts: int = 3)
  Line 74: sim = client.simulate(expression, settings=_BASE_SETTINGS)
  Line 92-98: def grade_one(client, conn, expression, run_id, parent_alpha_id=None) -> dict
  Line 148: sim, alpha = _simulate_to_alpha(client, expression)
  Line 179: settings_json = json.dumps(_BASE_SETTINGS)
  Line 321: def grade_many(client, conn, expressions, run_id, max_workers=1, db_path=None, parent_map=None) -> list

From db.py (lines to modify):
  Lines 15-44: _DDL list of CREATE TABLE IF NOT EXISTS statements
  Lines 47-53: _ALPHA_COLS list
  Lines 62-68: init_db(path) opens conn, sets WAL+busy_timeout, runs _DDL, runs migrations
  Lines 71-78: idempotent ALTER TABLE migration block (Phase 3 adds 'diagnosis')
  Lines 94-117: upsert_checks (uses INSERT OR REPLACE — NOT the model for checks_history)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend grade.py with optional settings param (backward-compatible)</name>
  <files>grade.py</files>
  <read_first>
    Read grade.py in full before editing. The exact lines are: _simulate_to_alpha at line 62,
    client.simulate call at line 74, grade_one at line 92, _simulate_to_alpha call at line 148,
    settings_json assignment at line 179, grade_many at line 321.
    Read 04-PATTERNS.md "grade.py (modify)" section for exact before/after signatures.
    Read CLAUDE.md constraint: SDK simulate() regular param is buggy — settings= keyword is correct.
  </read_first>
  <behavior>
    - _simulate_to_alpha(client, expression, settings=None, attempts=3): when settings is None, use _BASE_SETTINGS; when non-None, use the passed dict
    - client.simulate(expression, settings=active_settings) — never client.simulate(expression, regular=...)
    - grade_one(..., settings: Optional[dict] = None): passes settings down to _simulate_to_alpha; when settings is non-None uses it for settings_json recording instead of _BASE_SETTINGS
    - grade_many(..., settings_map: Optional[dict] = None): in the worker-pair loop, passes settings_map.get(expr) (or None) to each grade_one call
    - Existing callers with no settings arg get identical behavior (None falls through to _BASE_SETTINGS)
  </behavior>
  <action>
    Modify _simulate_to_alpha signature at line 62: add `settings: dict = None` before `attempts`.
    Add `active_settings = settings if settings is not None else _BASE_SETTINGS` as first line of function body.
    Change line 74 from `client.simulate(expression, settings=_BASE_SETTINGS)` to `client.simulate(expression, settings=active_settings)`.

    Modify grade_one signature at line 92: add `settings: Optional[dict] = None` after `parent_alpha_id=None`.
    Change line 148 from `_simulate_to_alpha(client, expression)` to `_simulate_to_alpha(client, expression, settings=settings)`.
    After the sim line, add `active_settings = settings if settings is not None else _BASE_SETTINGS`.
    Change line 179 from `settings_json = json.dumps(_BASE_SETTINGS)` to `settings_json = json.dumps(active_settings)`.
    Wherever _BASE_SETTINGS["decay"], _BASE_SETTINGS["neutralization"], _BASE_SETTINGS["truncation"] are read for the alpha_dict, replace with active_settings["decay"] etc.

    Modify grade_many signature at line 321: add `settings_map: Optional[dict] = None` after `parent_map`.
    In the worker pair-normalization loop (lines 348-356), when building each grade_one call, pass `settings=settings_map.get(expr) if settings_map else None`.

    No other changes. All three changes are pure additive signature extensions — existing tests and callers are unaffected.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "import grade; import inspect; sig = inspect.signature(grade.grade_one); assert 'settings' in sig.parameters, 'settings param missing from grade_one'; sig2 = inspect.signature(grade.grade_many); assert 'settings_map' in sig2.parameters, 'settings_map missing from grade_many'; print('grade.py signatures OK')"</automated>
  </verify>
  <done>
    grade_one signature includes `settings: Optional[dict] = None`.
    grade_many signature includes `settings_map: Optional[dict] = None`.
    _simulate_to_alpha uses active_settings (not hardcoded _BASE_SETTINGS) for client.simulate call.
    Calling grade_one(client, conn, expr, run_id) without settings produces identical behavior to before.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend db.py with checks_history DDL, append helper, note_path migration, _ALPHA_COLS update</name>
  <files>db.py</files>
  <read_first>
    Read db.py in full before editing. Key sections:
    - Lines 15-44: _DDL list — add checks_history CREATE TABLE + index here
    - Lines 47-53: _ALPHA_COLS list — add "note_path" after "diagnosis"
    - Lines 62-68: init_db body — migration block location
    - Lines 71-78: Phase 3 idempotent ALTER TABLE block (exact pattern to copy for note_path)
    - Lines 94-117: upsert_checks (uses INSERT OR REPLACE — do NOT use this pattern for append_checks_history)
    Read 04-PATTERNS.md "db.py (modify)" section for exact DDL and append helper.
    Read 04-RESEARCH.md Pattern 3 for checks_history schema and rationale.
  </read_first>
  <behavior>
    - checks_history table: columns id (AUTOINCREMENT PK), alpha_id TEXT NOT NULL, name TEXT NOT NULL, result TEXT, value REAL, limit_val REAL, checked_at TEXT NOT NULL, run_tag TEXT
    - idx_checks_history_alpha index on (alpha_id, name, checked_at)
    - append_checks_history(conn, alpha_id, checks_list, run_tag=""): uses plain INSERT (never INSERT OR REPLACE), inserts checked_at as datetime.utcnow().isoformat()
    - note_path migration: idempotent ALTER TABLE alphas ADD COLUMN note_path TEXT inside try/except sqlite3.OperationalError
    - _ALPHA_COLS: "note_path" appears after "diagnosis" and before "status"
    - init_db remains idempotent — running twice on the same DB has no effect
  </behavior>
  <action>
    In _DDL list (after existing Phase 3 DDL entries): append two new strings —
    one `CREATE TABLE IF NOT EXISTS checks_history (...)` with the 8-column schema
    (id AUTOINCREMENT, alpha_id, name, result TEXT, value REAL, limit_val REAL, checked_at TEXT NOT NULL, run_tag TEXT),
    and one `CREATE INDEX IF NOT EXISTS idx_checks_history_alpha ON checks_history(alpha_id, name, checked_at)`.

    In _ALPHA_COLS list: insert the string "note_path" after "diagnosis" (maintain list order).

    In init_db(), after the existing Phase 3 ALTER TABLE migration block: add a new identical try/except block
    that runs `conn.execute("ALTER TABLE alphas ADD COLUMN note_path TEXT")` + `conn.commit()`,
    catching sqlite3.OperationalError with pass.

    Add new top-level function `append_checks_history(conn, alpha_id, checks_list, run_tag="")` that:
    - Computes `now = datetime.utcnow().isoformat()`
    - Builds `rows` list of 7-tuples: (alpha_id, c["name"], c.get("result"), c.get("value"), c.get("limit"), now, run_tag)
    - Calls `conn.executemany("INSERT INTO checks_history (alpha_id,name,result,value,limit_val,checked_at,run_tag) VALUES (?,?,?,?,?,?,?)", rows)`
    - Calls `conn.commit()`

    Place append_checks_history immediately after upsert_checks for proximity.
    No changes to upsert_checks or any other existing function.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import sqlite3, db
conn = db.init_db(':memory:')
# checks_history exists
conn.execute('SELECT id, alpha_id, name, result, value, limit_val, checked_at, run_tag FROM checks_history LIMIT 0')
# note_path column exists on alphas
conn.execute('SELECT note_path FROM alphas LIMIT 0')
# append is non-overwriting
db.append_checks_history(conn, 'a1', [{'name':'LOW_SHARPE','result':'FAIL','value':0.9,'limit':1.25}])
db.append_checks_history(conn, 'a1', [{'name':'LOW_SHARPE','result':'FAIL','value':0.8,'limit':1.25}])
rows = conn.execute('SELECT id FROM checks_history WHERE alpha_id=?',('a1',)).fetchall()
assert len(rows) == 2, f'expected 2 rows, got {len(rows)}'
print('db.py schema changes OK')
conn.close()
"</automated>
  </verify>
  <done>
    checks_history table created by init_db with 8 columns and the composite index.
    append_checks_history inserts two distinct rows for two separate calls on the same alpha_id (no overwrite).
    alphas.note_path column accessible via SELECT after init_db on existing or new DB.
    _ALPHA_COLS contains "note_path" after "diagnosis".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grade.py internal → BRAIN API | settings dict from optimizer.py flows into client.simulate(); values must originate from ARCHETYPE_HEURISTICS or DB query, never from user-provided text |
| SQLite write path | append_checks_history and init_db migrations write to alpha_kb.db |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | grade.py settings param | mitigate | settings dict sourced only from ARCHETYPE_HEURISTICS constant and parameterized DB query in optimizer.py (Plan 03); never from user CLI input |
| T-04-02 | Tampering | append_checks_history SQL | mitigate | uses ? parameterized executemany — already codebase norm; confirmed in task action |
| T-04-03 | Information Disclosure | checked_at timestamps in checks_history | accept | local SQLite file; no network exposure; timestamps are non-sensitive run metadata |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | no new packages in Phase 4 (RESEARCH.md Package Legitimacy Audit: none) |
</threat_model>

<verification>
Run both automated verify commands from the tasks above.
Then: `cd /Users/winter.__.kor/quant && python -c "import grade, db; print('imports OK')"` — confirms no import errors introduced.
If test_phase4.py exists (Plan 02): `pytest test_phase4.py::test_history_append_only test_phase4.py::test_grade_settings_param -x` — run only the tests that touch this plan's changes.
</verification>

<success_criteria>
grade_one and grade_many signatures include optional settings/settings_map params, defaulting to None (existing callers unchanged).
_simulate_to_alpha uses active_settings (falls back to _BASE_SETTINGS when None).
db.init_db creates checks_history on fresh DB and survives being run twice on existing DB.
append_checks_history produces distinct rows on repeated calls — SELECT COUNT(*) confirms no overwrite.
alphas.note_path column exists after init_db runs on a DB that predates Phase 4.
_ALPHA_COLS list contains "note_path".
</success_criteria>

<output>
Create `/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-01-SUMMARY.md` when done.
</output>
