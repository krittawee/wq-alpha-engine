---
phase: 04-optimization-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - optimizer.py
  - optimize.py
autonomous: true
requirements:
  - OPT-01

must_haves:
  truths:
    - "For a NEAR alpha, build_variants returns ≤4 settings dicts from archetype heuristics + past PASS combos, never the current combo"
    - "run_optimize simulates each variant via grade.grade_many (not a new sim path), with settings_map per variant"
    - "Each simulated variant is recorded in DB with parent_alpha_id = the NEAR alpha's alpha_id"
    - "selfcorr.proxy_gate is called before variant simulation for each NEAR alpha"
    - "optimize.py performs single-shot login before run_optimize, propagates 401 without retry"
    - "obsidian.regen_all is called as a side-effect at the end of run_optimize (per D-11)"
  artifacts:
    - path: "optimizer.py"
      provides: "ARCHETYPE_HEURISTICS constant + build_variants() + run_optimize()"
      contains: "ARCHETYPE_HEURISTICS"
    - path: "optimize.py"
      provides: "/optimize CLI entrypoint"
      contains: "from wq_login import login"
  key_links:
    - from: "optimizer.run_optimize"
      to: "grade.grade_many"
      via: "settings_map={expression: variant_settings}"
      pattern: "settings_map"
    - from: "optimizer.run_optimize"
      to: "selfcorr.proxy_gate"
      via: "called before grade_many per NEAR alpha"
      pattern: "proxy_gate"
    - from: "optimizer.run_optimize"
      to: "obsidian.regen_all"
      via: "called at end of run_optimize as side-effect (D-11)"
      pattern: "regen_all"
---

<objective>
Build the Settings Optimizer: optimizer.py containing the ARCHETYPE_HEURISTICS table, build_variants() function, and run_optimize() orchestrator; plus optimize.py CLI entrypoint. This delivers OPT-01 end-to-end.

Purpose: For every NEAR alpha in the DB, the optimizer proposes ≤4 settings variants from the heuristic table blended with past PASS settings, simulates them via the existing grade_many concurrency path (≤3 sims, single-shot auth), records outcomes back to DB with parent_alpha_id, and regenerates Obsidian notes as a side-effect.

Output: optimizer.py (service module), optimize.py (CLI entrypoint).
</objective>

<execution_context>
@/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-RESEARCH.md
@/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-PATTERNS.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/ROADMAP.md

<interfaces>
<!-- Signatures from live codebase — read these files before implementing -->

From grade.py (after Plan 01 modifications):
  grade_one(client, conn, expression, run_id, parent_alpha_id=None, settings=None) -> dict
  grade_many(client, conn, expressions, run_id, max_workers=1, db_path=None, parent_map=None, settings_map=None) -> list[dict]
  _BASE_SETTINGS: dict with keys decay, neutralization, truncation, region, universe, delay

From selfcorr.py:
  proxy_gate(alpha_id: str, conn: Connection) -> bool   # True = skip (too correlated)
  # Degrades gracefully to False if no PnL cached for the alpha

From researcher.py:
  ARCHETYPES: list[str]  # 8 labels: reversal, momentum, value_garp, quality, growth, low_volatility, liquidity_volume, sentiment_event

From db.py (after Plan 01):
  init_db(path: str) -> Connection
  upsert_alpha(conn, alpha_dict) -> None   # records variant with parent_alpha_id
  _ALPHA_COLS: list includes note_path

From editor.py:
  classify_from_checks(checks: list) -> str  # returns 'pass'/'near'/'fail'
  EditorAuthError: exception class

From hunt.py (CLI pattern lines 379-430):
  login() — single-shot BRAIN auth
  401 propagation pattern: getattr(getattr(e, "response", None), "status_code", None) == 401
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: optimizer.py — ARCHETYPE_HEURISTICS + build_variants</name>
  <files>optimizer.py</files>
  <read_first>
    Read 04-RESEARCH.md Pattern 1 (full ARCHETYPE_HEURISTICS table for all 8 archetypes).
    Read 04-PATTERNS.md "optimizer.py" section for imports pattern and NEAR alpha query.
    Read researcher.py for the exact ARCHETYPES list (8 labels — must be the keys).
    Read grade.py lines 44-60 for _BASE_SETTINGS keys.
    Read db.py lines 47-53 for _ALPHA_COLS (verify decay/neutralization/truncation are columns).
    Read 04-RESEARCH.md Pitfall 1 (NULL archetype fallback) and Pitfall 3 (expression dedup).
  </read_first>
  <behavior>
    - ARCHETYPE_HEURISTICS maps each of the 8 researcher.ARCHETYPES labels to a list of 4 (decay, neutralization, truncation) tuples (exact values from 04-RESEARCH.md Pattern 1)
    - build_variants(alpha_row: dict, conn: Connection) -> list[dict]: returns ≤4 settings dicts
    - When alpha_row["archetype"] is None or empty, defaults to "reversal" (Pitfall 1 fallback) and logs a warning
    - Excludes the current (decay, neutralization, truncation) combo from returned variants
    - Merges heuristic candidates with up to 2 distinct PASS/ACTIVE combos from DB (sharpe DESC LIMIT 10 query, then filter out current), deduplicates by (d,n,t) tuple, caps at 4
    - Returns list of full settings dicts: each is a copy of _BASE_SETTINGS with decay/neutralization/truncation overridden (region/universe/delay unchanged — per D-01)
    - Returns empty list if all heuristic combos match the current combo (degenerate case)
  </behavior>
  <action>
    Create optimizer.py. Imports: json, sqlite3, uuid from stdlib; datetime from datetime; Optional from typing; db, grade, researcher, selfcorr as project modules.

    Define ARCHETYPE_HEURISTICS dict at module top. Copy all 8 archetype entries verbatim from 04-RESEARCH.md Pattern 1 (reversal, momentum, value_garp, quality, growth, low_volatility, liquidity_volume, sentiment_event — each maps to a list of exactly 4 (decay, neutralization, truncation) tuples).

    Implement build_variants(alpha_row, conn):
    1. Extract archetype = alpha_row.get("archetype") or "reversal". If None, print warning.
    2. current = (alpha_row.get("decay"), alpha_row.get("neutralization"), alpha_row.get("truncation")).
    3. heuristic_combos = [c for c in ARCHETYPE_HEURISTICS.get(archetype, ARCHETYPE_HEURISTICS["reversal"]) if c != current].
    4. pass_combos query: `SELECT DISTINCT decay, neutralization, truncation FROM alphas WHERE status IN ('pass', 'ACTIVE') AND decay IS NOT NULL AND neutralization IS NOT NULL AND truncation IS NOT NULL ORDER BY sharpe DESC LIMIT 10` — filter out current.
    5. Merge: iterate heuristic_combos + pass_combos, deduplicate by tuple, cap at 4.
    6. For each surviving (d,n,t): copy dict(_BASE_SETTINGS), set ["decay"]=d, ["neutralization"]=n, ["truncation"]=t. Append to result.
    7. Return result (list of full settings dicts, len 0–4).

    Do NOT implement run_optimize yet — that is Task 2.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import sqlite3, db, optimizer
conn = db.init_db(':memory:')
alpha_row = {'alpha_id':'a1','expression':'rank(returns)','archetype':'reversal','decay':15,'neutralization':'SUBINDUSTRY','truncation':0.08}
variants = optimizer.build_variants(alpha_row, conn)
assert 0 < len(variants) <= 4, f'Expected 1-4 variants, got {len(variants)}'
for v in variants:
    assert v['decay'] != 15 or v['neutralization'] != 'SUBINDUSTRY' or v['truncation'] != 0.08, 'current combo in variants'
    assert 'region' in v and 'universe' in v, 'region/universe must be preserved'
print(f'build_variants OK: {len(variants)} variants')
conn.close()
"</automated>
  </verify>
  <done>
    ARCHETYPE_HEURISTICS contains exactly 8 keys matching researcher.ARCHETYPES values.
    build_variants returns 1–4 settings dicts for a NEAR reversal alpha.
    No returned variant matches the current (decay,neutralization,truncation) tuple.
    Each returned variant preserves region/universe/delay from _BASE_SETTINGS.
    NULL archetype defaults to "reversal" without raising an exception.
  </done>
</task>

<task type="auto">
  <name>Task 2: optimizer.py — run_optimize orchestrator + optimize.py CLI</name>
  <files>optimizer.py, optimize.py</files>
  <read_first>
    Read 04-PATTERNS.md "optimizer.py" section: proxy_gate call pattern, grade_many call with settings_map, 401 propagation pattern, error handling.
    Read 04-PATTERNS.md "optimize.py" section: full CLI pattern from hunt.py lines 379-430.
    Read 04-RESEARCH.md Pattern 2 (self-correlation handling for variants) and Pattern 8 (CLI pattern).
    Read 04-RESEARCH.md Pitfall 2 (parent_map/settings_map usage for lineage).
    Read 04-RESEARCH.md Pitfall 3 (expression dedup / duplicate status handling).
    Read hunt.py lines 379-430 for the exact CLI structure (auth, error handling, sys.exit codes).
    Read CLAUDE.md for the concurrency (≤3 sims), single-shot auth, 401-stops-run constraints.
  </read_first>
  <action>
    Add run_optimize(client, db_path="alpha_kb.db", max_workers=1) to optimizer.py:
    1. Open conn via db.init_db(db_path).
    2. Query all NEAR alphas: `SELECT alpha_id, expression, archetype, decay, neutralization, truncation FROM alphas WHERE status='near'`.
    3. For each NEAR alpha row (as dict):
       a. Call selfcorr.proxy_gate(near_alpha_id, conn). If True, print a skip message and continue.
       b. Call build_variants(alpha_row, conn) to get ≤4 settings dicts.
       c. If empty, skip (log "no variants for alpha_id").
       d. For each variant_settings in the list, call grade.grade_many with:
          - client=client
          - conn=conn (note: grade_many opens per-worker conns; pass conn for context only)
          - expressions=[near_alpha["expression"]]
          - run_id=str(uuid.uuid4())
          - max_workers=1 (sequential — each variant uses different settings, must be one-at-a-time)
          - db_path=db_path
          - parent_map={near_alpha["expression"]: near_alpha_id}
          - settings_map={near_alpha["expression"]: variant_settings}
       e. For each result in grade_many results: if result.get("status") not in ("duplicate","invalid","error"), count it as a completed sim.
       f. 401 HTTPError propagates immediately (raise — never re-auth per CLAUDE.md).
    4. After processing all NEAR alphas, call obsidian.regen_all(conn) as a side-effect (D-11, per CONTEXT.md).
    5. Return a summary dict: {"near_alphas_processed": N, "variants_simulated": M, "variants_passed": K}.

    Create optimize.py (new file). Mirror hunt.py __main__ block exactly:
    - argparse with --db (default "alpha_kb.db") and --max-workers (default 1, type int)
    - Print "[optimize] logging in..."
    - client = login() (from wq_login import login) — single call before the loop
    - Print "[optimize] authenticated"
    - try/except EditorAuthError → print + sys.exit(1)
    - except requests.exceptions.HTTPError checking status_code==401 → print "BRAIN AUTH EXPIRED — 401. Re-run to re-authenticate." + sys.exit(1)
    - else raise for non-401 HTTP errors
    - import optimizer; call optimizer.run_optimize(client=client, db_path=args.db, max_workers=args.max_workers)
    - Print summary on completion
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import ast, sys
with open('optimize.py') as f: ast.parse(f.read())
with open('optimizer.py') as f: src = f.read()
ast.parse(src)
assert 'proxy_gate' in src, 'proxy_gate call missing from optimizer.py'
assert 'settings_map' in src, 'settings_map missing from grade_many call'
assert 'regen_all' in src or 'regen_archetype' in src, 'obsidian regen call missing'
assert 'parent_map' in src, 'parent_map (lineage) missing'
print('optimizer.py + optimize.py structure OK')
"</automated>
  </verify>
  <done>
    run_optimize queries NEAR alphas, skips proxy-gated ones, calls build_variants, calls grade_many with settings_map per variant.
    Each grade_many call uses parent_map={expression: near_alpha_id} so DB records parent_alpha_id.
    401 from BRAIN propagates up and stops the run without retry.
    obsidian.regen_all (or equivalent) called after all NEAR alphas processed.
    optimize.py parses cleanly and contains single-shot login pattern.
    pytest test_phase4.py::test_build_variants_cap passes.
    pytest test_phase4.py::test_optimizer_calls_grade_many passes.
    pytest test_phase4.py::test_variant_lineage passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optimizer.py → grade.grade_many → BRAIN API | settings dicts from ARCHETYPE_HEURISTICS and DB flow into BRAIN simulations |
| optimize.py CLI → authenticated BRAIN session | single-shot login; 401 stops run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Elevation of Privilege | optimizer BRAIN sim budget | mitigate | max_workers default=1 (sequential); ≤4 variants per NEAR alpha; concurrency cap enforced by grade_many (≤3 total) |
| T-04-10 | Tampering | optimizer.py SQL queries | mitigate | all queries use ? parameterized form (codebase norm); verified in code action |
| T-04-11 | Denial of Service | BRAIN throttle abuse | mitigate | max_workers=1 default + grade_many cap (≤3) prevents exceeding BRAIN slot cap |
| T-04-12 | Information Disclosure | auth session in optimize.py | mitigate | login() called once; 401 raises without exposing session token; no token logged |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | no new packages |
</threat_model>

<verification>
cd /Users/winter.__.kor/quant && pytest test_phase4.py::test_build_variants_cap test_phase4.py::test_build_variants_no_self test_phase4.py::test_optimizer_calls_grade_many test_phase4.py::test_variant_lineage -x
All 4 optimizer tests green.
python -c "import optimizer, optimize" — no import errors.
</verification>

<success_criteria>
ARCHETYPE_HEURISTICS covers all 8 archetypes from researcher.ARCHETYPES.
build_variants returns ≤4 dicts and excludes the current settings combo.
run_optimize calls selfcorr.proxy_gate before simulating any variant for a NEAR alpha.
grade_many called with both parent_map and settings_map so lineage and settings are recorded.
obsidian regen triggered at end of run_optimize (D-11 side-effect).
optimize.py: single-shot auth, 401 exits with message, no in-loop re-auth.
</success_criteria>

<output>
Create `/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-03-SUMMARY.md` when done.
</output>
