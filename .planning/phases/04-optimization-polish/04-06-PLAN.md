---
phase: 04-optimization-polish
plan: 06
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
  - "04-04"
  - "04-05"
files_modified:
  - .claude/skills/optimize/SKILL.md
  - .claude/skills/decay/SKILL.md
autonomous: false
requirements:
  - OPT-01
  - OPT-02
  - OPT-03

must_haves:
  truths:
    - "pytest test_phase4.py passes with all 12 tests green"
    - "pytest test_phase4.py test_phase3.py passes together (no regressions)"
    - "/optimize skill file exists and wires to optimize.py"
    - "/decay skill file exists and wires to decay.py"
    - "ROADMAP success criterion 1: optimizer produces ≤4 variants from heuristics+PASS, verified by test"
    - "ROADMAP success criterion 2: decay monitor surfaces degraded alphas, verified by test"
    - "ROADMAP success criterion 3: Obsidian notes exist for every archetype and failure family, verified by test"
  artifacts:
    - path: ".claude/skills/optimize/SKILL.md"
      provides: "/optimize slash-command wiring"
      contains: "python optimize.py"
    - path: ".claude/skills/decay/SKILL.md"
      provides: "/decay slash-command wiring"
      contains: "python decay.py"
  key_links:
    - from: "test_phase4.py"
      to: "optimizer.build_variants"
      via: "pytest test_build_variants_cap"
      pattern: "test_build_variants"
    - from: "test_phase4.py"
      to: "decay_monitor.detect_decay"
      via: "pytest test_decay_degraded"
      pattern: "test_decay"
---

<objective>
Complete Phase 4: create the two skill files for /optimize and /decay commands, run the full test suite, and verify all 3 ROADMAP success criteria pass. This is the phase gate that unlocks /gsd:verify-work.

Purpose: The skill files wire the new Python orchestrators to slash commands following the /hunt and /iterate pattern from Phase 3. The full test run (12 new + Phase 3 regression) confirms no regressions and all OPT-01/02/03 requirements met.

Output: .claude/skills/optimize/SKILL.md, .claude/skills/decay/SKILL.md, green full test suite.
</objective>

<execution_context>
@/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-RESEARCH.md
@/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-PATTERNS.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/ROADMAP.md
@/Users/winter.__.kor/quant/.planning/REQUIREMENTS.md

<interfaces>
<!-- Skill file pattern from existing /hunt skill (read .claude/skills/ before writing) -->

Expected skill structure (mirror /hunt and /find-alphas):
  .claude/skills/{name}/SKILL.md — describes what the command does + invocation
  Each SKILL.md includes: what the command does, how to invoke (python {entrypoint}.py), arguments

From ROADMAP.md Phase 4 Success Criteria (exact wording):
  1. For a NEAR alpha, the Settings Optimizer proposes <=4 settings variants drawn from archetype heuristics and past PASS settings in SQLite (never a blind grid sweep), simulates them, and records outcomes back to the DB
  2. The decay monitor queries the time-stamped `checks` table and surfaces any alpha whose key metrics have degraded across successive check runs
  3. An Obsidian note exists for every thesis run, every archetype, and every notable failure family, each referencing its associated `alpha_id`(s) from SQLite
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /optimize and /decay skill files</name>
  <files>.claude/skills/optimize/SKILL.md, .claude/skills/decay/SKILL.md</files>
  <read_first>
    Check .claude/skills/ directory (ls .claude/skills/) to see existing skill files (/hunt, /iterate, /find-alphas).
    Read one existing SKILL.md (e.g. .claude/skills/hunt/SKILL.md) for the exact format.
    Read 04-RESEARCH.md Pattern 8 for the CLI invocation details (optimize.py --db, decay.py --threshold).
    Read 04-CONTEXT.md D-11 for the command descriptions (what each does).
    Read 04-CONTEXT.md D-12 to confirm /hunt is unchanged (do NOT modify hunt skill).
  </read_first>
  <action>
    Create .claude/skills/optimize/ directory and SKILL.md:
    - Title: /optimize — Settings Optimizer + Obsidian Note Regen
    - Description: Runs the Settings Optimizer over all NEAR alphas in the DB. For each NEAR alpha, builds ≤4 settings variants from archetype heuristics and past PASS settings, simulates them via the existing grade_many path (≤3 concurrent, single-shot auth), and records outcomes with parent_alpha_id set. After all NEAR alphas are processed, regenerates Archetype and Failure notes in alpha-kb/. Does NOT auto-tune during /hunt.
    - Invocation: python optimize.py [--db alpha_kb.db] [--max-workers 1]
    - Auth note: requires a live BRAIN session — run after wq_login.py if session has expired.
    - Side effects: writes/updates alpha-kb/Archetypes/*.md and alpha-kb/Failures/*.md.

    Create .claude/skills/decay/ directory and SKILL.md:
    - Title: /decay — Decay Monitor
    - Description: Re-checks all PASS and submitted (ACTIVE) alphas against BRAIN, appends results to the checks_history time-series table, and flags any alpha whose Sharpe or fitness has dropped more than the threshold (default 15%) vs the previous check. Prints a CLI summary table. Writes alpha-kb/Decay.md. Requires ≥2 history points per alpha to flag degradation — freshly-checked alphas are "no data", not "degraded". Run on demand; not folded into /hunt.
    - Invocation: python decay.py [--db alpha_kb.db] [--threshold 0.15]
    - Auth note: requires a live BRAIN session.

    mkdir commands: `mkdir -p /Users/winter.__.kor/quant/.claude/skills/optimize` and `/Users/winter.__.kor/quant/.claude/skills/decay`.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && ls .claude/skills/optimize/SKILL.md .claude/skills/decay/SKILL.md && grep -c "python optimize.py" .claude/skills/optimize/SKILL.md && grep -c "python decay.py" .claude/skills/decay/SKILL.md</automated>
  </verify>
  <done>
    .claude/skills/optimize/SKILL.md exists and contains "python optimize.py".
    .claude/skills/decay/SKILL.md exists and contains "python decay.py".
    Both files describe the command purpose, invocation, and auth requirement.
    No modification to existing skill files (/hunt, /iterate, /find-alphas).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 modified grade.py + db.py (settings param + checks_history + note_path migration).
    Plan 02 wrote test_phase4.py with 12 unit tests covering all 3 OPT requirements.
    Plan 03 created optimizer.py (ARCHETYPE_HEURISTICS + build_variants + run_optimize) + optimize.py CLI.
    Plan 04 created decay_monitor.py (detect_decay + run_decay) + decay.py CLI.
    Plan 05 created obsidian.py (regen_archetype_notes + regen_failure_notes + write_decay_note + regen_all).
    Plan 06 Task 1 created /optimize and /decay skill files.

    Automated test run: pytest test_phase4.py test_phase3.py -v
  </what-built>
  <how-to-verify>
    1. Run the full test suite:
       cd ~/quant && pytest test_phase4.py test_phase3.py -v

       Expected: ALL tests green. 12 Phase 4 tests + Phase 3 tests, zero failures.

    2. Check the three ROADMAP success criteria manually:

       Criterion 1 (OPT-01): python -c "import sqlite3, db, optimizer; conn = db.init_db(':memory:'); row = {'alpha_id':'x','expression':'rank(returns)','archetype':'reversal','decay':15,'neutralization':'SUBINDUSTRY','truncation':0.08}; v = optimizer.build_variants(row, conn); print(f'Variants: {len(v)} (expected 1-4)'); assert len(v) <= 4"

       Criterion 2 (OPT-02): python -c "import db, decay_monitor; conn = db.init_db(':memory:'); print(decay_monitor.detect_decay(conn, 'test'))"
       — should print {'status': 'no_data', 'metric': None} (correct — no history yet)

       Criterion 3 (OPT-03): python -c "import db, obsidian, researcher; from pathlib import Path; import tempfile; conn = db.init_db(':memory:'); tmp = Path(tempfile.mkdtemp()); paths = obsidian.regen_archetype_notes(conn, vault_root=tmp); print(f'Archetype notes: {len(paths)} (expected {len(researcher.ARCHETYPES)})')"
       — should print "Archetype notes: 8 (expected 8)"

    3. Check skill files exist:
       ls ~/.quant/.claude/skills/optimize/SKILL.md ~/quant/.claude/skills/decay/SKILL.md
       (adjust path to ~/quant if needed)

    4. Quick smoke test of imports (no BRAIN call):
       python -c "import optimizer, decay_monitor, obsidian; print('All Phase 4 modules import OK')"
  </how-to-verify>
  <resume-signal>Type "approved" if all tests green and 3 criteria verified, or describe any failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill SKILL.md → Claude Code CLI execution | Skill files invoke python scripts; no untrusted input flows through skill files themselves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Tampering | SKILL.md CLI args | accept | skill files describe CLI args; actual execution goes through shell; no arbitrary code injection surface beyond what already exists for /hunt |
| T-04-22 | Elevation of Privilege | /optimize sim budget | mitigate | SKILL.md documents --max-workers default 1; grade_many cap ≤3 enforced in optimizer.py (Plan 03) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | no new packages in Phase 4 |
</threat_model>

<verification>
Primary gate: cd /Users/winter.__.kor/quant && pytest test_phase4.py test_phase3.py -v
Expected output: all tests PASSED, 0 failed.

Secondary: python -c "import optimizer, decay_monitor, obsidian" exits 0.

Regression check: pytest test_phase3.py -v must be fully green (no Phase 3 regressions from grade.py modification).
</verification>

<success_criteria>
pytest test_phase4.py: all 12 tests green.
pytest test_phase3.py: all Phase 3 tests still green (no regressions from grade.py settings param addition).
pytest test_phase4.py test_phase3.py -v: combined suite fully green.
.claude/skills/optimize/SKILL.md and .claude/skills/decay/SKILL.md both exist.
Each skill file contains the invocation command (python optimize.py / python decay.py).
optimizer.build_variants returns ≤4 variants (asserted by test_build_variants_cap).
decay_monitor.detect_decay correctly handles no_data / degraded / stable (asserted by 3 tests).
obsidian.regen_archetype_notes creates 8 notes (asserted by test_archetype_notes_count).
</success_criteria>

<output>
Create `/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-06-SUMMARY.md` when done.
</output>
