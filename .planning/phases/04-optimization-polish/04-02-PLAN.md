---
phase: 04-optimization-polish
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - test_phase4.py
autonomous: true
requirements:
  - OPT-01
  - OPT-02
  - OPT-03

must_haves:
  truths:
    - "All 12 OPT-01/02/03 test cases exist in test_phase4.py and are structurally valid (importable)"
    - "Tests use in-memory SQLite — zero BRAIN API calls in any test"
    - "Tests can run against the modified db.py from Plan 01 and pass"
  artifacts:
    - path: "test_phase4.py"
      provides: "12 unit tests covering all 3 ROADMAP success criteria"
      contains: "test_build_variants_cap"
  key_links:
    - from: "test_phase4.py"
      to: "db.init_db(':memory:')"
      via: "pytest fixture conn"
      pattern: "init_db.*:memory:"
---

<objective>
Write the full test_phase4.py test suite covering all 12 requirement cases from the RESEARCH.md validation architecture table. Tests are written to run against modules that may not yet be fully implemented (optimizer.py, decay_monitor.py, obsidian.py) by using mocks where needed. The test file serves as the executable specification that Plans 03-05 must satisfy.

Purpose: Provides a failing-then-passing test target for each feature. Running `pytest test_phase4.py -x` at any point during Wave 2 shows exactly which capabilities are complete. Mirrors test_phase3.py pattern exactly.

Output: test_phase4.py with 12 passing tests after Plans 01-05 complete.
</objective>

<execution_context>
@/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-RESEARCH.md
@/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-PATTERNS.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/ROADMAP.md

<interfaces>
<!-- Extracted from codebase for executor reference -->

From test_phase3.py pattern (inline fixtures, no conftest.py):
  @pytest.fixture
  def conn():
      c = db.init_db(":memory:")
      yield c
      c.close()

From 04-RESEARCH.md Validation Architecture table (12 test names):
  test_build_variants_cap         — OPT-01: build_variants returns ≤4 dicts
  test_build_variants_no_self     — OPT-01: excludes current settings tuple
  test_optimizer_calls_grade_many — OPT-01: grade_many called with settings_map (mock)
  test_variant_lineage            — OPT-01: variant has parent_alpha_id = NEAR alpha
  test_decay_no_data              — OPT-02: detect_decay returns no_data with <2 history rows
  test_decay_degraded             — OPT-02: returns degraded when drop > threshold
  test_decay_stable               — OPT-02: returns stable when drop < threshold
  test_history_append_only        — OPT-02: two appends = two rows (never overwrites)
  test_archetype_notes_count      — OPT-03: regen_archetype_notes writes 1 file per archetype
  test_failure_notes_families     — OPT-03: regen_failure_notes groups by primary check name
  test_note_path_written          — OPT-03: alphas.note_path populated after regen
  test_wikilinks_in_notes         — OPT-03: note body contains [[alpha_id]] wikilinks

From 04-PATTERNS.md test_phase4.py section:
  append_checks_history test: inserts 2 rows with same alpha_id, asserts len==2
  mock grade_many: from unittest.mock import patch, MagicMock
</interfaces>
</context>

<feature>
  <name>test_phase4.py — full Phase 4 test suite</name>
  <files>test_phase4.py</files>
  <behavior>
    OPT-01 tests:
    - test_build_variants_cap: call optimizer.build_variants(alpha_row, conn) with an alpha_row that has archetype='reversal' and some (decay,neutralization,truncation) — assert len(result) <= 4
    - test_build_variants_no_self: include current (decay,neutralization,truncation) in alpha_row — assert none of the returned variant dicts match the current combo exactly
    - test_optimizer_calls_grade_many: patch grade.grade_many with a mock returning []; call optimizer.run_optimize(client=MagicMock(), conn=conn, db_path=':memory:'); assert grade_many mock was called
    - test_variant_lineage: insert a near-status alpha into conn; mock grade_many to return [{'status':'pass','alpha_id':'v1','sharpe':1.5}]; run optimizer; check that the mock was called with parent_map containing the near alpha's id

    OPT-02 tests:
    - test_decay_no_data: call decay_monitor.detect_decay(conn, 'a1') with no checks_history rows — assert result['status'] == 'no_data'
    - test_decay_degraded: insert 2 checks_history rows for 'a1' / 'LOW_SHARPE' — new_val=0.8, old_val=1.2 (drop=33%) — call detect_decay(conn, 'a1', threshold_pct=0.15) — assert result['status'] == 'degraded'
    - test_decay_stable: insert 2 rows — new_val=1.2, old_val=1.3 (drop=7.7%) — call detect_decay with threshold=0.15 — assert result['status'] == 'stable'
    - test_history_append_only: call append_checks_history twice for same alpha_id; assert SELECT COUNT(*) == 2

    OPT-03 tests:
    - test_archetype_notes_count: call obsidian.regen_archetype_notes(conn, tmp_path); assert len(returned paths) == len(researcher.ARCHETYPES) and all files exist
    - test_failure_notes_families: insert 3 fail alphas with different primary failing checks; call obsidian.regen_failure_notes(conn, tmp_path); assert 3 distinct note files created
    - test_note_path_written: call obsidian.regen_archetype_notes(conn, tmp_path); insert an alpha with archetype='reversal'; call regen; query alphas.note_path — assert it is not None
    - test_wikilinks_in_notes: insert a fail alpha; call regen_failure_notes; read one of the generated notes; assert '[[' in note_content

    All tests: use conn fixture (in-memory SQLite via db.init_db(':memory:')), mock any BRAIN API calls with MagicMock, use tmp_path pytest fixture for vault root in OPT-03 tests.
  </behavior>
  <implementation>
    Start with RED: write all 12 test functions. Many will fail initially because optimizer.py,
    decay_monitor.py, obsidian.py do not exist yet. This is expected — tests define the spec.

    The test file must be importable (no syntax errors) even before the feature modules exist.
    Use conditional imports or lazy imports inside test functions:
      import importlib, pytest
      optimizer = pytest.importorskip("optimizer")
    This lets the test file load even if optimizer.py does not exist yet (the test is skipped, not an error).

    After Plans 03-05 are complete, all 12 tests go GREEN without modification.

    Follow test_phase3.py style exactly: no conftest.py, inline fixtures, sqlite3 in-memory.
    Use tmp_path (built-in pytest fixture) for vault root in obsidian tests.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → in-memory SQLite | All DB access in tests uses :memory: — no disk writes, no production DB touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | test_phase4.py DB fixture | accept | in-memory SQLite only; no production data accessible from tests |
| T-04-08 | Information Disclosure | mock BRAIN client in tests | accept | MagicMock objects never make real HTTP calls; no credentials in test code |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | no new packages; pytest already in requirements |
</threat_model>

<verification>
After Plan 01 is complete and this plan's test file is written:
  cd /Users/winter.__.kor/quant && pytest test_phase4.py::test_history_append_only test_phase4.py::test_decay_no_data -x

After Plans 03-05 are complete:
  cd /Users/winter.__.kor/quant && pytest test_phase4.py -v
  (all 12 tests green)

After all plans including Plan 06:
  cd /Users/winter.__.kor/quant && pytest test_phase4.py test_phase3.py -v
  (full suite green)
</verification>

<success_criteria>
test_phase4.py is importable with no syntax errors.
12 test functions present, one per row in the RESEARCH.md validation table.
test_history_append_only and test_decay_no_data/degraded/stable pass after Plan 01 (these only depend on db.py).
All 12 tests pass after Plans 03, 04, 05 complete.
Zero BRAIN API calls made during any test run (confirmed by absence of requests.get/post calls in test output).
</success_criteria>

<output>
Create `/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-02-SUMMARY.md` when done.
</output>
