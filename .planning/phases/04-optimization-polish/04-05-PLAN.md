---
phase: 04-optimization-polish
plan: 05
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - obsidian.py
autonomous: true
requirements:
  - OPT-03

must_haves:
  truths:
    - "regen_archetype_notes creates exactly one .md file per archetype in alpha-kb/Archetypes/"
    - "regen_failure_notes creates one .md file per distinct primary-check-failure family in alpha-kb/Failures/"
    - "Each note contains [[alpha_id]] wikilinks for its associated alphas"
    - "alphas.note_path is updated in DB after each regen (two-way link DB→note)"
    - "regen_all() calls both regen functions plus write_decay_note stub in one call (used by optimizer.run_optimize as side-effect)"
    - "Note filenames are slugified (path-traversal-safe) — from find_alphas.slugify"
    - "write_decay_note(degraded_list, conn) writes/overwrites alpha-kb/Decay.md with current summary"
  artifacts:
    - path: "obsidian.py"
      provides: "regen_archetype_notes + regen_failure_notes + write_decay_note + regen_all"
      contains: "from find_alphas import slugify"
    - path: "alpha-kb/Archetypes/"
      provides: "one .md per archetype after /optimize"
      contains: "existing .gitkeep — needs content"
    - path: "alpha-kb/Failures/"
      provides: "one .md per failure family after /optimize"
      contains: "existing .gitkeep — needs content"
  key_links:
    - from: "obsidian.regen_archetype_notes"
      to: "alphas.note_path"
      via: "UPDATE alphas SET note_path=? WHERE archetype=?"
      pattern: "UPDATE alphas SET note_path"
    - from: "obsidian.regen_failure_notes"
      to: "checks table"
      via: "SELECT name FROM checks WHERE alpha_id=? AND result='FAIL'"
      pattern: "checks.*FAIL"
---

<objective>
Build obsidian.py: the Obsidian prose layer module for Archetype notes, Failure-family notes, and the Decay note. Also implements the two-way DB→note linking (alphas.note_path update) and the regen_all() composite function called by optimizer.run_optimize.

Purpose: Delivers OPT-03 end-to-end. Every thesis run, archetype, and failure family gets a human-readable Obsidian note linked back to alpha_ids in SQLite. Notes are regenerated deterministically from DB each run — no stale drift.

Output: obsidian.py (service module). No new CLI entrypoint — this module is imported by optimizer.py.
</objective>

<execution_context>
@/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-RESEARCH.md
@/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-PATTERNS.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/ROADMAP.md

<interfaces>
<!-- Signatures from live codebase — read before implementing -->

From find_alphas.py:
  VAULT_ROOT = Path(__file__).parent / "alpha-kb"
  THESES_DIR = VAULT_ROOT / "Theses"
  slugify(name: str) -> str   # strips path-traversal chars, returns alnum-safe string
  Lines 66-124: _build_frontmatter pattern (--- YAML delimiters, key:value)
  Lines 178-179: wikilink pattern — " ".join(f"[[{aid}]]" for aid in ids)
  Lines 432-433: note write pattern — mkdir(parents=True, exist_ok=True) + open/write
  Line 321: write_runs_row — post-write DB update pattern (analogous to note_path update)

From researcher.py:
  ARCHETYPES: list[str]  # 8 archetype labels — iterate for one note per archetype

From db.py (after Plan 01):
  init_db returns conn with note_path column on alphas table

From 04-RESEARCH.md Pattern 5:
  PRIORITY list for failure family: ["CONCENTRATED_WEIGHT","HIGH_TURNOVER","MATCHES_COMPETITION","LOW_SUB_UNIVERSE_SHARPE","LOW_FITNESS","LOW_SHARPE"]
  get_failure_family(alpha_id, conn): SELECT name FROM checks WHERE alpha_id=? AND result='FAIL'

From 04-RESEARCH.md Pattern 6:
  Archetype note template: frontmatter (title, archetype, updated, alpha_ids, tags) + 3 sections (Heuristics table, NEAR Alphas, PASS Alphas)

From 04-RESEARCH.md Pattern 7:
  regen_archetype_notes(conn, vault_root) -> list[str]
  regen_failure_notes(conn, vault_root) -> list[str]
  note_path DB update after write

From 04-RESEARCH.md Pitfall 5:
  Wikilinks are intentionally unresolved in Obsidian (no individual alpha notes yet); add comment in notes

From 04-RESEARCH.md Pitfall 6:
  Decay.md is a single file overwritten each run; full history is in checks_history
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: obsidian.py — Archetype notes + Failure notes + note_path update</name>
  <files>obsidian.py</files>
  <read_first>
    Read find_alphas.py in full — extract slugify (lines 42-63), VAULT_ROOT/THESES_DIR (lines 32-34), _build_frontmatter pattern (lines 66-124), wikilink pattern (lines 178-179), note write pattern (lines 432-433), write_runs_row DB update (line 321).
    Read researcher.py lines 21-30 for exact ARCHETYPES list.
    Read 04-RESEARCH.md Pattern 5 (failure family PRIORITY list and get_failure_family logic).
    Read 04-RESEARCH.md Pattern 6 (archetype note template — 3 sections).
    Read 04-RESEARCH.md Pattern 7 (regen functions + note_path update).
    Read 04-PATTERNS.md "obsidian.py" section (imports, vault layout constants, all patterns).
    Read 04-RESEARCH.md Pitfall 5 (wikilink unresolved comment) and Pitfall 6 (single Decay.md).
  </read_first>
  <behavior>
    - ARCHETYPES_DIR = VAULT_ROOT / "Archetypes"; FAILURES_DIR = VAULT_ROOT / "Failures"; DECAY_NOTE = VAULT_ROOT / "Decay.md"
    - PRIORITY = ["CONCENTRATED_WEIGHT","HIGH_TURNOVER","MATCHES_COMPETITION","LOW_SUB_UNIVERSE_SHARPE","LOW_FITNESS","LOW_SHARPE"]
    - get_failure_family(alpha_id, conn) -> str: queries checks table for FAIL rows, returns first match in PRIORITY order, "OTHER" if none
    - regen_archetype_notes(conn, vault_root=VAULT_ROOT) -> list[str]: one note per researcher.ARCHETYPES entry, written to vault_root/"Archetypes"/{slugify(archetype)}.md; after writing, UPDATE alphas SET note_path=? WHERE archetype=?; returns list of written paths
    - regen_failure_notes(conn, vault_root=VAULT_ROOT) -> list[str]: queries FAIL alphas, groups by get_failure_family, writes one note per family to vault_root/"Failures"/{slugify(family)}.md; after writing each, UPDATE alphas SET note_path=? WHERE alpha_id IN (...) for that family; returns list of written paths
    - Each note body: YAML frontmatter (---, title, archetype/family, updated date, alpha_ids list, tags, ---), then sections with markdown tables + wikilinks ([[alpha_id]])
    - Add comment "_Alpha IDs are referenced but do not have individual notes yet._" in each note (Pitfall 5)
    - Note files created with ARCHETYPES_DIR.mkdir(parents=True, exist_ok=True) before write
    - All SQL queries use ? parameterized form
  </behavior>
  <action>
    Create obsidian.py. Imports: re, sqlite3, json from stdlib; datetime from datetime; Path from pathlib; Optional, List from typing; db, researcher from project; slugify from find_alphas.

    Define vault layout constants using same _HERE = Path(__file__).parent pattern as find_alphas.py:
    VAULT_ROOT = _HERE / "alpha-kb"; ARCHETYPES_DIR = VAULT_ROOT / "Archetypes"; FAILURES_DIR = VAULT_ROOT / "Failures"; DECAY_NOTE = VAULT_ROOT / "Decay.md".

    Define PRIORITY list (6 check names in order per RESEARCH.md Pattern 5).

    Implement get_failure_family(alpha_id, conn) using parameterized SELECT from checks table.

    Implement _render_archetype_note(archetype, conn) -> str:
    - Query PASS alphas with this archetype: SELECT alpha_id, decay, neutralization, truncation, sharpe FROM alphas WHERE archetype=? AND status IN ('pass','ACTIVE') ORDER BY sharpe DESC
    - Query NEAR alphas: SELECT alpha_id, expression, sharpe FROM alphas WHERE archetype=? AND status='near'
    - Build frontmatter: ---\ntitle: {archetype} Archetype\narchetype: {archetype}\nupdated: {today}\nalpha_ids:\n  - {aid}\ntags: [archetype, alpha]\n---
    - Build 3 sections: "## Heuristics" (table of PASS settings + sharpe + [[alpha_id]]), "## NEAR Alphas (optimizer candidates)" (table), "## PASS Alphas" (table)
    - All tables include [[alpha_id]] wikilinks per 04-PATTERNS.md wikilink pattern
    - Append "_Alpha IDs are referenced but do not have individual notes yet._" footer

    Implement regen_archetype_notes(conn, vault_root=VAULT_ROOT) -> list[str]:
    - ARCHETYPES_DIR = vault_root / "Archetypes"; mkdir(parents=True, exist_ok=True)
    - For archetype in researcher.ARCHETYPES: render note, write to {slugify(archetype)}.md, update alphas.note_path, append path to written list
    - Return written

    Implement _render_failure_note(family, alpha_ids, conn) -> str:
    - Query expression, sharpe, archetype for each alpha_id in alpha_ids
    - Build frontmatter + "## Failing Check: {family}" header + table with alpha_id, expression snippet, sharpe + wikilinks
    - Add failure remediation hint per family (brief inline tip, not LLM-generated)

    Implement regen_failure_notes(conn, vault_root=VAULT_ROOT) -> list[str]:
    - FAILURES_DIR = vault_root / "Failures"; mkdir(parents=True, exist_ok=True)
    - Query distinct FAIL alpha_ids; group by get_failure_family
    - For each (family, alpha_ids): render note, write to {slugify(family)}.md, UPDATE alphas SET note_path=? WHERE alpha_id IN ({placeholders})
    - Return written paths
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import sqlite3, db, obsidian, researcher
from pathlib import Path
import tempfile, os

conn = db.init_db(':memory:')
with tempfile.TemporaryDirectory() as tmp:
    vault = Path(tmp)

    # archetype notes
    paths = obsidian.regen_archetype_notes(conn, vault_root=vault)
    assert len(paths) == len(researcher.ARCHETYPES), f'Expected {len(researcher.ARCHETYPES)} files, got {len(paths)}'
    for p in paths:
        assert Path(p).exists(), f'Note file missing: {p}'

    # failure notes — insert a FAIL alpha + check
    conn.execute(\"INSERT INTO alphas (alpha_id, expression, status) VALUES ('fail1','rank(returns)','fail')\")
    conn.execute(\"INSERT INTO checks (alpha_id, name, result, value, limit_val) VALUES ('fail1','LOW_SHARPE','FAIL',0.8,1.25)\")
    conn.commit()
    fail_paths = obsidian.regen_failure_notes(conn, vault_root=vault)
    assert len(fail_paths) >= 1, 'Expected at least 1 failure note'
    content = Path(fail_paths[0]).read_text()
    assert '[[' in content, 'Missing wikilink in failure note'

    print(f'obsidian.py archetype notes: {len(paths)}, failure notes: {len(fail_paths)} OK')
conn.close()
"</automated>
  </verify>
  <done>
    regen_archetype_notes creates exactly len(researcher.ARCHETYPES) files.
    All archetype note files exist under vault_root/Archetypes/.
    regen_failure_notes creates 1 file per distinct failure family for FAIL alphas.
    Each failure note contains [[alpha_id]] wikilinks.
    alphas.note_path is set in DB after regen (two-way link works).
    pytest test_phase4.py::test_archetype_notes_count passes.
    pytest test_phase4.py::test_failure_notes_families passes.
    pytest test_phase4.py::test_note_path_written passes.
    pytest test_phase4.py::test_wikilinks_in_notes passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: obsidian.py — write_decay_note + regen_all</name>
  <files>obsidian.py</files>
  <read_first>
    Read 04-RESEARCH.md Pitfall 6 (single Decay.md overwritten each run — full history in checks_history).
    Read 04-RESEARCH.md Pattern 7 for the regen_all function structure.
    Read 04-CONTEXT.md D-07 (output = CLI table + Obsidian note linked to affected alpha_ids).
    Read 04-CONTEXT.md D-11 (notes regen as side-effect of /optimize; /decay writes Decay.md).
    Read the existing Task 1 code in obsidian.py before adding to it (no re-read needed if Task 1 just completed).
  </read_first>
  <action>
    Add write_decay_note(degraded_list, conn, vault_root=VAULT_ROOT) to obsidian.py:
    - vault_root.mkdir(parents=True, exist_ok=True)
    - Build content: "# Decay Report\n\n_Last updated: {today}_\n\n_Alpha IDs are referenced but do not have individual notes yet._\n\n"
    - If degraded_list is empty: content += "No degraded alphas detected in the last run.\n"
    - Else: build a markdown table — headers: Alpha ID | Metric | Old Value | New Value | Drop % | Checked At — one row per degraded dict, with [[alpha_id]] wikilinks
    - Write content to DECAY_NOTE (overwrite — single file per D-10 philosophy).
    - Return str(DECAY_NOTE)

    Add regen_all(conn, vault_root=VAULT_ROOT) to obsidian.py:
    - Calls regen_archetype_notes(conn, vault_root)
    - Calls regen_failure_notes(conn, vault_root)
    - Does NOT call write_decay_note (decay note is written by decay_monitor, not by /optimize)
    - Returns dict {"archetype_notes": [...paths], "failure_notes": [...paths]}

    This completes obsidian.py as a standalone service module. All functions are importable by optimizer.py (regen_all) and decay_monitor.py (write_decay_note).
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import obsidian, inspect
# confirm all four public functions exist
for fn in ('regen_archetype_notes','regen_failure_notes','write_decay_note','regen_all'):
    assert hasattr(obsidian, fn), f'{fn} missing from obsidian.py'

# test regen_all returns dict with expected keys
import sqlite3, db
from pathlib import Path
import tempfile
conn = db.init_db(':memory:')
with tempfile.TemporaryDirectory() as tmp:
    result = obsidian.regen_all(conn, vault_root=Path(tmp))
    assert 'archetype_notes' in result
    assert 'failure_notes' in result

# test write_decay_note creates Decay.md
with tempfile.TemporaryDirectory() as tmp:
    degraded = [{'alpha_id':'a1','metric':'LOW_SHARPE','old_value':1.3,'new_value':0.9,'drop_pct':0.31,'checked_at':'2026-06-11T00:00:00'}]
    path = obsidian.write_decay_note(degraded, conn, vault_root=Path(tmp))
    content = Path(path).read_text()
    assert '[[a1]]' in content, 'Wikilink missing in Decay.md'
    assert 'LOW_SHARPE' in content

print('obsidian.py regen_all + write_decay_note OK')
conn.close()
"</automated>
  </verify>
  <done>
    obsidian.py exports regen_archetype_notes, regen_failure_notes, write_decay_note, regen_all.
    regen_all returns {"archetype_notes": [...], "failure_notes": [...]} without calling write_decay_note.
    write_decay_note creates/overwrites vault_root/Decay.md with degraded alpha table.
    Decay.md contains [[alpha_id]] wikilinks for degraded alphas.
    optimizer.py can call obsidian.regen_all(conn) as a side-effect without import error.
    decay_monitor.py can call obsidian.write_decay_note(degraded, conn) without import error.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| obsidian.py → filesystem write | Note filenames derived from archetype labels and check names; must go through slugify |
| alphas.note_path update | SQL UPDATE uses ? parameterized form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | note filename path traversal | mitigate | all filenames pass through find_alphas.slugify() — strips /, .., null bytes; archetype and check name labels are controlled constants, not user input |
| T-04-18 | Tampering | obsidian.py SQL UPDATE note_path | mitigate | UPDATE uses ? parameterized form; note_path value is a local filesystem path constructed from slugify output — not from user input |
| T-04-19 | Information Disclosure | alpha_ids in note files | accept | note files written to local alpha-kb/ vault only; no network exposure; alpha_ids are non-secret run identifiers |
| T-04-20 | Tampering | Decay.md overwrite | accept | intentional design (D-10 philosophy); full history in checks_history; no data loss |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | no new packages; pathlib/re/sqlite3 are stdlib |
</threat_model>

<verification>
cd /Users/winter.__.kor/quant && pytest test_phase4.py::test_archetype_notes_count test_phase4.py::test_failure_notes_families test_phase4.py::test_note_path_written test_phase4.py::test_wikilinks_in_notes -x
All 4 obsidian tests green.
python -c "import obsidian" — no import errors.
ls alpha-kb/Archetypes/ — confirms 8 .md files created (after a real /optimize run).
</verification>

<success_criteria>
regen_archetype_notes creates exactly 8 .md files (one per researcher.ARCHETYPES entry).
regen_failure_notes creates one .md per failure family (≥1 for current DB with 60 FAIL alphas).
All notes contain [[alpha_id]] wikilinks.
alphas.note_path is set in DB after regen — SELECT note_path FROM alphas WHERE archetype='reversal' returns a non-NULL path.
write_decay_note creates alpha-kb/Decay.md with markdown table of degraded alphas.
regen_all() callable from optimizer.run_optimize without error.
write_decay_note() callable from decay_monitor.run_decay without error.
</success_criteria>

<output>
Create `/Users/winter.__.kor/quant/.planning/phases/04-optimization-polish/04-05-SUMMARY.md` when done.
</output>
