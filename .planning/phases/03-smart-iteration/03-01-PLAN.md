---
phase: 03-smart-iteration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - editor.py
  - db.py
autonomous: true
requirements:
  - ITR-01
  - ITR-02

must_haves:
  truths:
    - "editor.classify_from_checks(alpha_id, conn) returns ('pass'|'near'|'fail', [failing_check_names]) for any IS-resolved alpha"
    - "NEAR is assigned only when ≤2 failing numeric checks all within 20% of limit AND no MATCHES_COMPETITION/CONCENTRATED_WEIGHT fail"
    - "editor.diagnose_and_mutate(alpha_id, conn) returns a dict with status, diagnosis string, and 1-3 validated mutation expressions"
    - "Every mutation expression passes validate.validate(conn, expr) and db.expr_exists(conn, expr) before inclusion — invalids dropped silently"
    - "Every mutation stub is PRE-INSERTED into alphas with parent_alpha_id=source_alpha_id and status='queued' BEFORE diagnose_and_mutate returns -- parent_alpha_id is set at insert time, never patched afterward"
    - "diagnose_and_mutate writes the diagnosis string to alphas.diagnosis for the source alpha_id via parameterized UPDATE"
    - "db.py _DDL includes diagnosis TEXT column; init_db() applies ALTER TABLE ... ADD COLUMN diagnosis TEXT idempotently if column absent"
    - "PENDING checks are excluded from classification (Phase B not yet complete guard)"
  artifacts:
    - path: "editor.py"
      provides: "Hybrid Editor module — deterministic classification + LLM mutation proposal"
      exports:
        - "classify_from_checks"
        - "diagnose_and_mutate"
    - path: "db.py"
      provides: "diagnosis TEXT column added to alphas table schema"
      contains: "diagnosis TEXT"
  key_links:
    - from: "editor.classify_from_checks"
      to: "checks table"
      via: "parameterized SELECT WHERE result != 'PENDING'"
      pattern: "result != .PENDING"
    - from: "editor.diagnose_and_mutate"
      to: "validate.validate + db.expr_exists"
      via: "mutation validation gate"
      pattern: "validate.validate.*expr_exists"
    - from: "editor.diagnose_and_mutate"
      to: "alphas.diagnosis"
      via: "parameterized UPDATE alphas SET diagnosis=? WHERE alpha_id=?"
      pattern: "UPDATE alphas SET diagnosis"
    - from: "editor.diagnose_and_mutate pre-insert"
      to: "alphas.parent_alpha_id"
      via: "db.upsert_alpha with parent_alpha_id=alpha_id, status='queued' before return"
      pattern: "status.*queued"
---

<objective>
Build editor.py — the hybrid Editor module that deterministically classifies each
graded alpha as PASS/NEAR/FAIL by reading the checks table (D-01, D-05..D-07) and
proposes 1-3 LLM-authored mutation expressions with parent_alpha_id lineage (D-02, D-03).

Purpose: Closes the grade → diagnose → mutate arc of the iteration loop. The
deterministic tier implements the NEAR classification algorithm verified in
03-RESEARCH.md. The LLM tier writes the human-readable "why" and proposes mutations
that are gated through validate.py + db.expr_exists before acceptance.

Output: editor.py with classify_from_checks(), diagnose_and_mutate(),
and helpers; ready to be imported by grade.py and hunt.py in later waves.
</objective>

<execution_context>
@/Users/winter.__.kor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/winter.__.kor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/ROADMAP.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-CONTEXT.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-RESEARCH.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-PATTERNS.md
@/Users/winter.__.kor/quant/researcher.py
@/Users/winter.__.kor/quant/grade.py
@/Users/winter.__.kor/quant/db.py
@/Users/winter.__.kor/quant/validate.py
@/Users/winter.__.kor/quant/ideator.py
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From db.py:
  expr_exists(conn: sqlite3.Connection, expression: str) -> Optional[str]
    Returns existing alpha_id if expression already in DB, else None.
  upsert_alpha(conn: sqlite3.Connection, alpha_dict: dict) -> None
    alphas table columns relevant to mutations:
      alpha_id, expression, parent_alpha_id, archetype, status, run_id, created_at
      (all others can be None for mutation stubs — they get filled when graded)

From validate.py:
  validate(conn: sqlite3.Connection, expression: str) -> tuple[bool, str]
    Returns (True, "") if valid, (False, reason) if not.

From grade.py — checks table populated by upsert_checks():
  checks columns: alpha_id, name, result, value, limit_val
  result values seen: "PASS", "FAIL", "PENDING"
  PENDING rows = Phase B not yet resolved (SELF_CORRELATION, PROD_CORRELATION)

From researcher.py — hybrid pattern to mirror:
  build_thesis(conn, archetype=None) -> dict
  Internal structure: deterministic DB reads assemble context dict,
  then LLM subagent (Claude tool_use / subprocess call) writes prose on top.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic classification core (classify_from_checks)</name>
  <files>editor.py</files>
  <read_first>
    - 03-RESEARCH.md §Check Classification Research and §Code Examples (lines 606-638, 688-714) — the verified NEAR algorithm, HARD_FAIL_CHECKS frozenset, EPSILON=0.01 denominator floor
    - 03-RESEARCH.md Pitfall 1 (LOW_SUB_UNIVERSE_SHARPE near-zero limit) and Pitfall 2 (SELF_CORRELATION PENDING)
    - 03-PATTERNS.md §editor.py — deterministic classification core pattern (lines 48-80)
    - researcher.py lines 1-82 — module docstring + imports pattern to mirror
    - grade.py lines 106-116 — dedup/validate pattern; status vocabulary context
  </read_first>
  <behavior>
    - classify_from_checks("alpha_near_sharpe", conn) returns ("near", ["LOW_SHARPE"])
      when LOW_SHARPE value is 4% below limit, no hard fails, no other numeric fails
    - classify_from_checks("alpha_hard_fail", conn) returns ("fail", ["MATCHES_COMPETITION"])
      immediately when MATCHES_COMPETITION result="FAIL" regardless of numeric checks
    - classify_from_checks("alpha_pending", conn) returns ("pass", [])
      when only PENDING checks exist (Phase B incomplete — resolved=[])
    - classify_from_checks("alpha_too_many_fails", conn) returns ("fail", [...])
      when 3 numeric checks fail even if all within 20% margin (D-07 cap = 2)
    - classify_from_checks("alpha_concentrated", conn) returns ("fail", ["CONCENTRATED_WEIGHT"])
      per D-06: CONCENTRATED_WEIGHT is a hard fail regardless of numeric proximity
  </behavior>
  <action>
    Create editor.py. Start with module docstring (mirrors researcher.py header style),
    then imports: sqlite3, typing.Optional, db, validate.

    Define module-level constants:
      HARD_FAIL_CHECKS = frozenset({"MATCHES_COMPETITION", "CONCENTRATED_WEIGHT"})
      EPSILON = 0.01  (denominator floor — prevents division-by-zero for near-zero limit_val)

    Implement classify_from_checks(alpha_id: str, conn: sqlite3.Connection) -> tuple[str, list[str]]:
    - Parameterized SELECT name, result, value, limit_val FROM checks WHERE alpha_id=?
    - Filter to resolved = [(n, r, v, l) for ... if r != "PENDING"] (per D-05 note in RESEARCH Pitfall 2)
    - If any resolved row has result="FAIL" and name in HARD_FAIL_CHECKS: return ("fail", [name]) immediately (D-06)
    - Collect numeric_fails: rows where result="FAIL" and val is not None and lim is not None;
      compute gap = abs(val - lim) / max(abs(lim), EPSILON)
    - If no numeric_fails: return ("pass", [])
    - If len(numeric_fails) <= 2 AND all(gap <= 0.20 for _, gap in numeric_fails): return ("near", [names])  (D-05, D-07)
    - Otherwise: return ("fail", [names])

    The function signature, constant names, and EPSILON value must match the verified
    algorithm in 03-RESEARCH.md §Code Examples exactly. Use parameterized SQL — never
    string-interpolation (per 03-PATTERNS.md §Parameterized SQL).
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -m pytest test_phase3.py::test_criterion_1_near_classification -x -q 2>/dev/null || python -c "
import sqlite3, db, editor, tempfile, os, shutil
tmpdir = tempfile.mkdtemp()
tmp_db = os.path.join(tmpdir, 'test.db')
shutil.copy('alpha_kb.db', tmp_db)
conn = db.init_db(tmp_db)
# Insert a NEAR case: LOW_SHARPE value=1.22, limit=1.25 → gap=0.024 ≤ 0.20, 1 fail → near
conn.execute('INSERT OR IGNORE INTO alphas(alpha_id,expression,status) VALUES(?,?,?)', ('TEST01','ts_rank(close,5)','fail'))
conn.execute('INSERT OR REPLACE INTO checks VALUES(?,?,?,?,?)', ('TEST01','LOW_SHARPE','FAIL',1.22,1.25))
conn.commit()
status, fails = editor.classify_from_checks('TEST01', conn)
assert status == 'near', f'Expected near got {status}'
assert 'LOW_SHARPE' in fails
print('classify_from_checks: PASS')
conn.close()
"
    </automated>
  </verify>
  <done>
    editor.py exists. classify_from_checks returns "near" for a single numeric fail within 20% margin,
    "fail" for MATCHES_COMPETITION regardless of numeric checks, "pass" for all-PENDING,
    "fail" for 3 numeric fails within margin (cap exceeded), "fail" for CONCENTRATED_WEIGHT.
    HARD_FAIL_CHECKS and EPSILON constants defined at module level.
  </done>
</task>

<task type="auto">
  <name>Task 2: LLM mutation tier (diagnose_and_mutate) with lineage</name>
  <files>editor.py</files>
  <read_first>
    - 03-PATTERNS.md §editor.py — full pattern including LLM tier (lines 82-116): DB query pattern, mutation validation gate, error handling
    - 03-RESEARCH.md §Architecture Patterns Pattern 1 (lines 322-347): diagnose_and_mutate function signature and structure
    - 03-CONTEXT.md D-01, D-02, D-03, D-04 — locked constraints: 1-3 mutations, validate+dedup gate, invalids dropped silently, parent_alpha_id lineage
    - researcher.py lines 230-280 — build_thesis LLM call pattern (context assembly before LLM invocation)
    - ideator.py lines 424-431 — queueable() validation gate pattern to mirror exactly
    - db.py lines 73-84 — upsert_alpha signature; note parent_alpha_id column
    - db.py lines 15-43 — _DDL list and init_db pattern; understand how to add diagnosis TEXT column idempotently
  </read_first>
  <action>
    Add to editor.py (after classify_from_checks):

    Private helper _build_editor_context(alpha_row, checks_rows, failing_checks, status) -> str:
    - Assembles a structured plain-text context block for the LLM containing:
      the expression, sharpe/fitness/turnover values, the status classification,
      each failing check name + value + limit, and an explicit request for:
      (a) a one-sentence human-readable diagnosis explaining WHY it failed
      (b) 1-3 mutation expressions (listed one per line) that might pass the failing checks
    - The prompt MUST include the avoid_motifs list if provided (for FSA steer per D-15):
      add optional avoid_motifs: list[str] = None parameter; if non-empty, append an
      "Avoid these structural motifs: ..." line to the context.
    - No fenced code blocks in the action; prompt wording is Claude's discretion (03-CONTEXT.md).

    Implement diagnose_and_mutate(alpha_id: str, conn: sqlite3.Connection, avoid_motifs: list[str] = None) -> dict:
    - Call classify_from_checks(alpha_id, conn); if status == "pass", return early with no mutations
    - Read alpha row: parameterized SELECT alpha_id, expression, sharpe, fitness, turnover, status
    - Read resolved checks: SELECT name, result, value, limit_val WHERE alpha_id=? AND result!='PENDING'
    - Call _build_editor_context(alpha_row, checks_rows, failing_checks, status, avoid_motifs)
    - LLM call: invoke a Claude subagent (same subprocess/tool_use pattern as researcher.py build_thesis)
      passing the context; expect JSON response with keys "diagnosis" (str) and "mutations" (list[str])
    - Validation gate (D-03, mirror ideator.py lines 424-431):
        valid_mutations = []
        for each proposed expression:
          ok, _ = validate.validate(conn, expr)
          if not ok: continue
          if db.expr_exists(conn, expr) is not None: continue
          valid_mutations.append(expr)
    - PRE-INSERT each valid mutation into alphas via db.upsert_alpha BEFORE returning (BLOCKER 2 fix):
        parent_alpha_id = alpha_id  (lineage — D-02, set at insert time, never patched afterward)
        status = "queued"  (distinguishes un-graded mutations from graded pass/fail rows)
        created_at = utcnow
        all other fields None (filled when graded)
      This pre-insert means grade_many will find the existing row by db.expr_exists; the
      parent_alpha_id is already set — no post-hoc update is ever needed or correct.
    - Persist diagnosis string to the SOURCE alpha's DB row:
        conn.execute("UPDATE alphas SET diagnosis=? WHERE alpha_id=?", (diagnosis_str, alpha_id))
        conn.commit()
      This satisfies ROADMAP criterion 1 ("every alpha carries a human-readable diagnosis").
    - Return dict: {"alpha_id": alpha_id, "status": status, "diagnosis": diagnosis_str, "mutations": [expr for pre-inserted stubs]}

    Error handling (mirror grade.py lines 79-88):
      If LLM call raises HTTPError with status 401: re-raise (propagate auth expiry — CLAUDE.md).
      Any other exception: return {"alpha_id": alpha_id, "status": status, "diagnosis": None, "mutations": []}.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import editor, inspect
src = inspect.getsource(editor)
assert 'def classify_from_checks' in src
assert 'def diagnose_and_mutate' in src
assert 'parent_alpha_id' in src, 'lineage column must be set'
assert 'validate.validate' in src, 'mutation gate missing'
assert 'expr_exists' in src, 'dedup gate missing'
assert '401' in src, '401 propagation missing'
assert "diagnosis" in src, 'diagnosis column write missing'
assert "queued" in src, 'pre-insert status=queued missing'
assert "UPDATE alphas SET diagnosis" in src, 'diagnosis UPDATE missing'
print('editor.py structure: PASS')
"
    </automated>
  </verify>
  <done>
    editor.diagnose_and_mutate exists, calls classify_from_checks, assembles LLM context,
    gates every proposed mutation through validate.validate + db.expr_exists,
    PRE-INSERTS each mutation stub with parent_alpha_id=source_alpha_id and status="queued" before returning,
    writes diagnosis string to alphas.diagnosis for the source alpha via UPDATE,
    and propagates 401 without catching it.
    Returns dict with alpha_id, status, diagnosis, mutations keys.
  </done>
</task>


<task type="auto">
  <name>Task 3: db.py — add diagnosis TEXT column (idempotent migration)</name>
  <files>db.py</files>
  <read_first>
    - db.py lines 15-43 — _DDL list and init_db() pattern: understand how CREATE TABLE IF NOT EXISTS and PRAGMA busy_timeout are used
    - db.py lines 55-70 — init_db() applies each DDL statement in order; no separate migration step exists
  </read_first>
  <action>
    Modify db.py to add a diagnosis TEXT column to the alphas table:

    1. Add "diagnosis TEXT" to the CREATE TABLE IF NOT EXISTS alphas DDL in _DDL (line ~24,
       insert after "pnl_path TEXT," or just before "status TEXT"):
       The column stores the human-readable diagnosis string from editor.diagnose_and_mutate.
       Nullable (NULL for alphas graded before Phase 3 or that are PASS with no diagnosis needed).

    2. Add _ALPHA_COLS entry: insert "diagnosis" into _ALPHA_COLS list after "pnl_path" so
       upsert_alpha can write diagnosis values via the existing INSERT OR REPLACE path.

    3. Add an idempotent ALTER TABLE migration in init_db() AFTER the existing DDL loop:
       Try to add the column; ignore OperationalError if it already exists (SQLite raises
       "duplicate column name" for ALTER TABLE ADD COLUMN on an existing column):

         try:
             conn.execute("ALTER TABLE alphas ADD COLUMN diagnosis TEXT")
             conn.commit()
         except sqlite3.OperationalError:
             pass  # Column already exists (idempotent)

       This handles the case where alpha_kb.db was created by Phase 1/2 init_db() before
       this column existed. The ALTER TABLE is SQLite-idempotent via the try/except pattern.

    CRITICAL: Do NOT change the primary key, existing column order, or any other DDL.
    The diagnosis column is append-only — existing rows get NULL for diagnosis, which is correct.
  </action>
  <verify>
    <automated>python -c "
import db, tempfile, os
tmp = tempfile.mktemp(suffix='.db')
conn = db.init_db(tmp)
# Check column exists
cols = [row[1] for row in conn.execute('PRAGMA table_info(alphas)').fetchall()]
assert 'diagnosis' in cols, f'diagnosis column missing; columns: {cols}'
# Check upsert_alpha accepts diagnosis
db.upsert_alpha(conn, {'alpha_id': 'TEST_DIAG', 'expression': 'ts_rank(close,5)', 'status': 'pass', 'diagnosis': 'test diagnosis'})
row = conn.execute('SELECT diagnosis FROM alphas WHERE alpha_id=?', ('TEST_DIAG',)).fetchone()
assert row[0] == 'test diagnosis', f'Expected test diagnosis got {row[0]}'
conn.close()
os.unlink(tmp)
print('db.py diagnosis column: PASS')
" && python -c "import db; print('db.py imports OK')"
    </automated>
  </verify>
  <done>
    db.py _DDL CREATE TABLE includes diagnosis TEXT column. _ALPHA_COLS includes "diagnosis".
    init_db() applies ALTER TABLE ADD COLUMN diagnosis TEXT with try/except OperationalError
    so existing databases are upgraded idempotently on next init_db() call.
    upsert_alpha can write diagnosis values. PRAGMA table_info(alphas) shows diagnosis column.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → DB insert | LLM proposes mutation expressions; these cross into the DB/queue |
| LLM output → grading queue | Mutations queue for BRAIN simulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | LLM-proposed mutation expressions | mitigate | Every expression gated through validate.validate(conn, expr) + db.expr_exists before DB insert (D-03 LOCKED). validate.py checks against live catalog — unknown operators/fields rejected. |
| T-03-02 | Elevation of Privilege | diagnose_and_mutate calling login/re-auth | mitigate | Function signature has no login() call; 401 propagates immediately rather than retrying (CLAUDE.md single-shot auth constraint enforced in error handler). |
| T-03-03 | Information Disclosure | SQL injection via alpha_id or expression strings | mitigate | All SQL uses parameterized queries (?, ...) — no string interpolation (03-PATTERNS.md §Parameterized SQL). |
| T-03-SC | Tampering | No new npm/pip/cargo installs | accept | No new packages; all stdlib. Package legitimacy gate not applicable. |
</threat_model>

<verification>
Run after plan execution:
  cd /Users/winter.__.kor/quant && python -c "import editor; print('editor.py imports OK')"
  cd /Users/winter.__.kor/quant && python -c "
import editor
assert hasattr(editor, 'classify_from_checks')
assert hasattr(editor, 'diagnose_and_mutate')
assert hasattr(editor, 'HARD_FAIL_CHECKS')
assert hasattr(editor, 'EPSILON')
print('All exports present')
"
</verification>

<success_criteria>
1. editor.py exists and imports cleanly in the project venv.
2. classify_from_checks implements HARD_FAIL_CHECKS, EPSILON, PENDING-filter, NEAR D-05..D-07 logic exactly.
3. diagnose_and_mutate PRE-INSERTS mutation stubs with parent_alpha_id=source_alpha_id and status="queued" before returning; writes diagnosis to alphas.diagnosis for the source alpha; propagates 401.
4. db.py adds diagnosis TEXT column to _DDL + _ALPHA_COLS + idempotent ALTER TABLE migration in init_db().
5. Function signatures match what hunt.py (plan 05) and grade.py (plan 04) will call.
</success_criteria>

<output>
Create `.planning/phases/03-smart-iteration/03-01-SUMMARY.md` when done.
</output>
