---
phase: 03-smart-iteration
plan: 06
type: execute
wave: 3
depends_on:
  - 03-05
files_modified:
  - test_phase3.py
autonomous: true
requirements:
  - ITR-01
  - ITR-02
  - ITR-03
  - ITR-04

must_haves:
  truths:
    - "test_phase3.py exits 0 with all 4 criterion tests passing"
    - "ZERO grade/simulate/login calls in the test suite"
    - "Criterion 1: editor.classify_from_checks correctly classifies NEAR (within 20%, ≤2 fails), FAIL (hard fail / >2 fails / >20% gap), PASS (no fails or all-PENDING)"
    - "Criterion 1 also asserts: after diagnose_and_mutate, alphas.diagnosis is non-empty (not NULL) for the classified alpha (WARNING 2 — ROADMAP criterion 1 persistence)"
    - "Criterion 2: editor.diagnose_and_mutate pre-inserts mutation stubs with parent_alpha_id set at insert time (status=queued); confirmed by DB query — no post-hoc update required"
    - "Criterion 3: selfcorr max_pearson correctly identifies a near-identical PnL series as above the cutoff; load_returns returns daily differences"
    - "Criterion 4: fsa.mine_frequent_motifs returns [] below min_samples; fsa.filter_candidates drops candidates with known-frequent motifs"
    - "Criterion 4 also asserts: before/after fsa.diversity_metric comparison produces a dict with top_motif_share for both snapshots (measurable diversity per ROADMAP criterion 4)"
  artifacts:
    - path: "test_phase3.py"
      provides: "Machine-verifiable Phase 3 criterion tests — no sim/login calls"
      contains: "test_criterion_1_near_classification"
  key_links:
    - from: "test_phase3.py"
      to: "editor, selfcorr, fsa"
      via: "direct imports + temp DB fixture"
      pattern: "def test_criterion_"
---

<objective>
Build test_phase3.py — machine-verifiable tests for all 4 ROADMAP Phase 3 success criteria.
Zero BRAIN API or login calls. Uses temp DB copies and synthetic data exactly like test_phase2.py.

Purpose: Provides the automated acceptance gate that the orchestrator uses to verify
Phase 3 is complete before marking it done.
Output: test_phase3.py passing cleanly under pytest.
</objective>

<execution_context>
@/Users/winter.__.kor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/winter.__.kor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/ROADMAP.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-CONTEXT.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-RESEARCH.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-05-SUMMARY.md
@/Users/winter.__.kor/quant/test_phase2.py
@/Users/winter.__.kor/quant/editor.py
@/Users/winter.__.kor/quant/selfcorr.py
@/Users/winter.__.kor/quant/fsa.py
@/Users/winter.__.kor/quant/db.py
@/Users/winter.__.kor/quant/validate.py
</context>

<interfaces>
<!-- What test_phase3.py tests — contracts from Phase 3 modules -->

editor.classify_from_checks(alpha_id, conn) -> tuple[str, list[str]]
editor.diagnose_and_mutate(alpha_id, conn, avoid_motifs=None) -> dict
  mutations key: list[str] — each already validated and inserted with parent_alpha_id

selfcorr.load_returns(pnl_path) -> list[float]
selfcorr._pearson(x, y) -> float   (private but testable directly)
selfcorr.max_pearson(candidate_path, reference_paths) -> float
selfcorr.get_selfcorr_limit(conn) -> Optional[float]

fsa.extract_abstract_subtrees(expr) -> list[str]
fsa.mine_frequent_motifs(conn, threshold, min_samples) -> list[str]
fsa.filter_candidates(candidates, avoid_motifs) -> list[dict]
fsa.diversity_metric(conn) -> dict

ROADMAP Phase 3 success criteria (what must be machine-verifiable):
  1. Every alpha in SQLite carries PASS/NEAR/FAIL status + human-readable diagnosis
  2. NEAR/FAIL alphas produce at least one mutation with parent_alpha_id set
  3. Local PnL-based self-corr pre-filter eliminates known-duplicate candidates without BRAIN API call
  4. FSA mines motifs from passing alphas; Ideator prompt excludes them; diversity metric available
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: test_phase3.py — all 4 criterion tests</name>
  <files>test_phase3.py</files>
  <read_first>
    - test_phase2.py (full file) — temp DB fixture pattern, module-level patches, assert message style, test function naming, "ZERO grade/simulate/login calls" guarantee
    - ROADMAP.md Phase 3 Success Criteria (exactly 4 items)
    - 03-RESEARCH.md §Check Classification Research (lines 213-275) — exact NEAR/FAIL test cases with real DB values
    - 03-RESEARCH.md Pitfall 1 (LOW_SUB_UNIVERSE_SHARPE near-zero limit) and Pitfall 2 (PENDING checks)
    - selfcorr.py (plan 02) public API — load_returns, max_pearson, _pearson
    - fsa.py (plan 03) public API — mine_frequent_motifs, filter_candidates, diversity_metric
    - editor.py (plan 01) — classify_from_checks, diagnose_and_mutate
  </read_first>
  <action>
    Create test_phase3.py. Module docstring lists all 4 criteria being tested.
    Imports: os, shutil, sys, tempfile, json, math, pathlib.Path, db, editor, fsa, selfcorr.

    Fixture helper _temp_db() -> tuple[str, sqlite3.Connection]:
    - Copy alpha_kb.db to tempfile, return (tmp_path, conn)
    - Mirror test_phase2.py fixture pattern exactly

    test_criterion_1_near_classification():
    - Insert synthetic alpha row (expression='ts_rank(close,5)', status='fail')
    - Insert checks rows:
        Case A: LOW_SHARPE value=1.22, limit=1.25 → gap=0.024 → NEAR (single fail within 20%)
        Case B: MATCHES_COMPETITION result='FAIL' → FAIL regardless of numeric checks
        Case C: LOW_SHARPE value=1.0, limit=1.25 → gap=0.20 → FAIL (exactly at boundary — D-05 says "within 20%" means gap <= 0.20, so 0.20 is a boundary; document which side)
        Case D: all checks result='PENDING' → PASS (Pitfall 2)
        Case E: LOW_SUB_UNIVERSE_SHARPE value=0.0, limit=0.0 → use EPSILON floor → not NEAR (Pitfall 1)
        Case F: 3 numeric fails each within 20% → FAIL (D-07 cap exceeded)
    - For each case: assert classify_from_checks returns expected (status, failing_checks)
    - Assert descriptive message includes case letter on failure
    - Diagnosis persistence check (WARNING 2 / ROADMAP criterion 1):
        Mock _call_llm_editor to return a fixed diagnosis for Case A (NEAR alpha)
        Call diagnose_and_mutate on the Case A alpha
        Query alphas table: assert alphas.diagnosis IS NOT NULL and diagnosis != "" for that alpha_id
        (This confirms the diagnosis string was persisted to DB, not just returned in the dict)

    test_criterion_2_mutation_lineage():
    - This test exercises the deterministic parts of diagnose_and_mutate only
    - Insert a NEAR alpha with LOW_SHARPE fail
    - Mock the LLM call in _call_llm_editor to return a fixed mutations list:
        [valid_expr_from_catalog, "invalid_expr_____UNKNOWN_OP(close,5)"]
    - Call diagnose_and_mutate; assert:
        returned dict has "mutations" key
        valid expressions appear in mutations list
        invalid expression (unknown op) NOT in mutations list (dropped by validate gate)
        Check DB: inserted mutation stub has parent_alpha_id = source alpha_id AND status = "queued"
          (pre-insert confirmed — lineage set at insert time, not patched post-grading)
    - Alternative if LLM mock is complex: test the validation gate directly:
        Insert candidate expression into alphas; call db.expr_exists to confirm dedup works;
        call validate.validate with an unknown-operator expression to confirm rejection;
        assert parent_alpha_id is a column in alphas table schema
        assert the queued stub row has parent_alpha_id set (not NULL)
    - Must NOT call grade/simulate/login

    test_criterion_3_pearson_prefilter():
    - Create two identical PnL JSON files (perfect correlation = 1.0)
    - Create one orthogonal PnL file (near-zero correlation)
    - Test max_pearson(identical_path, [identical_path]) returns value close to 1.0
    - Test max_pearson(orthogonal_path, [identical_path]) returns value < 0.5
    - Insert SELF_CORRELATION check row with limit_val=0.7; assert get_selfcorr_limit returns 0.7
    - Assert no BRAIN API calls were made (no requests.Session usage — test is pure compute)
    - Test load_returns: write synthetic JSON, assert returns list of floats with len = pnls-1

    test_criterion_4_fsa_mining():
    - Insert 6 PASS alpha rows all using "ts_rank(close,5)" expression
    - Call mine_frequent_motifs(conn, threshold=0.5, min_samples=5)
    - Assert "ts_rank(FIELD,NUM)" in result (appears in 6/6 = 100% of PASS alphas)
    - Call filter_candidates with that motif as avoid list
    - Assert ts_rank expression is dropped; a different expression (e.g. "rank(close)") is kept
    - Insert only 4 PASS alpha rows; assert mine_frequent_motifs returns [] (cold-start guard)
    - Diversity metric before/after comparison (WARNING 3 / ROADMAP criterion 4):
        snapshot_before = fsa.diversity_metric(conn)  (6 PASS alphas, all ts_rank → top_motif_share near 1.0)
        Insert 4 more PASS alphas with "rank(close)" expression (different motif)
        snapshot_after = fsa.diversity_metric(conn)   (10 PASS alphas, mixed motifs → lower top_motif_share)
        Assert snapshot_before["top_motif_share"] > snapshot_after["top_motif_share"]
          (top-motif concentration dropped after adding structurally diverse alphas — measurable diversity)
        Assert both snapshots contain "top_motif_share", "pass_alpha_count", "unique_motifs" keys
        Assert snapshot_after["pass_alpha_count"] == 10
    - Call diversity_metric on fresh DB with no PASS alphas; assert returns {"pass_alpha_count": 0, ...}

    if __name__ == "__main__":
        Run all test functions and print PASS/FAIL per criterion.
        Exit code 0 if all pass, 1 if any fail.
        Mirror test_phase2.py __main__ pattern.

    CRITICAL: All test data is synthetic — no calls to grade/simulate/login/client.
    Use only DB inserts + direct module function calls.
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -m pytest test_phase3.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    test_phase3.py runs under pytest with all 4 test functions passing (0 failures).
    No simulate/grade/login calls anywhere in the file.
    Each test function name starts with test_criterion_ and maps to a ROADMAP Phase 3 success criterion.
    Exit code 0 from pytest.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test DB writes | Synthetic rows inserted into temp copy of alpha_kb.db |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Tampering | Test writing to live alpha_kb.db | mitigate | All tests use tempfile.TemporaryDirectory() + shutil.copy pattern (from test_phase2.py). Live DB is read-only within tests. |
| T-03-19 | Information Disclosure | Test file calling login()/BRAIN API | mitigate | Test functions have no import of wq_login; no requests.Session usage. Verified by grep in acceptance check. |
| T-03-SC | Tampering | No new pip installs | accept | pytest already in venv (9.0.3). No new packages. |
</threat_model>

<verification>
Run after plan execution:
  cd /Users/winter.__.kor/quant && python -m pytest test_phase3.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR|passed|failed"
  cd /Users/winter.__.kor/quant && grep -c "login\|simulate\|grade_one\|grade_many" test_phase3.py || echo "0 forbidden calls found"
</verification>

<success_criteria>
1. pytest test_phase3.py exits 0 with all 4 test_criterion_* functions passing.
2. Zero calls to grade/simulate/login/client in test_phase3.py.
3. Criterion 1 covers: NEAR single-fail, hard-fail, PENDING-only, ≤20% boundary, >2-fails, EPSILON floor, AND diagnosis persistence (alphas.diagnosis non-empty after diagnose_and_mutate).
4. Criterion 2 asserts mutation stub has parent_alpha_id set at insert time (status=queued), confirmed by DB query.
5. Criterion 3 exercises max_pearson with synthetic PnL and asserts get_selfcorr_limit reads from DB (not hardcoded).
6. Criterion 4 exercises cold-start guard (< min_samples → empty list), motif filtering, AND before/after diversity_metric comparison showing measurable diversity change.
</success_criteria>

<output>
Create `.planning/phases/03-smart-iteration/03-06-SUMMARY.md` when done.
</output>
