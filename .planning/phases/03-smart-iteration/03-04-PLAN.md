---
phase: 03-smart-iteration
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - grade.py
  - find_alphas.py
autonomous: true
requirements:
  - ITR-01
  - ITR-03
  - ITR-04

must_haves:
  truths:
    - "grade.py status vocabulary includes 'near' and grade_one can write 'near' into alphas.status"
    - "grade_one calls selfcorr.proxy_gate before simulate() when parent_alpha_id is provided (Hook A)"
    - "grade_one calls selfcorr.fetch_and_cache_pnl + selfcorr.is_duplicate_by_pnl after IS survivor check, before trigger_correlation_check (Hook B)"
    - "A locally-duplicate candidate gets status='duplicate' in DB and trigger_correlation_check() is skipped"
    - "grade_one accepts parent_alpha_id as a parameter so mutations can set lineage"
    - "find_alphas.py calls fsa.mine_frequent_motifs + fsa.filter_candidates before queueable() to gate Ideator output"
    - "find_alphas.py passes the avoid-list to researcher.build_thesis so Researcher's LLM prompt gets the motif steer"
  artifacts:
    - path: "grade.py"
      provides: "Extended grader with NEAR status vocab + selfcorr hook A and hook B wired"
    - path: "find_alphas.py"
      provides: "Extended generation path with FSA filter + avoid-list injection into thesis"
  key_links:
    - from: "grade_one"
      to: "selfcorr.proxy_gate"
      via: "called before _simulate_to_alpha when parent_alpha_id is known"
      pattern: "selfcorr.proxy_gate"
    - from: "grade_one"
      to: "selfcorr.is_duplicate_by_pnl"
      via: "called after is_survivor, before trigger_correlation_check"
      pattern: "selfcorr.is_duplicate_by_pnl"
    - from: "find_alphas.find_alphas"
      to: "fsa.mine_frequent_motifs"
      via: "called after generate_candidates before queueable"
      pattern: "fsa.mine_frequent_motifs"
---

<objective>
Wire the three new modules (editor, selfcorr, fsa) into the existing grade.py and
find_alphas.py pipelines.

grade.py changes (ITR-01, ITR-03):
  1. Add 'near' to status vocabulary — grade_one now accepts parent_alpha_id param
     and writes it into the alpha row.
  2. Hook A: proxy_gate called pre-sim for mutations (avoids wasting sim slot).
  3. Hook B: precise selfcorr filter after IS survivor before Phase B BRAIN call.

find_alphas.py changes (ITR-04):
  1. Import fsa; call mine_frequent_motifs + filter_candidates before queueable.
  2. Pass avoid_motifs list into researcher.build_thesis for LLM steer.

Purpose: These are integration/wiring tasks — no new logic is created here.
All logic lives in editor.py, selfcorr.py, fsa.py (plans 01-03). The wiring
is surgical — existing behavior for non-mutation callers is unchanged.

Output: grade.py and find_alphas.py with all Phase 3 hooks active.
</objective>

<execution_context>
@/Users/winter.__.kor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/winter.__.kor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/ROADMAP.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-CONTEXT.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-RESEARCH.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-PATTERNS.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-01-SUMMARY.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-02-SUMMARY.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-03-SUMMARY.md
@/Users/winter.__.kor/quant/grade.py
@/Users/winter.__.kor/quant/find_alphas.py
@/Users/winter.__.kor/quant/researcher.py
@/Users/winter.__.kor/quant/ideator.py
</context>

<interfaces>
<!-- Contracts from plans 01-03 that are wired here -->

From selfcorr.py (plan 02):
  proxy_gate(parent_alpha_id: str, conn: sqlite3.Connection) -> bool
    True = parent too-correlated, skip sim. False = allow sim (also returns False on any error).
  fetch_and_cache_pnl(client, alpha_id: str, conn, pnl_dir="pnl_cache") -> Optional[str]
    Downloads PnL, writes JSON, updates pnl_path. Returns path or None.
  get_reference_pnl_paths(conn) -> list[str]
  get_selfcorr_limit(conn) -> Optional[float]
  is_duplicate_by_pnl(candidate_path, reference_paths, limit_val) -> bool

From fsa.py (plan 03):
  mine_frequent_motifs(conn, threshold=0.5, min_samples=5) -> list[str]
  filter_candidates(candidates: list[dict], avoid_motifs: list[str]) -> list[dict]

From researcher.py (existing):
  build_thesis(conn, archetype=None) -> dict
  Plan 04 will pass avoid_motifs to build_thesis — researcher.py build_thesis
  must accept an optional avoid_motifs parameter; add it if not already present.

From grade.py (current — being modified):
  grade_one(client, conn, expression, run_id) -> dict
  After modification: grade_one(client, conn, expression, run_id, parent_alpha_id=None) -> dict
  grade_many(client, conn, expressions, run_id, max_workers=1, db_path=None) -> list

From find_alphas.py (current — being modified):
  find_alphas(db_path, archetype=None, prose=None) -> dict
  Calls researcher.build_thesis, ideator.generate_candidates, then ideator.queueable
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: grade.py — NEAR status vocab + parent_alpha_id param + selfcorr hooks</name>
  <files>grade.py</files>
  <read_first>
    - grade.py lines 1-50 (imports) — add: import selfcorr at top
    - grade.py lines 91-248 (grade_one full function) — the two injection points (Hook A pre-sim line ~120; Hook B post-sim line ~200)
    - 03-PATTERNS.md §grade.py (lines 449-498): exact Hook A and Hook B code patterns with comments
    - 03-RESEARCH.md §Anti-Patterns (lines 522-528): "Calling POST /check for locally-filtered duplicates" — Hook B must skip trigger_correlation_check() entirely after local duplicate detection
    - 03-RESEARCH.md §Architecture Patterns (lines 93-103): Architectural Responsibility Map — "grade sets initial pass/fail; editor reclassifies to NEAR" clarifies that grade_one does NOT call classify_from_checks; that is the caller's job
    - 03-CONTEXT.md D-04: grade_many concurrency cap already enforced; do not change it
  </read_first>
  <action>
    Modify grade.py:

    1. Add import at top: import selfcorr

    2. Update grade_one signature to: grade_one(client, conn, expression, run_id, parent_alpha_id=None)
       - Default parent_alpha_id=None preserves backward compatibility for all existing callers

    3. In the alpha_dict construction (Step 6, around line 158-186):
       - Change "parent_alpha_id": None to "parent_alpha_id": parent_alpha_id
       This is the lineage write for mutations. CRITICAL: this is the only line change needed here.

    4. Hook A — pre-sim proxy gate (insert AFTER Step 0 dedup check, BEFORE Step 2 simulate,
       around line 117, between the is_valid check and _simulate_to_alpha call):
       Block: if parent_alpha_id is not None:
           if selfcorr.proxy_gate(parent_alpha_id, conn):
               print(f"[grade] proxy-gate filtered (parent {parent_alpha_id}): {expression[:40]}")
               return {"expression": expression, "status": "duplicate", "alpha_id": None}
       Note: graceful degrade is inside proxy_gate — if PnL unavailable it returns False (allow).
       Note: do NOT add a DB write here; status='duplicate' is returned but not persisted
       (the expression was never simulated, so there is no alpha_id to write against).

    5. Hook B — precise selfcorr filter (insert AFTER is_survivor check, BEFORE trigger_correlation_check,
       inside the "if is_survivor:" block, around line 200-201):
       Block:
         pnl_path = selfcorr.fetch_and_cache_pnl(client, alpha_id, conn)
         if pnl_path is not None:
             ref_paths = selfcorr.get_reference_pnl_paths(conn)
             limit_val = selfcorr.get_selfcorr_limit(conn)
             if limit_val is not None and selfcorr.is_duplicate_by_pnl(pnl_path, ref_paths, limit_val):
                 conn.execute("UPDATE alphas SET status='duplicate' WHERE alpha_id=?", (alpha_id,))
                 conn.commit()
                 print(f"[grade] local selfcorr duplicate: {alpha_id}")
                 status = "duplicate"
                 return {
                     "expression": expression, "alpha_id": alpha_id, "status": "duplicate",
                     "is_survivor": is_survivor, "sharpe": sharpe, "fitness": fitness,
                     "self_corr": None, "prod_corr": None, "checks": checks_raw,
                 }
         # Only reach trigger_correlation_check if NOT locally duplicate
         trigger_correlation_check(client, alpha_id)
         ...

       CRITICAL: the existing trigger_correlation_check call and all Phase B logic that follows
       must be preserved and still execute for non-duplicate IS survivors.

    6. _grade_isolated (inside grade_many, lines ~281-294) passes db_path for per-thread connections —
       do NOT change this. selfcorr hooks receive the worker's local conn, which is correct.

    All edits are surgical. Do NOT change MAX_CONCURRENT_SIMS, grade_many signature,
    poll_correlation, or trigger_correlation_check. Do NOT add 'near' logic inside grade_one —
    reclassification to near is done by editor.classify_from_checks after grading, not during.
    The status written by grade_one remains 'pass' or 'fail' as today; 'near' is a post-grading
    reclassification applied by the editor (per Architectural Responsibility Map).
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import grade, inspect
src = inspect.getsource(grade.grade_one)
assert 'selfcorr.proxy_gate' in src, 'Hook A missing'
assert 'selfcorr.fetch_and_cache_pnl' in src, 'Hook B fetch missing'
assert 'selfcorr.is_duplicate_by_pnl' in src, 'Hook B filter missing'
assert 'parent_alpha_id' in src, 'parent_alpha_id param missing'
# Check param default
import inspect as ins
sig = ins.signature(grade.grade_one)
assert 'parent_alpha_id' in sig.parameters, 'param not in signature'
print('grade.py modifications: PASS')
" && python -c "import grade; print('grade.py imports OK after modification')"
    </automated>
  </verify>
  <done>
    grade.py imports selfcorr. grade_one signature has parent_alpha_id=None. alpha_dict writes
    parent_alpha_id. Hook A skips sim when proxy_gate returns True. Hook B fetches PnL, sets
    status='duplicate' and returns early before trigger_correlation_check when locally duplicate.
    Existing behavior for non-mutation callers (parent_alpha_id=None) is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: find_alphas.py — FSA filter + avoid-list injection; researcher.py — avoid_motifs param</name>
  <files>find_alphas.py, researcher.py</files>
  <read_first>
    - find_alphas.py lines 360-436 (find_alphas function): the exact position after generate_candidates (line ~398) and before queueable (line ~424) where FSA filter inserts
    - find_alphas.py lines 1-40 (imports): add import fsa
    - 03-PATTERNS.md §find_alphas.py (lines 502-527): FSA hook injection patterns with exact comment markers
    - researcher.py lines 230-280 (build_thesis function): where to accept avoid_motifs and inject into the prompt/context
    - 03-CONTEXT.md D-15: "mined avoid-list is injected into the Researcher thesis prompt + Editor mutation prompt"; D-19: "FSA mines + filters inside the generation path"
  </read_first>
  <action>
    Modify find_alphas.py:

    1. Add import at top: import fsa

    2. In find_alphas() after step 2 (generate candidates, ~line 398) and BEFORE step 9 (queueable, ~line 424):
       Insert FSA hook:
         # [Phase 3 FSA hook — D-14, D-15, D-19]
         avoid_motifs = fsa.mine_frequent_motifs(conn)
         # Convert candidates to dicts with "expression" key for filter API
         cand_dicts = [{"expression": c.get("expression", ""), **c} for c in candidates]
         filtered_dicts = fsa.filter_candidates(cand_dicts, avoid_motifs)
         # Rebuild candidates list in original format (preserve all keys)
         candidates = filtered_dicts
         if avoid_motifs:
             print(f"[find_alphas] FSA filtered: {len(cand_dicts) - len(candidates)} candidates dropped ({len(avoid_motifs)} avoid motifs)")

    3. Pass avoid_motifs into build_thesis call:
       Change: thesis = researcher.build_thesis(conn, archetype=archetype)
       To:     thesis = researcher.build_thesis(conn, archetype=archetype, avoid_motifs=avoid_motifs)
       Note: avoid_motifs may not be defined yet at build_thesis call time (it's called before generate_candidates).
       Restructure order if needed: call mine_frequent_motifs BEFORE build_thesis so the avoid list is ready.
       Final order: mine_frequent_motifs → build_thesis(avoid_motifs) → generate_candidates → filter_candidates → queueable

    Modify researcher.py:

    4. Update build_thesis signature to accept: avoid_motifs: list[str] = None
       (or Optional[list] — backward compatible default)

    5. In build_thesis, when assembling the LLM prompt/context for the thesis:
       If avoid_motifs is non-empty, append a section like:
         "Structural motifs to AVOID (overused in past PASS alphas): {', '.join(avoid_motifs)}"
       This is the "upstream LLM steer" component of D-15.
       Wording is Claude's discretion (03-CONTEXT.md §Claude's Discretion).
       If avoid_motifs is empty or None: no change to prompt.

    All changes are surgical additions — existing logic is not removed or restructured beyond
    the build_thesis call ordering described above. Do not change the human-stop behavior
    of find_alphas (it still stops before grading — D-18 Phase 2 behavior preserved).
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import find_alphas, researcher, fsa, inspect
# Check find_alphas imports fsa
fa_src = inspect.getsource(find_alphas)
assert 'import fsa' in fa_src or 'fsa.mine_frequent_motifs' in fa_src, 'fsa not imported/used in find_alphas'
assert 'fsa.filter_candidates' in fa_src, 'fsa.filter_candidates not called'
print('find_alphas.py FSA hook: PASS')

# Check researcher.build_thesis accepts avoid_motifs
import inspect as ins
sig = ins.signature(researcher.build_thesis)
assert 'avoid_motifs' in sig.parameters, 'avoid_motifs param missing from build_thesis'
print('researcher.build_thesis avoid_motifs param: PASS')
" && python -c "import find_alphas, researcher; print('Imports OK')"
    </automated>
  </verify>
  <done>
    find_alphas.py imports fsa, calls mine_frequent_motifs before build_thesis, passes avoid_motifs
    to build_thesis, filters candidates via filter_candidates before queueable.
    researcher.build_thesis accepts avoid_motifs parameter and injects it into the LLM context when non-empty.
    find_alphas human-stop behavior preserved (no grading called).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selfcorr hooks → grade_one control flow | Hook B early-return bypasses BRAIN POST /check |
| fsa filter → generation path | FSA filter silently drops candidates before queueing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Denial of Service | Hook B PnL fetch inside grade_one (sequential, in-worker) | mitigate | fetch_and_cache_pnl has graceful degrade (returns None on non-401 errors); None path skips Hook B and falls through to trigger_correlation_check as before. 401 propagates per CLAUDE.md. |
| T-03-11 | Spoofing | Hook B sets status='duplicate' without BRAIN verification | accept | This is the intended optimization (D-08b). The local Pearson filter is calibrated against BRAIN's stored self_corr. D-13 graceful degrade ensures BRAIN POST /check is still used when local PnL unavailable. |
| T-03-12 | Tampering | FSA filter silently drops candidates | accept | Dropping is intentional diversity control (D-14). filter returns the unchanged list if avoid_motifs is empty; cold-start guard in mine_frequent_motifs prevents premature filtering. |
| T-03-13 | Authentication | Hook A/B using the same BRAIN session as grade_one | mitigate | selfcorr.fetch_and_cache_pnl propagates 401 (same auth constraint as all other BRAIN calls). Hook A (proxy_gate) is DB-only — no BRAIN call. |
  | T-03-SC | Tampering | No new pip installs | accept | grade.py and find_alphas.py modifications add no new packages. |
</threat_model>

<verification>
Run after plan execution:
  cd /Users/winter.__.kor/quant && python -c "import grade, find_alphas; print('imports OK')"
  cd /Users/winter.__.kor/quant && python -c "
import inspect, grade, find_alphas, researcher
# grade_one has both hooks and new param
src = inspect.getsource(grade.grade_one)
for check in ['selfcorr.proxy_gate','selfcorr.fetch_and_cache_pnl','parent_alpha_id']:
    assert check in src, f'Missing in grade_one: {check}'
# find_alphas uses fsa
fa_src = inspect.getsource(find_alphas.find_alphas)
assert 'fsa' in fa_src
# researcher accepts avoid_motifs
sig = inspect.signature(researcher.build_thesis)
assert 'avoid_motifs' in sig.parameters
print('All wiring verified')
"
</verification>

<success_criteria>
1. grade.py imports cleanly after modification; existing tests still pass.
2. grade_one has parent_alpha_id parameter (default None) and writes it into alpha row.
3. Hook A: proxy_gate called for mutations; graceful-degrades on missing PnL.
4. Hook B: PnL fetched post-sim; duplicate detected locally → status='duplicate', trigger_correlation_check skipped.
5. find_alphas.py calls FSA mine + filter before queueable, passes avoid_motifs to build_thesis.
6. researcher.build_thesis accepts avoid_motifs; injects motifs into prompt when non-empty.
</success_criteria>

<output>
Create `.planning/phases/03-smart-iteration/03-04-SUMMARY.md` when done.
</output>
