---
phase: 03-smart-iteration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - fsa.py
autonomous: true
requirements:
  - ITR-04

must_haves:
  truths:
    - "fsa.extract_abstract_subtrees(expr) returns a list of operator-shape motif strings parsed via stdlib ast; returns [] on SyntaxError (ternary safety)"
    - "fsa.mine_frequent_motifs(conn) returns [] when fewer than min_samples PASS alphas exist (cold-start guard)"
    - "fsa.mine_frequent_motifs(conn) returns motifs that appear in >= threshold fraction of PASS alphas"
    - "fsa.filter_candidates(candidates, avoid_motifs) drops any candidate whose expression contains a motif in avoid_motifs; is a no-op when avoid_motifs is empty"
    - "fsa.py queries only status='pass' alphas, never ACTIVE (ternary safety per 03-RESEARCH pitfall 3)"
  artifacts:
    - path: "fsa.py"
      provides: "AST subtree mining + frequency counting + candidate filter + avoid-list generation"
      exports:
        - "extract_abstract_subtrees"
        - "mine_frequent_motifs"
        - "filter_candidates"
  key_links:
    - from: "fsa.mine_frequent_motifs"
      to: "alphas table"
      via: "SELECT expression FROM alphas WHERE status='pass'"
      pattern: "status='pass'"
    - from: "fsa.extract_abstract_subtrees"
      to: "Python stdlib ast"
      via: "ast.parse(expr, mode='eval')"
      pattern: "ast.parse"
---

<objective>
Build fsa.py — the Frequent Subtree Avoidance module (ITR-04, D-14..D-15).
Uses Python stdlib ast to mine structural motifs from PASS alphas, counts frequency,
and exports an avoid-list + filter function. The avoid-list is injected into Researcher
and Editor prompts (LLM steer), and filter_candidates is called in the generation path
to drop Ideator candidates structurally identical to over-used patterns.

Purpose: Prevents the autonomous loop from re-discovering the same structural families.
Provides the before/after structural diversity metric for ROADMAP criterion 4.
Output: fsa.py importable by find_alphas.py (plan 04) and hunt.py (plan 05).
</objective>

<execution_context>
@/Users/winter.__.kor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/winter.__.kor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/winter.__.kor/quant/.planning/ROADMAP.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-CONTEXT.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-RESEARCH.md
@/Users/winter.__.kor/quant/.planning/phases/03-smart-iteration/03-PATTERNS.md
@/Users/winter.__.kor/quant/ideator.py
@/Users/winter.__.kor/quant/researcher.py
@/Users/winter.__.kor/quant/db.py
</context>

<interfaces>
<!-- Key contracts from codebase that fsa.py integrates with -->

From ideator.py:
  generate_candidates(conn, thesis) -> list[dict]
    Each candidate dict has key "expression": str
  queueable(candidates: list[dict]) -> list[dict]
    Filters to valid+novel candidates — fsa.filter_candidates runs BEFORE queueable

From db.py:
  alphas table columns: alpha_id, expression, status
  status values: 'pass', 'fail', 'near', 'ACTIVE', 'duplicate', 'invalid', 'timeout', 'error'

From find_alphas.py (plan 04 will wire this):
  find_alphas() calls ideator.generate_candidates() then — after plan 04 — calls
  fsa.mine_frequent_motifs(conn) and fsa.filter_candidates(candidates, avoid_motifs)
  before ideator.queueable()

Integration contract for hunt.py (plan 05):
  avoid_motifs = fsa.mine_frequent_motifs(conn)
  candidates = fsa.filter_candidates([{"expression": e} for e in raw], avoid_motifs)

CLAUDE.md / RESEARCH constraints:
  - Never query ACTIVE alphas for FSA — 6/16 use ternary (?:) syntax
  - ast.parse(expr, mode='eval') raises SyntaxError on ternary — wrap in try/except
  - Threshold and min_samples are Claude's discretion (03-CONTEXT.md §Claude's Discretion)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AST subtree extraction and frequency mining</name>
  <files>fsa.py</files>
  <read_first>
    - 03-RESEARCH.md §FSA Research does not have a dedicated header but see §Standard Stack (ast stdlib entry) and §Anti-Patterns (ternary pitfall, lines 524-527)
    - 03-RESEARCH.md Pitfall 3 (ternary expressions crash ast.parse, lines 566-572)
    - 03-RESEARCH.md §Code Examples lines 694-714 (extract_abstract_subtrees verified implementation)
    - 03-PATTERNS.md §fsa.py (lines 211-287): full module docstring, imports, DB query, AST extraction, filter function patterns
    - ideator.py lines 74-80 — _wrap_vector helper shows the FastExpr operator/field calling convention
  </read_first>
  <behavior>
    - extract_abstract_subtrees("ts_rank(close, 5)") returns ["ts_rank(FIELD,NUM)"]
    - extract_abstract_subtrees("rank(ts_mean(close,20))") returns ["rank(CALL)", "ts_mean(FIELD,NUM)"]
    - extract_abstract_subtrees("close ? open : high") returns [] (ternary → SyntaxError → safe empty)
    - extract_abstract_subtrees("this is not valid python!!!") returns []
    - mine_frequent_motifs(conn) returns [] when fewer than 5 PASS alphas in DB (cold-start guard with min_samples=5 default)
    - mine_frequent_motifs(conn_with_10_pass_alphas_all_using_ts_rank) returns ["ts_rank(FIELD,NUM)"] or similar motifs at >= 0.5 threshold
  </behavior>
  <action>
    Create fsa.py. Module docstring (researcher.py header style), then imports:
    ast, from collections import Counter, sqlite3, from typing import Optional.
    No external dependencies.

    Define module-level defaults (Claude's discretion per 03-CONTEXT.md):
      DEFAULT_THRESHOLD = 0.5   (motif must appear in ≥50% of PASS alphas)
      DEFAULT_MIN_SAMPLES = 5   (cold-start guard: return [] if fewer than this many PASS alphas)

    Private _arg_type(node) -> str:
    - If ast.Call: return "CALL"
    - If ast.Constant and isinstance(value, (int, float)): return "NUM"
    - If ast.Constant (other): return "STR"
    - If ast.Name: return "FIELD"
    - If ast.BinOp: return "BINOP"
    - Else: return "_"
    Mirror exactly the verified implementation from 03-RESEARCH.md §Code Examples lines 706-714.

    Implement extract_abstract_subtrees(expr: str) -> list[str]:
    - Wrap ast.parse(expr.strip(), mode='eval') in try/except SyntaxError: return []
    - Recursive _visit(node): if ast.Call, build fname from node.func.id (else '?'),
      build arg_types list via _arg_type(), append "fname(arg1,arg2,...)" to shapes,
      recurse into each node.args child
    - Call _visit(tree.body) and return shapes
    - Non-Call nodes at root (e.g. BinOp as root) are silently skipped (safe)

    Implement mine_frequent_motifs(conn: sqlite3.Connection, threshold: float = DEFAULT_THRESHOLD, min_samples: int = DEFAULT_MIN_SAMPLES) -> list[str]:
    - Parameterized SELECT expression FROM alphas WHERE status='pass' (never ACTIVE)
    - If len(pass_exprs) < min_samples: return [] (cold-start guard)
    - Counter over all (expr, motif) pairs; use set(extract_abstract_subtrees(expr)) per alpha
      so each motif counted at most once per alpha (not per occurrence)
    - total = len(pass_exprs)
    - Return [motif for motif, cnt in counter.items() if cnt / total >= threshold]
    All SQL parameterized (never string-interpolated).
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import fsa
# Basic AST extraction
shapes = fsa.extract_abstract_subtrees('ts_rank(close, 5)')
assert 'ts_rank(FIELD,NUM)' in shapes, f'Got {shapes}'
print('extract_abstract_subtrees(ts_rank): PASS')

# Ternary safety
safe = fsa.extract_abstract_subtrees('close ? open : high')
assert safe == [], f'Expected [] got {safe}'
print('ternary safety: PASS')

# Cold-start guard (live DB has < 5 pass alphas likely, or inject known count)
import db, tempfile, os, shutil
tmpdir = tempfile.mkdtemp()
tmp_db = os.path.join(tmpdir, 'test.db')
shutil.copy('alpha_kb.db', tmp_db)
conn = db.init_db(tmp_db)
# With default min_samples=5, if fewer than 5 pass alphas, should return []
pass_count = conn.execute(\"SELECT COUNT(*) FROM alphas WHERE status='pass'\").fetchone()[0]
motifs = fsa.mine_frequent_motifs(conn)
if pass_count < 5:
    assert motifs == [], f'Cold-start guard failed: got {motifs} with {pass_count} pass alphas'
    print(f'cold-start guard (pass_count={pass_count}): PASS')
else:
    print(f'mine_frequent_motifs returned {len(motifs)} motifs from {pass_count} pass alphas')
conn.close()
"
    </automated>
  </verify>
  <done>
    fsa.extract_abstract_subtrees returns operator-shape motif strings for valid FastExpr,
    returns [] for ternary/invalid. mine_frequent_motifs returns [] below min_samples threshold,
    returns high-frequency motifs above it. SQL queries only status='pass'.
  </done>
</task>

<task type="auto">
  <name>Task 2: filter_candidates and diversity metric helper</name>
  <files>fsa.py</files>
  <read_first>
    - 03-CONTEXT.md D-15 — filter is the "non-negotiable core"; LLM steer is the bonus
    - 03-PATTERNS.md §fsa.py filter_candidates pattern (lines 276-287): filter a list[dict], return filtered list
    - 03-CONTEXT.md §Specific Ideas — diversity metric: motif-frequency concentration before vs after Phase 3 (top-motif share should drop)
    - ideator.py lines 437-445 — queueable() pattern showing how a filter-and-return function works in this codebase
  </read_first>
  <action>
    Add to fsa.py:

    Implement filter_candidates(candidates: list[dict], avoid_motifs: list[str]) -> list[dict]:
    - If avoid_motifs is empty: return candidates unchanged (no-op)
    - avoid_set = set(avoid_motifs)
    - For each candidate dict (must have "expression" key):
        motifs = set(extract_abstract_subtrees(candidate["expression"]))
        if motifs & avoid_set is non-empty: drop candidate
        else: keep
    - Return list of kept candidates
    - If extract_abstract_subtrees raises (it shouldn't — wrapped in try/except): keep the candidate
      (safe default — graceful degrade per D-13 spirit)

    Implement diversity_metric(conn: sqlite3.Connection) -> dict:
    - Used to compute the ROADMAP criterion 4 before/after metric
    - SELECT expression FROM alphas WHERE status='pass'
    - For each expression, extract_abstract_subtrees; count all motifs
    - Find top motif and its frequency share (top_count / total_motif_occurrences)
    - Return dict: {"pass_alpha_count": int, "unique_motifs": int,
                    "top_motif": str, "top_motif_share": float}
    - If no PASS alphas: return {"pass_alpha_count": 0, "unique_motifs": 0, "top_motif": None, "top_motif_share": 0.0}
    - This function is read-only (SELECT only) — no DB writes
  </action>
  <verify>
    <automated>cd /Users/winter.__.kor/quant && python -c "
import fsa

# filter_candidates no-op when no avoid_motifs
cands = [{'expression': 'ts_rank(close,5)'}, {'expression': 'rank(close)'}]
result = fsa.filter_candidates(cands, [])
assert result == cands, f'No-op failed: {result}'
print('filter_candidates no-op: PASS')

# filter_candidates drops matching
result = fsa.filter_candidates(cands, ['ts_rank(FIELD,NUM)'])
assert len(result) == 1 and result[0]['expression'] == 'rank(close)', f'Filter failed: {result}'
print('filter_candidates drop: PASS')

# diversity_metric structure
import db, tempfile, os, shutil
tmpdir = tempfile.mkdtemp()
tmp_db = os.path.join(tmpdir, 'test.db')
shutil.copy('alpha_kb.db', tmp_db)
conn = db.init_db(tmp_db)
metric = fsa.diversity_metric(conn)
assert 'pass_alpha_count' in metric
assert 'top_motif_share' in metric
print(f'diversity_metric: PASS ({metric})')
conn.close()
"
    </automated>
  </verify>
  <done>
    filter_candidates is a no-op on empty avoid list; drops candidates whose expression
    contains any motif in the avoid set. diversity_metric returns dict with pass_alpha_count,
    unique_motifs, top_motif, top_motif_share — usable for ROADMAP criterion 4 before/after comparison.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB alphas.expression → ast.parse | Expression strings from DB passed to Python eval-like parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | ast.parse on expression strings from DB | accept | ast.parse(mode='eval') is read-only — cannot execute code, only parses to an AST. SyntaxError wrapped; no exec/eval call. Low risk: local tool, DB is trusted source. |
| T-03-09 | Information Disclosure | fsa queries ACTIVE alpha expressions | mitigate | fsa.py queries ONLY status='pass' — never ACTIVE. Ternary ternary safety via SyntaxError catch is defense-in-depth. Verified in 03-RESEARCH Pitfall 3. |
| T-03-SC | Tampering | No new pip installs | accept | Zero new packages; ast, collections, sqlite3 are all stdlib. |
</threat_model>

<verification>
Run after plan execution:
  cd /Users/winter.__.kor/quant && python -c "import fsa; print('fsa.py imports OK')"
  cd /Users/winter.__.kor/quant && python -c "
import fsa
for fn in ['extract_abstract_subtrees','mine_frequent_motifs','filter_candidates','diversity_metric']:
    assert hasattr(fsa, fn), f'Missing: {fn}'
print('All exports present')
"
</verification>

<success_criteria>
1. fsa.py imports cleanly with stdlib-only imports.
2. extract_abstract_subtrees handles valid FastExpr and returns [] on SyntaxError (ternary-safe).
3. mine_frequent_motifs returns [] below min_samples cold-start threshold.
4. filter_candidates is a no-op on empty avoid list; drops candidates with matching motifs.
5. diversity_metric returns a dict suitable for before/after comparison (ROADMAP criterion 4).
</success_criteria>

<output>
Create `.planning/phases/03-smart-iteration/03-03-SUMMARY.md` when done.
</output>
